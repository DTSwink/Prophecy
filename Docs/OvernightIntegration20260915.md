# Active overnight integration work

The full requested goal is not complete. The user woke and requested wrap-up on 2026-09-16; further long-running work is paused pending fresh direction.

Final wrap-up state: standard-component circular/spherical linear limits are now installed through Live Coding. All10 generic/standard native tests passed; actual Blueprint component centered diagonal-drive test gives radius20.000119cm for a20cm limit, with equal X/Y14.14222cm and no Chaos ownership. The original off-center test held the same radius but did not reach symmetric travel within2s; its separate report is retained. Soft-linear settings no longer soften Locked axes. No overhead from the radial solver row for ordinary locked/free-only joints. Evidence Saved/Diagnostics/StandardConstraints/RadialPIE.json, RadialOffCenterPIE.json and BuildRadialConstraints.log. Editor remains open/clean/testNN, PIE off; temporary idle FPS restored60. Latest patches remain due in the next planned normal build; do not restart just to replace a working live patch. Soft angular and other advanced profile parity are still incomplete. The paragraphs below describe earlier checkpoints and are superseded where applicable.

Latest verification, 2026-09-16: named-bone runtime constraint passed on the actual Jolt player's hand_r, native-only ownership, 12 moving samples with maximum anchor gap0.03112cm, and standard Break Constraint cleanup (`Saved/Diagnostics/StandardConstraints/BonePIE.json`). Dodge with Jolt physical following passed25steps against slash; both Dodge and Parry passed25steps against pike (`LiveDodgePhysicalPair`, `LiveDodgePhysicalPike`, `LiveParryPhysicalPike`). Pike's rejection was incorrect: the source harness uses the same sword blade catalog as slashes, now accepted by the live adapter. Installed via successful Live Coding109.26s, no object changes/restart. Physical fixtures explicitly disable automatic target publication before disabling Blueprint Tick and publish the targets themselves; failing to do that caused an initial fixture-only ordering fault, now corrected. Authored assets untouched, testNN open/clean/PIE stopped. Advanced constraint semantics remain open; synthetic spear has no separate live projectile source yet.

Latest live status, 2026-09-16: **Dodge is now installed and exposed through Start NN Dodge**, sharing Stop NN Defense/Get NN Defense Status with Parry. Normal build succeeded (136.65 s); a subsequent private Live Coding patch (96.40 s) keeps captured walk/run weights consistent and sizes Parry buffers separately. Actual testNN actors passed 25 ordered Dodge steps, fixed leg lengths, explicit stop/handoff, close slash and headbutt harmful-contact termination, and mixed walk/run Dodge -> Parry -> Dodge lifecycle while the other defender continued. These are kinematic live-NN checks; the close encounters were harmful hits, not successful evasions. The original complete Dodge (1046 keys) and Parry (nine keys) comparators still pass unchanged. Reports: Saved/Diagnostics/LiveDodgePair, LiveDodgeClosePair, LiveDodgeHeadbuttPair, LiveDefenseLifecycle; usage in Docs/NNDefense.md. Editor is open on testNN, PIE stopped, no dirty packages or temporary constraint fixtures. Remaining scope: broader live attack-source/physical-following cases and advanced constraint semantics. Older entries below describe intermediate states and are superseded by this paragraph.

Latest numerical status, 2026-09-16: COMPLETE fixed Dodge comparison passes all 1046 keys with unchanged atol/rtol=1e-5 (maximum absolute element error 1.52587890625e-5, within the combined absolute+relative tolerance; zero failures). The native trace includes both walk/run neural branches, every intermediate state/control/predictor row, all real and padded proposals, and held terminal trajectory. Only supplied episode inputs seed it; no predicted reference output is fed back. `Tools/NN/ExportDodgeTraceFixture.py`, `Prophecy.NN.Defense.DodgeFullTrace`, and `Saved/DefenseIntegration/CompareNativeDodge.py` reproduce the check using the original comparator. Report: `Saved/DefenseIntegration/Models/unreal_dodge_full_comparison.json`. The earlier six-step mismatch is resolved by locally precise feature arithmetic preventing `/fp:fast` division/reduction rewrites. All 11 defense tests pass, including Dodge's original 13-collider geometry over eight poses; the complete nine-key Parry comparator remains green. Installed through successful Live Coding; editor left open and clean. Dodge gameplay and the other open scope below remain incomplete.

1. Ordinary placed and runtime `UPhysicsConstraintComponent` instances must create native Jolt constraints against the actual imported bodies. Cover endpoint frames/bones/world anchoring, limits, drives, collision suppression, breaking/readback, rebinding and cleanup. Preserve deliberate Chaos worlds and avoid duplicate ownership of the sword controller's own grip. Verify real placed/component and runtime creation paths, not just native joint fixtures.
2. Only `BP_ProphecyManualPoseAgent` gets debug input recording. Variables: Recording Input, Playing Input, Recording Slot, Replayed Input. Capture all keyboard letters plus controller buttons/axes; expose live input even when neither mode runs. Slot recording overwrites when recording starts, playback advances once per actor tick. Provide one initialization function for the user to wire into BeginPlay; do not wire their gameplay functions. Keep core agents unaffected. Verify recording and exact frame playback, slot changes, stopping, and live mode.
3. Integrate saved dodge and parry checkpoints using `C:/Users/singerie/Documents/Cursor/stepper/training/slashes2/saved_defense_checkpoints`. Both reference directories and handoffs exist. Parry is upper-only 258→512→512→90 and requires completed external lower locomotion. First prove exported numerical/intermediate-state parity against each reference, then use actual optimized game inference. Finally test two real scene agents with attacker inference first and previous/next attacker data fed to defense at the same policy step. A prerecorded visualization or isolated model test alone does not satisfy the live test.

Do not declare the goal complete based on a subset. See the progress section for completed and remaining work. Existing saved scene/Blueprint user logic and accepted checkpoints must be preserved except the requested additions.

## Progress, 2026-09-15 late evening

- Native Jolt dependency and normal Editor build succeeded; editor reopened on testNN. Extended `Prophecy.Jolt.Constraints.StandardComponents` passed, including drives, reaction readback, breaking/delegate, world anchor and rebinding. Actual SCS Blueprint-component and runtime PhysicsConstraintActor PIE fixture passed too (`Saved/Diagnostics/StandardConstraints/BlueprintPIE.json`): native ownership, moving anchor, locked offset, world endpoint and standard break. A first editor transient-actor fixture did not enter PIE; this was a fixture failure and source map remained clean. Some advanced Chaos profile semantics (notably multi-axis radial linear limits and angular soft limits) are not yet mapped; do not claim exhaustive constraint parity.
- Manual-only input recorder implemented, saved and verified through actual PIE. Four instance-editable variables and `Initialize Input Recording`, deliberately unwired. Packed-codec automation and exact recorded/replayed frames passed. See `Docs/ManualInputRecorder.md`. Normal build succeeded on 2026-09-16 and Unreal reopened on testNN.
- Exact saved defense source snapshots and checkpoint hashes verified. `Tools/NN/ExportDefenseNetworks.py` exported four neural-only ONNX models into `Saved/DefenseIntegration/Models`; all supplied network calls pass at 1e-5 absolute/relative tolerance (worst absolute error 2.63e-6). This is **only neural parity**, not full defense rollout or Unreal runtime integration. Native geometry/recurrence, live scheduling and actual two-agent defense test remain required.
- Native Unreal ORT inference now passes every saved neural call both individually and batched. `Prophecy.NN.Defense.ConditioningReference` passed 12 conditioning calls with zero difference; `StateReference` passed 48 rebase/carry calls with maximum error 5.97e-8. All three tests passed after the normal build. Full pose reconstruction and live defense integration are still pending.
- Further native parity on 2026-09-16: `GeometryReference` passed 26 saved parry decoder/transported-pose calls (max position error 5.97e-7 m, rotation element error 8.35e-7). `ParryRecurrence` passed all three valid consecutive inferred steps of the supplied episode, running ORT from its own recurrent history; max input/output/pose/state element error 1.58e-6. Completed lower joints stay bit-for-bit unchanged, and duplicate completion is rejected. These additions are currently a successful Live Coding patch. Collider/contact comparison and live scene scheduling remain pending, so this is not yet the complete parry delivery.
- `DodgeLegAndBankReference` passed 14 saved control/leg/root transitions, including six bank balances, gates/drop, modified lower state, signed leg FK and next root. Maximum element error 5.44e-7. The parry tests also passed again after removing redundant leg solves from final upper reconstruction. Dodge's complete frozen lower inference and active scene path remain pending.
- `DodgeUpperTransitionReference` passed 14 complete upper/control/reconstruction/rebase transitions with native ORT output, using each saved lower proposal. Maximum element error 3.97e-6. Both policies expose private Prepare/Complete functions so active agents can share batched neural calls; there is no new default actor/component Tick. A causal dodge lower encoder and checkpoint-specific pin projection are now implemented, but the complete native recurrent rollout test is still being built/verified. Passing isolated upper transitions is not equivalent to passing the full recurrent rollout.
- Complete dodge causal rollout now executes all six real generated frames with both native networks and its own evolving history. All resulting poses, roots, pins, bank balances and recurrent states pass the supplied 1e-5 absolute/relative comparisons. **The strict test still fails** on one final-frame normalized lower velocity input, channel 94: absolute error 1.07288361e-5. No tolerance was loosened. Read-only numerical separation confirms PyTorch on saved input reproduces the oracle exactly; Unreal ORT on identical input differs by up to 2.09e-6 in these calls, while tiny recurrent differences are amplified by the velocity normalization. This is evidence of backend rounding, not proof that every source of accumulation has been isolated. Reports: `Saved/DefenseIntegration/Models/unreal_dodge_causal_diagnostics.json` and `dodge_numerical_analysis.json`.
- Native parry collider/strict swept-contact implementation normal-built on 2026-09-16. `ParryContacts` passed all 20 collider attachments over five reference poses and 80 swept pairs. Maximum collider element error 1.1920929e-7; confirmed blade contact at source frame 3.897614360 versus recorded 3.897614479, zero unresolved pairs. Independent cases cover contact missed by both endpoint overlaps, exhausted search never granting a block, harmful ties, drawn blade presence and arm-label remapping. Geometry is loaded once, active calls use fixed storage; no default Tick or per-agent collider allocation. Full inferred-pose candidate comparison is the next check; actual scene scheduling remains outstanding.
- **Complete fixed parry reference passed**: `ue_parry_candidate.json` now comes from two reconstructed primers plus three sequential native ORT steps, native collider attachment and native swept contact. The original handoff comparator passed all nine required keys with its unchanged atol/rtol=1e-5. Max position error 5.96046448e-7 m, basis 1.78813934e-6, collider axes 1.91619620e-6; roots exact; block-time error 2.38418579e-7 source frames. The first full comparison caught a test-harness primer mistake (raw decoder instead of baseline-transported decoder); corrected to the authoritative `full_fk_globals` workflow, without changing oracle data or tolerances. Report `Saved/DefenseIntegration/Models/unreal_parry_full_comparison.json`. The production geometry/contact code is in the successful normal build; latest candidate-export test is a successful Live Coding patch with no object changes. Unreal remains open on testNN, no authored assets changed. **This passes the supplied fixed parry episode, not live two-agent gameplay or all attack kinds.**
- **Live parry integrated and tested, 2026-09-16**: optional shared manager runtime, `Start NN Parry` / `Stop NN Defense` / `Get NN Defense Status`; no authored Blueprint wiring. Normal build succeeded (124.58 s), followed by a successful private-code Live Coding optimization. Two actual testNN actors ran 25 native defense steps with exact newly completed attacker frames; stopped inference stayed stopped. A second encounter approached through normal locomotion to 124.87 cm and ran eleven steps before a right-thigh harmful hit terminated the defense (not a successful block). A three-actor lifecycle covered drawn blade + undrawn arm, shared batch 2→1→2, independent stop, fresh restart/right arm, and opponent-end cleanup. All PIE fixtures passed and ended cleanly. Reports under `Saved/Diagnostics/LiveParryPair`, `LiveParryClosePair`, `LiveParryLifecycle`; usage/limits in `Docs/NNDefense.md`. These tests use kinematic presentation and actual live NN policies, not prerecorded poses. The latest optimization builds only the selected attacker collider and preserves its full axes for CCD. Dodge gameplay, the strict dodge numerical mismatch, pike/synthetic spear attack-source metadata, and remaining advanced constraint semantics still prevent overall completion.
