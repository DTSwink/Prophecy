# Effective character rig and controller audit

**Subsequent implementation decisions/evidence, 9 September 2026:** the user approved stock Jolt hard angular limits with unchanged PHAT angles and no retuning; a custom soft constraint is deferred. This supersedes the open soft-limit choice described in the historical audit below. The corrected [32-channel runtime audit](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_scope_rig_32channels_20260909_0819.json>) completed FullSim/NativeWorld/Passive without report errors or nonfinite transforms and supersedes the earlier channel-response arrays. Current native manual capture/replay implementation and validation are tracked in [JoltIntegrationStatus.md](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltIntegrationStatus.md>).

Source and evidence audit, **9 September 2026**, UE **5.7.4**. The original source pass and this documentation update were read-only. This update additionally analyzes the separately completed CDO/SCS commandlet and native runtime benchmark outputs identified below; no further process, build, mode switch or asset save was run to write it. This note supplements [JoltAssetInventoryFindings.md](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltAssetInventoryFindings.md>) and capability rows C01–C07/C11/C13. It records existing behavior, not permission to change it.

**Finding:** an authored PHAT profile is not the effective rig. The new native runtime snapshots confirm all-Free angular axes for the HalfSim NativeWorld/Passive controller cases and Limited179° with soft flags for ordinary native FullSim. The current manual-agent Blueprint CDO has manual pose application enabled and its separate SCS `PhysicalMesh` explicitly overrides the Physics Asset to the 22-body/21-joint `PA_UEFN_Mannequin`. Its manual Physical branch does neither ordinary angular override. The remaining question is the actual placed/spawned actor's initialized mode, profile history and controller execution; the CDO and SCS themselves are now inspected. Sterile `FullSim` still uses the native force/torque controller, not the manual follower's PreIntegrate velocity controller.

## Authored starting point

The fresh manifest at `Saved/JoltMigration/Inventory-20260909-051135/AssetManifest.json` contains both candidate character assets:

- `/Game/Characters/UEFN_Mannequin/Rigs/PA_UEFN_Mannequin.PA_UEFN_Mannequin`, referenced by the native-default `/Game/_mygame/SKM_UEFN_Mannequin` mesh's registry tags.
- `/Game/_mygame/MetaHumans/BossUEFN/PA_Mine.PA_Mine`, referenced by the Boss mesh's registry tags.

Each has 22 bodies, 21 constraints, 18 capsules, four boxes and 47 disabled collision pairs. **All 21 cone structs and all 21 twist structs have `bSoftConstraint=true`.** Authored motion makes soft swing eligible on all 21 constraints, and soft twist eligible on 13; the other eight twists are Locked. All joints enable projection, mass conditioning and the linear joint solver; all bodies enable inertia conditioning. The authored asset solver counts are 6/1/1.

The named `Free` profile is a separate authored profile: 17 joints are fully angular-Free; four retain Swing2/Twist Locked, namely constraint indices 7 (`lowerarm_l`), 11 (`lowerarm_r`), 16 (`calf_l`), and 19 (`calf_r`). HalfSim's explicit all-Free override is therefore **not equivalent to applying the named `Free` profile**.

These counts describe asset data. They do not establish a placed component's Physics Asset override, current joint validity, active profile, live mass/inertia, current engine CVars, or actual limit contact during motion. The manifest exporter reported no own errors and no asset modifications; its enclosing commandlet nevertheless failed on the already documented preexisting Blueprint ensure/warning. No successful clean commandlet exit is claimed here.

## Branches that establish the live rig

| Entry/control path | Confirmed angular/body behavior | Consequence for soft limits |
|---|---|---|
| `SetSimulationMode(Physical)`, ordinary mesh, `RootAndJointTorque` | [ProphecyAgent.cpp:1710](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:1710>): excluded bodies stop simulating; a constraint whose child is excluded or is the physical root is terminated. Every other joint gets Swing1/Swing2/Twist Limited at `clamp(PhysicalAngularLimitDegrees,0,179)`. Mass conditioning follows the actor property; SLERP drives are disabled; default motor-disable also disables twist/swing drives. Called on ordinary Physical entry at `:1897`. | Native angle default is 179°. Setters preserve existing softness, stiffness, damping and restitution. For a fresh candidate rig with every body included, all 21 twist soft flags now accompany Limited axes, including the eight formerly Locked twists. Wide limits are not Free limits; actual force activation remains motion-dependent. |
| `SetSimulationMode(Physical)`, `bManualNNPoseApplication=true` **and** separate pose-reference mesh | [ProphecyAgent.cpp:1797](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:1797>): simulate/blend bodies below the root, clear the animation instance, wake, apply MACD and solver counts; return before ordinary rig configuration. | No angular-limit/profile override here. Authored defaults, component creation, earlier live profile mutations and mode history determine the retained articulation. Do not apply the ordinary 179° conclusion to this branch. |
| Ordinary `PerBodyWorld` Physical | `SetSimulationMode` applies `FPhysicalAnimationData` below the root and enables the native physical-animation component; it does not call `ConfigureRootAndJointTorquePhysics`. | No general anatomical angular-limit replacement in this branch. Native target drives and anatomical limits are separate constraints/contracts. |
| Enter HalfSim, or change any of its eight methods | [ProphecyAgentHalfSimulation.cpp:180](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgentHalfSimulation.cpp:180>) calls [ProphecyHalfSimDriveComponent.cpp:44](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyHalfSimDriveComponent.cpp:44>). Its loop at `:57` sets all three angular motions Free, limits to zero, disables twist/swing drives, selects SLERP and enables SLERP only for joint-motor methods. This runs for NativeWorld/Local/LocalPelvis, JointMotors/JointMotorsPelvis, WorldForcePD, Passive and WorldOneStep. | No anatomical angular limit is effective immediately after this configuration, even if soft bits remain true. Linear attachment constraints, mass/inertia conditioning and drives can still affect the result. Subsequent external calls could override it; this source invariant is not a perpetual runtime guarantee. |
| Leave HalfSim | Entry saves each live `ProfileInstance` by `JointName` (`ProphecyAgentHalfSimulation.cpp:105`). Exit copies saved profiles back (`:232`, and the other exit branch). Manual Physical return restores them directly; ordinary Physical return calls `SetSimulationMode` again and can reapply the 179° override. `Driver::Stop` disables motors but does not restore angular limits itself. | History matters. Returning to manual Physical can restore the pre-HalfSim soft articulation. Stopping a controller alone is not proof that authored limits returned. |
| `MySetPhysicsAsset` | [ProphecyAgent.cpp:1955](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:1955>) snapshots body transforms/velocities/blend/simulation/gravity/awake state and recreates the physics asset with `SetPhysicsAsset(New,true)`. HalfSim refresh frees the new joints; ordinary nonmanual Physical reapplies its drive branch; separate manual Physical does not run the 179° configuration. | The saved runtime state is per-body, not an arbitrary live per-joint profile snapshot. Recreated asset defaults and the reconfiguration branch govern new joints; HalfSim exit still has its earlier saved profile map. Asset identity and mode history must be present in replacement acceptance evidence. |
| `AProphecyNativePhysicalAgent` experimental control | [ProphecyNativePhysicalAgent.cpp:185](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyNativePhysicalAgent.cpp:185>) selects authored DefaultInstance motions/angles if `SetNativeUseAuthoredAngularLimits(true)`, otherwise all angular motions Free. Default is false; activation calls this control after Physical entry. It uses `PerBodyWorld` and manual=false. | This explicit public control can restore authored Limited/Locked motion. It does not copy the whole authored profile, so it is not proof all other profile fields were reset. |

The UE setters confirm preservation of soft data: [ConstraintInstance.h:452](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/Engine/Classes/PhysicsEngine/ConstraintInstance.h:452>), `:484`, `:516` only assign motion and angle, then call `UpdateAngularLimit`. [ConstraintInstance.cpp:211](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/Engine/Private/PhysicsEngine/ConstraintInstance.cpp:211>) pushes the existing cone/twist structs to an unbroken live constraint. Chaos also sanitizes its solver settings, including Locked-axis softness and unused angles; see [PBDJointConstraints.cpp:406](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDJointConstraints.cpp:406>). Record live constraint validity and converted settings, not only the UObject-side profile.

`ApplyPhysicalSolverSettings` ([ProphecyAgent.cpp:2619](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:2619>)) writes per-body Chaos position/velocity/projection counts, clamped to minimum 1/1/0. Native defaults are **4/1/0**, so these supersede the asset's 6/1/1 on paths that apply them. This function does not change angular motions, soft flags or joint projection/conditioning properties. A joint's `bEnableProjection=true` alone does not show that projection ran.

`SetPhysicalDriveMode` is restricted to Kinematic while the drive is not configured (`:2557`). `ApplyPhysicalDriveSettingsNow` refreshes HalfSim or changes native physical-animation data/strength/ticking (`:2568`); it does not re-run the ordinary anatomical limit configuration. Directly editing an exposed actor property and calling this method therefore does not establish that every associated live joint property was updated.

A bounded search found **no current project native call or exported Blueprint node** for `SetConstraintProfile`, `SetConstraintProfileForAll`, `ApplyPhysicalAnimationProfileBelow` or a function named `SetPhysicalAnimationProfile`. That is not proof no editor/tool workflow ever applies a profile. UE's actual distinction is important: `SetConstraintProfile[ForAll]` copies anatomical constraint profiles ([SkeletalMeshComponentPhysics.cpp:1454](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/Engine/Private/SkeletalMeshComponentPhysics.cpp:1454>)); `ApplyPhysicalAnimationProfileBelow` looks up **body drive data** ([PhysicalAnimationComponent.cpp:150](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/Engine/Private/PhysicsEngine/PhysicalAnimationComponent.cpp:150>)). The project currently calls `ApplyPhysicalAnimationSettings[Below]` with data instead.

The diagnostic `prophecy.Physical.OneFrameParity` is **not a read-only rig audit**: it freezes manager/agent ticking, changes simulation, contacts, gravity and possibly limits, then pauses the world. Its `OneFrameAngularLimits` CVar is only consumed inside that command ([ProphecyAgent.cpp:495](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:495>), `:587`, `:732`). The CVar default label “production” means keep the result of Physical entry, not necessarily restore authored PHAT angles. Do not invoke this command to discover unmodified production state.

## Controllers are different contracts

| Controller | Existing rule and timing |
|---|---|
| Manual follower, PreIntegrate | [ProphecyAgent.cpp:92](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:92>): `V := V + ((TargetPosition-X)/h - V)*LinearStrength`; shortest-quaternion axis/angle gives `W := W + (Axis*Angle/h-W)*AngularStrength`. It rewrites Chaos velocities in PreIntegrate, not in its empty PreSimulate callback. Targets are published beforehand; `h` is the published maximum-step value, `min(frameDt,MaxSubstepDeltaTime)` if substepping, otherwise frameDt, rather than a callback query for actual solver substep time. Publication applies the current bone-to-body offset to finalized interpolated NN world targets (`:320`). Strengths are nonnegative, not clamped to one; zero skips that velocity rewrite. This callback does not read `bCancelGravity`. |
| Ordinary native RootAndJointTorque, including sterile `FullSim` | [ProphecyAgent.cpp:2081](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:2081>) samples/interpolates the pose store and calls `ApplyBodyWorldMagnetization` (`:2864`), which delegates to `ApplyOneStepBody`. Linear acceleration is `(Target-X-V*dt)/dt²`, with gravity subtraction **before** multiplying strength. Angular acceleration is `(Axis*Angle/dt-W)/dt*strength`. Both use `AddForce/AddTorqueInRadians(..., allowSubstepping=true, accelChange=true)`. This is not the manual callback; timing, gravity and target frame construction differ. |
| HalfSim WorldOneStep | Uses the same `ApplyOneStepBody` math ([ProphecyHalfSimDriveComponent.cpp:180](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyHalfSimDriveComponent.cpp:180>)), but builds component-space targets from animation-local transforms and the kinematic component carrier, not a physics-blended pose (`:131`). Per-body/base gain ratios and the drive multiplier determine strength. Gravity is passed only for a gravity-enabled body. |
| HalfSim WorldForcePD | At `:161`, implicit PD acceleration is `(Kp*Error-(Kd+Kp*dt)*Velocity)/(1+Kd*dt+Kp*dt²)`. Linear force cap divides by body mass; capped angular response converts acceleration through rotated inertia to torque. No `BodyCancelGravity` subtraction is used in this branch. Do not substitute OneStep or reinterpret gain values as native joint gains. |
| HalfSim native and joint motors | `SetBodySettings` (`:96`) routes world/local native target drives and pelvis support independently. Joint modes update SLERP gain/force parameters with Chaos angular-drive conversion scales and enable joint updates from animation. Unsupported pelvis variants remain distinct exposed methods. NativeWorld is the actor's default HalfSim method (`ProphecyAgent.h:353`). |

The constructor initializes only nine lower-body entries in `BodyMagnetizationSettings`; `IsBodyConfiguredForSimulation` returns false for a missing entry (`ProphecyAgent.cpp:2738`). The benchmark explicitly calls `SetAllBodyMagnetization(true,1,1)` to populate all PHAT bodies. Do not infer an arbitrary production actor's driven-body count from the benchmark's 22 bodies. HalfSim body-strength application checks enabled/strength/cancel-gravity fields, but does not use `bSimulateBody` as an exclusion test in that function (`ProphecyAgentHalfSimulation.cpp:151`); retain this distinction in acceptance cases.

`EnsureManualSimulation` ([ProphecyAttackFists.cpp:140](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAttackFists.cpp:140>)) only ensures a reference-local/finger animation proxy for a begun-play manual Physical actor that has no animation instance. It does not rename meshes, change constraints, or enable the ordinary drive path. The actual `GetPoseReferenceMesh` selection is at [ProphecyAgent.cpp:1242](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyAgent.cpp:1242>): manual=true selects a separate component named exactly `PhysicalMesh`, otherwise returns inherited `Mesh`.

The NN manager copies the Blueprint CDO's manual flag onto **spawned** agents, preserves the existing flag on **placed** agents, establishes tick prerequisites, and clears/hides the inherited `Mesh` for manual agents ([ProphecyNNLocomotionManager.cpp:2450](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Source/GameAnimationSample3/Private/ProphecyNNLocomotionManager.cpp:2450>), `:2483`, `:2525`). This normally makes ordinary `ApplyAbsoluteWorldMagnetization` return early for a manual shell, since that function requires the inherited Mesh asset. Automatic callback publication occurs after `Super::Tick`/Blueprint ReceiveTick and manual-animation reconciliation (`ProphecyAgent.cpp:1510`). Current CDO/placed flags and Blueprint publication calls must be recorded to prove actual ordering.

## What existing exports do and do not close

The current exported `BP_ProphecyManualPoseAgent` derives from `/Script/GameAnimationSample3.ProphecyAgent`. It contains a linked `SetSimulationMode` node in `begin_f2`, GUID `8BE1C8704C4BA09F31AB59A9CB9C10C6`, default Kinematic, and a linked EventGraph mode control GUID `E21E704A45DBE310AA06BA8956BDA63E` whose mode pin has a source connection. Two copies in `tick debugging` have no input Exec connection. Their presence does not establish which mode is currently selected in a running actor.

The retained [ComparePhysicalFollowBlueprints.out.txt](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/ComparePhysicalFollowBlueprints.out.txt>) has filesystem modification time **2026-08-13 04:53:42 local**. Its script uses `CDO.get_components_by_class`, not SCS/inherited-template enumeration. The pose-agent result lists only Capsule, Mesh and PhysicalAnimation; Mesh references SKM_UEFN with no Physics Asset override. The CMC result lists CharacterMesh0 with the same mesh and no override. This is historical CDO evidence, **not evidence that the current Blueprint has no PhysicalMesh**.

`Saved/CodexInspectManualBlueprintComponents.py`, `CodexInspectManualFollowerDefaults.py`, `CodexInspectManualFollowerPIEDetails.py` and `InspectLiveDriveSettings.py` are retained inspection scripts, not fresh results. `ProphecyInspectKellanProductionComponents.log` records a May 2026 MetaHuman reference-map material/component inspection, not this physics test's live rig. The broad asset manifest omitted CDO nonreplicated properties and SCS templates; the targeted report below now closes those omissions for the selected Blueprint, while construction results and placed/runtime actors remain unobserved.

## Current CDO/SCS evidence, 07:54 local

Source: [Components.json](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/ComponentAudit-20260909-075439/Components.json>), recorded `2026-09-09T05:54:49.328Z`, UE `5.7.4-51494982+++UE5+Release-5.7`. SHA-256: `58b4a406eaf87e56796a4c71fb1d52007aa425999d55f725ab1273c4f255fca5`.

The selected class is `/Game/_mygame/locomotion/BP_ProphecyManualPoseAgent.BP_ProphecyManualPoseAgent_C`, directly parented to native `ProphecyAgent`, then Pawn/Actor/Object. There are **33 selected actor properties, five CDO components, one SCS node and zero inherited component override records**. There are no parent Blueprint classes in this hierarchy. The five CDO components are Camera, Capsule, Mesh, PhysicalAnimation and SpringArm. The separate PhysicalMesh appears in the SCS, not in that CDO component array; its actor CDO object-reference property is `None`, which does not imply a missing SCS component.

| Current actor CDO setting | Recorded value |
|---|---|
| Manual route and default mode | `bManualNNPoseApplication=true`, `SimulationMode=Kinematic`, `PhysicalDriveMode=RootAndJointTorque`, `HalfSimDriveMethod=NativeWorld`, root `pelvis` |
| Automatic execution | AutoPublishManualFollowerSubstepTargets, AutoApplyWorldMagnetization, AutoInitializeAgentRuntime and AutoEnsureStandaloneNNManager are all true |
| NN, fingers and debug | NNInferenceEnabled=true; EnableAttackFists=true; ShowKinematicDebugMesh=true; UpperNNHasSword=false; AutoPossessPlayer/AI both Disabled |
| Global control and constraints | WorldMagnetizationEnabled=true; linear/angular scales=1; drive multiplier=1; MACDEnabled=true; GeneratePhysicalHitEvents=false; angular limit=179°; mass conditioning=true; disable constraint motors=true; update joints from animation=false |
| Solver counts and drive data | Actor counts 4/1/0; OrientationStrength=60000, AngularVelocityStrength=500, PositionStrength=40000, VelocityStrength=400. The export omits unchanged struct defaults, so it is not a list of every drive field. |
| Per-body defaults | `BodyMagnetizationSettings` has exactly the nine native lower-body entries: pelvis; thigh/calf/foot/ball left and right, each exported with unchanged struct defaults `()`. The type defaults enable simulation, magnetization and gravity cancellation with linear/angular scales 1. PhysicalFeedbackTolerances is empty; the Blueprint `physical bones` ArrayProperty exports an empty default. Runtime graph population is not observed. |

The sole SCS node is `SimpleConstructionScript_0.SCS_Node_2`, variable **PhysicalMesh**, GUID **`4BFD9D70413B729BDA1D659A53FB705E`**, attached to native **Capsule**, with no socket or child SCS node. `GetActualComponentTemplate(childBPGC)` resolves to the declared template itself (`hasEffectiveOverride=false`):

`/Game/_mygame/locomotion/BP_ProphecyManualPoseAgent.BP_ProphecyManualPoseAgent_C:PhysicalMesh_GEN_VARIABLE`

It is a stock `USkeletalMeshComponent`, uses `/Game/_mygame/SKM_UEFN_Mannequin.SKM_UEFN_Mannequin`, and has an **explicit `PhysicsAssetOverride`** to `/Game/Characters/UEFN_Mannequin/Rigs/PA_UEFN_Mannequin.PA_UEFN_Mannequin`, resolved as 22 authored bodies and 21 authored joints. This is stronger evidence than the earlier mesh registry tag. Its relative translation is **(3.935413, 0, -90.022712) cm**, rotation zero and scale (1,1,1). Inherited Mesh instead has translation (0,0,-86) and no explicit PHAT override, though it resolves the same mesh/PHAT.

PhysicalMesh template defaults are AnimationBlueprint with AnimClass=None, AlwaysTickPoseAndRefreshBones, URO=false, SkipSimulatingBones, SimulationUpdatesComponentTransform (UE's serialized enum spelling is `SimulationUpatesComponentTransform`), update-joints=false, per-poly collision=false and visible/not hidden. Its component BodyInstance defaults have **simulate=true, gravity=true**, auto-weld=false, start-awake=true, inertia-conditioning=true, CCD=false, MACD=false and notify-hit=false. Stored body iteration values are 8/2/1 with `bOverrideIterationCounts=false`; these are neither proof of live per-bone solver counts nor substitutes for the actor's 4/1/0 overrides. Likewise its `MassInKgOverride=100` has `bOverrideMass=false` and is not a measured 100 kg body.

The full **32-channel template collision** is Custom/QueryAndPhysics, object PhysicsBody. It ignores Visibility, Camera, EngineTraceChannel1, Traversable and sword_only; every other reported channel blocks, including WorldStatic/WorldDynamic/PhysicsBody, both Prophecy agent channels and floor. The Capsule CDO is QueryAndPhysics with ProphecyAgentCapsule object type, blocks all except Visibility/Camera, and has radius 30/half-height 86. Inherited Mesh is NoCollision with ProphecyAgentLimb object type. These are CDO/template settings, not the combined live shape filters after BeginPlay/manager/mode changes.

**Initialization question:** the actor CDO says Kinematic while PhysicalMesh's template says simulate=true. Native `GetSimulationMode` can report Physical for a manual separate mesh that actually simulates, and Blueprint initialization/manager binding can change its state. Neither declaration alone establishes the mode after startup. ShowKinematicDebugMesh=true also remains a recorded default, not evidence of an extra rendered mesh in the synthetic benchmark. A production observer must record the instantiated component and final mode/control state after the actual initialization chain.

**Commandlet outcome:** the JSON has `errors=[]`, `successWithinDeclaredScope=true`, no commandlet asset saves/maps/spawns/construction-script/explicit compilation requests, and `blueprintPackageDirtyAfterLoad=false`. [Unreal.log:1116](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/ComponentAudit-20260909-075439/Unreal.log:1116>) reports extraction `errors=0`; the next line reports commandlet Main result 0. The enclosing process nevertheless exited **1**, and the engine summary says **40 errors, one warning**. The stack and summary identify the **same preexisting** `/Game/_mygame/NewFunctionLibrary` **Ang Spring duplicate WorldContext entry-pin handled ensure**, at `K2Node_FunctionEntry.cpp:417` ([Unreal.log:1075](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/ComponentAudit-20260909-075439/Unreal.log:1075>)). Those counted error lines include the ensure's stack, not 40 distinct extraction defects. The stack passes through engine dependency-load Blueprint reconstruction/compilation-queue flushing; this is load-time engine behavior, despite no explicit commandlet compile call. This particular log does **not** show the earlier A_Sword depth-node warning. Preserve the successful bounded extraction and the failed overall process outcome separately; this is not a new migration regression or a clean commandlet pass.

## Completed synthetic runtime rig evidence

[jolt_scope_rig_20260909_0745.json](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_scope_rig_20260909_0745.json>), SHA-256 `ebcb1e9bb44fb5e869b85b8bcda2852cb8b98fb29ab493839f60d2c74ea81f11`, records **one agent**, floor/gravity, 30 warmup frames and 60 samples per case, at fixed 1/60 with substepping=false and async_physics=false. It contains three cases with empty top-level error. This is a one-agent rig audit, not a one-body fixture: every before/after snapshot has 22 dynamic bodies, 21 valid anatomical joints and zero Blueprint actors/nonfinite transforms.

| Case | Confirmed before and after live joint state | Configured native targets |
|---|---|---|
| FullSim | All 21 joints: Swing1/Swing2/Twist Limited179°, soft swing/twist=true | 0 |
| NativeWorld | All 21 joints: every angular axis Free, reported limit angles 0; soft flags remain true but no angular limit is eligible | 22 |
| Passive | Same all-Free anatomical state as NativeWorld | 0 |

All resolve the UEFN PHAT above. Both soft angular conversion CVars are captured at 100000 stiffness/1000 damping. The output also records per-body mass/inertia and body/mass frames and each joint's frames/current reflected profile. These establish current live `FConstraintInstance` settings in the synthetic paths; they do not observe a soft-limit violation force, prove a numerical Jolt equivalent, or identify a production Blueprint's profile history. NativeWorld and Passive here use the shared HalfSim controller on native benchmark actors; **they are not AgentHalfSim mode-transition cases**.

[jolt_chaos_baseline_100_20260909_0747.json](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_chaos_baseline_100_20260909_0747.json>), SHA-256 `d5e82e22f90af7376acbbcbfc74a39638c28157de424c356b4fb06db5ddc8b4e`, records six floor/gravity passes: **100 agents, FullSim and NativeWorld, repeats 0/1/2, 120 warmup frames and 300 samples each**, with empty error and the same fixed-step/substep/async settings. Every before/after total is 100 meshes, 2200 dynamic bodies, 2100 anatomical joints and zero Blueprint actors/nonfinite transforms. FullSim has zero native target constraints; NativeWorld has 2200. Its detailed rig record deliberately covers only the first mesh in each case; all recorded first-mesh joints retain the corresponding Limited179° or Free state before/after. This note makes no performance comparison or production acceptance claim from those timings.

**Diagnostic limit in these two immutable reports:** each per-body `channel_responses` array has 33 entries. UE's response container has only 32 actual channels; index 32 is the deprecated `ECC_OverlapAll_Deprecated` sentinel and is not valid channel evidence. Ignore that trailing entry and correct the diagnostic before subsequent runs. The valid entries are raw BodyInstance responses, which can differ from the component response settings and do not alone describe combined final Chaos shape filters. This issue was reported to the root; no benchmark/code/report bytes were edited by this documentation pass. The joint-state and count observations above use separate fields.

## Smallest additional read-only observation

The opt-in `-PhysicsBenchRigAudit` output has now supplied the synthetic before/after observations above, outside timed samples, and the targeted component commandlet has supplied current CDO/SCS defaults. Repeating those static inventories is not the missing step. Their remaining limit is the actual production actor after initialization and mode/profile changes.

For the current production manual follower, the smallest useful audit is one identity-resolved actor from the actual manager/player selection, plus distinct configuration groups if multiple shells differ. Observe the current authorized run and its existing mode changes; do not change properties or call parity/setup helpers merely to inspect them.

1. **Identity and applied defaults:** current map/world/PIE instance, actor path/class, placed versus manager-spawned, manager `AgentClass` and handle. Compare the initialized actor's manual/automatic-execution flags, root name, drive/mode/method, strengths, per-body map and solver counts with the now-recorded CDO. Record placed overrides and the instantiated PhysicalMesh/inherited Mesh/capsule/PhysicalAnimation identities, resolved assets and scales. The selected Blueprint's SCS/default PHAT no longer needs to be guessed.
2. **Initialized live state:** after BeginPlay, manager binding and a completed frame, record selected pose-reference component, anim class/source, carrier/bone-update modes, tick prerequisites, current enum and actual body-simulation state. Capture full collision object/response/enabled state for mesh and capsule, component/body gravity, CCD/MACD, notification flags and disabled pairs. This distinguishes the current controlled-contact experiment from the intended production matrix.
3. **All 21 actual joints and 22 actual bodies:** joint validity/broken state, names/bone mapping, complete current profile, frames, motion/angles/soft parameters, motor state, projection/mass conditioning; body simulation/awake/blend, transform, COM, mass/inertia and effective per-body solver flags/counts. Where possible record the locked/read-only converted Chaos joint settings as well as `ProfileInstance`, since conversion/sanitization and global settings are additional layers. Read current `p.Chaos.JointConstraint.SoftAngularStiffnessScale` and `SoftAngularDampingScale`; UE source defaults are 100000 and 1000 ([ChaosConstraintSettings.cpp:23](<C:/Program Files/Epic Games/UE_5.7/Engine/Source/Runtime/PhysicsCore/Private/ChaosConstraintSettings.cpp:23>)). Also retain active solver/drive override CVars and timestep/substep/async configuration.
4. **History only where relevant:** take the same snapshots after an already authorized HalfSim entry and return, method change or asset replacement. Tag the stage, frame, source/DLL/asset provenance and selected controller. A free HalfSim snapshot cannot close manual Physical restoration. Read target publication/callback presence and body target count without rewriting velocity, waking bodies or forcing bone refresh.

This is the evidence needed to classify soft angular limits as effective, ineligible or still unverified for each tested path. An existing successful motion sample inside 179° does not prove soft limits can be removed from the retained control surface. **No current-production soft-limit blocker is demonstrated solely by the asset flags.** A confirmed Limited+soft live path remains a behavior-equivalence gate under the research/plan: do not silently substitute hard limits, remove limits, or change the test route to avoid implementing a retained behavior. The demonstrated immediate testing gap is that the sterile native FullSim benchmark does not cover the production manual PreIntegrate contract.

## Original short launch example — distinct from the completed records above

The inspected [RunSterilePhysicsBenchmark.ps1](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Tools/NN/RunSterilePhysicsBenchmark.ps1>) starts an existing `UnrealEditor-Cmd.exe` binary in a hidden background process, sets BelowNormal priority and returns PID/report/log JSON immediately. It does not build or wait for completion. Its default counts are 100 agents, 120 warmup frames, 300 samples, three repeats. A short diagnostic baseline using the existing native cases is:

```powershell
& 'C:/Users/singerie/Documents/Unreal Projects/Prophecy/Tools/NN/RunSterilePhysicsBenchmark.ps1' -Count 100 -Warmup 60 -Samples 180 -Repeats 1 -Methods 'FullSim,AgentHalfSim' -FloorOnly -Label 'chaos_jolt_phase0_short_20260909'
```

That report label did not exist when checked in the original source pass; the script refuses an existing JSON report. The current launcher now exposes **`-RigAudit`**, forwarding `-PhysicsBenchRigAudit`. The example above does not include it and was not used for the two completed records documented above; those records used NativeWorld rather than AgentHalfSim and their own retained labels/counts.

The underlying command uses `GameAnimationSample3.uproject`, `/Engine/Maps/Entry?game=/Script/Engine.GameModeBase`, `-game -nullrhi -RenderOffscreen -nosound -unattended -NoSplash -NoLoadingScreen -NoLiveCoding -NoScreenMessages -NoVSync -nowrite -ProphecyPhysicsBenchmark`, the four numeric arguments, report/log paths, and optional Methods/FloorOnly flags. The benchmark sets fixed delta 1/60 and disables substepping; it reports the current async-physics setting rather than explicitly disabling async. It refuses Blueprint actors, uses native synthetic pose data, seeded case shuffling, and no rendering/NN workload.

For these two cases it spawns native AProphecyAgent with **manual=false**, automatic manual-target publication=false, automatic native magnetization=true, the SKM_UEFN mesh and every PHAT body enabled. `FullSim` selects ordinary Physical; `AgentHalfSim` selects HalfSim with its native-default NativeWorld method. Both then receive WorldStatic-only mesh contacts, gravity for the floor fixture, 4/1/0 per-body iterations and CCD/MACD=false. The floor is a native box; PhysicsBody is ignored, so no self/crowd contacts are measured. Every frame wakes bodies, and a head impulse with **velocity-change=true** is applied at warmup+30 frames. This is not a mass-scaled collision impulse fixture.

The short request contains two passes of 60+180 fixed frames (eight seconds of simulated time, not a wall-time promise). Its measurements are world actor/physics tick and frame interval; they are not solver-only CPU cost or rendered FPS. Existing quality fields are before/after snapshots, not full contact/trajectory parity. Completion requires the report's error field, expected pass/sample counts, finite body state, output log and actual process outcome; the returned PID alone is not success. One repeat is a smoke baseline only. Phase 0/P2 performance acceptance still needs repeated matched runs, the actual manual controller path, active rig provenance and the retained quality/contact cases.
