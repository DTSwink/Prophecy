# Recurring editor crash: 2026-09-11 08:14 local

Status: unresolved. The user reports these crashes began after the Jolt integration. Treat that correlation as a regression lead; the crash stack alone cannot exonerate the integration.

Evidence: `Saved/Crashes/UECC-Windows-24E7B3F447FFDFC0AD319B9683FD959E_0000/` (original log, context and dump).

- Normal-build editor PID 8176, running for 2,226 seconds. No Live Coding compile/reinstancing in this session.
- Fatal CPU access violation reading zero in Windows DXGI, on RHIThread. UE call site is `FD3D12Viewport::PresentInternal`, `WindowsD3D12Viewport.cpp:388`: `SwapChain1->Present(SyncInterval, Flags)`.
- Last PIE ended at 08:00:04; fatal at 08:14:57. This was not an active game-physics step and not requested shutdown (`IsRequestingExit=false`). Earlier integration memory corruption or rendering/lifecycle effects remain possible.
- The older 01:57/04:55 crashes failed in DXGI swap-chain creation for Slate notification windows. This one fails in presentation, not the identical instruction/stack. The 07:16 `FSimpleAssetEditor` shutdown crash was a separate failure with a verified correction to our restart helper.
- Active RHI was D3D12 on adapter 0 (RTX 4060 Laptop GPU), NVIDIA 566.14. Intel UHD is also installed; the generic PrimaryGPUBrand field is not evidence that UE rendered on Intel.
- Memory counters in this crash context are zero/unavailable and must not be interpreted as an OOM diagnosis. `bIsOOM=false`. The bounded System log query returned no entries for 08:12–08:16. An audio timeout logged alongside the crash does not establish its cause.

No root-cause patch has been established for these recurring DXGI failures. The force-node integration was still at source inspection; no new force routing was installed in this session.

Next discriminating work: a controlled fresh-process Chaos-only versus Jolt-use comparison, holding map, rendering settings and editor actions constant, including post-PIE idle/editor activity. Switching back to Chaos in a process that already ran Jolt cannot exclude prior corruption. A fresh Chaos-only run still loads the integration module, so it isolates execution, not module startup. If the difference follows Jolt use, investigate admission/pose publication/teardown with memory diagnostics; if it also reproduces without Jolt use, narrow further to module loading versus the graphics/editor environment. One non-crashing session is not proof for an intermittent failure. Do not change drivers/rendering options and physics together or claim unrelatedness from the DXGI stack alone.
