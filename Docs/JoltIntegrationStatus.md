# Jolt integration status

Runtime self-collision is available on the Prophecy Agent through whole-rig, selected-body, skeletal-subtree and individual-pair controls, plus reset and effective-pair readback. Requires active Jolt admission; disabling only affects pairs inside that character. Re-enabling restores captured PHAT rules, respecting other runtime suppressions. Native identities, body states, angular limits, servo, sword grip and Unreal query receivers remain intact. No per-frame filter synchronization or saved asset edits. The normal Editor build, six reflected nodes and all 82 Jolt tests pass; evidence: `Saved/JoltMigration/RuntimeSelfCollision-20260910/`. See [JoltFightSetup.md](JoltFightSetup.md#runtime-self-collision) for exact nodes and examples. No packaged/performance claim for this addition.

High-magnetization numerical crash containment is implemented. The native patch guards velocity and transform writes, uses safe arithmetic for huge finite rotations, and checks full shape bounds against Jolt's actual broadphase limit. A numerical fault stops the shared Jolt simulation before publishing the failed step; its last published pose stays available and Stop Play/start resets it. Gains, PHAT limits and backend selection are unchanged. All 78 Jolt tests pass, including normal/tenfold real-character trials, deliberate worker faults, multiple substeps and large islands. Development/Shipping native libraries and the normal Editor build pass; final shape-edge test additions were loaded with a successful test-only Live Coding patch. Evidence and limits: `Saved/JoltMigration/HighGainCrash-20260910/Implementation.md` and `result.json`. No new package or performance measurement is claimed.

The user accepts the best verified ~8 ms Shipping setup as sufficient optimization; [JoltAcceptedPerformance.md](JoltAcceptedPerformance.md) records its settings and limits. Current priority is fighter movement, physical animation, weapons and scene collision. **Cutting, rope, noose and boat integration are deferred by the user; leave the saved sword cutting loop disconnected.** At the user's request, Codex has saved a 127-vertex Training sword convex; strict Jolt preparation passes after reloading. Use standalone iteration and final packaging only. [JoltFightSetup.md](JoltFightSetup.md) documents the saved testNN startup actor and manual Blueprint alternative and character backend selection; whole-fight live transfer, including dropped weapons, remains unfinished.

Current handoff: open `/Game/testNN` and press Play. One saved `AProphecyJoltFightSetup` initializes the world/static scene and admits manual-NN Physical fighters after BeginPlay and latent startup; it disables MACD, preserves QueryOnly receivers and does not force later mode changes. Kinematic/HalfSim are skipped. Other fight maps can place one setup actor; disable `Enable Jolt` before Play for normal Chaos. Spawned fighters require existing NN registration/pose. Sword equip remains the user's existing `Equip Sword` call.

Angular controls: the fighter's `SetUseAuthoredAngularLimits(false/true)` frees all angular axes/restores PHAT angular fields, before or after Jolt admission. The native experiment's existing node forwards to it. Live UE constraint/profile angular edits synchronize at the next physics preparation. Native updates prevalidate the whole batch and preserve bodies, connector frames, linear anchors, servo packets and independent sword grips. QueryOnly recreation cannot silently replace captured angular overrides with PHAT defaults. Default selections and drive strengths are unchanged; broader profile/motor and HalfSim integration is not implied. The user owns gameplay/visual acceptance for this change.

Angular verification: the normal build and fresh-editor `Prophecy.Jolt.RigWorld.LiveAngularLimitsAtomic` and `Prophecy.Jolt.Character.AngularLimitControls` pass. Evidence is in `Saved/JoltMigration/AngularLimits-20260910/FixtureEditorBuild.log`, `EditorFixtureReopened.log`, and `result.json`. Actual head relative motion is 86.567145 degrees with Free axes versus 6.327433 with live UE angular locks in the eight-step matched control. All original body identities/states and the real sword grip survive limit toggles. The real-asset fixture preloads sword collision/class before creating its control worlds; the initial cold-setup failure and warm-control pass remain in the earlier EditorReopened.log. Three warnings remain in the final report: disconnected A_Sword Blueprint getter and two shared-fixture teardown warnings. These checks certify angular propagation and preservation, not gameplay feel.

Admission previously froze a transient difference between live Chaos body origins and rendered sockets as a permanent body-to-bone offset. The strong fighter's 88.78345 cm mismatch persisted through frames 2–120, stretching its right-hand parent link from 22.35 cm to 60.54 cm. `ProphecyJoltCharacterComponent.cpp` now uses Unreal's identity mapping for unwelded PHAT body origins and direct authored bone targets. Native body states, captured shape/COM scales, PHAT limits and drive settings are preserved.

The normal Development Editor build and fresh reopen pass (`Saved/JoltMigration/PoseIntegrity-20260910/EditorBuild.log`, `EditorReopened.log`). `Prophecy.Jolt.Character.DivergentBodyAndSocketAdmission` passes with the native rig deliberately offset 40 cm and 20 degrees from sockets: native poses are retained, rendered/query poses align, and bodies recover toward unchanged targets. The final armed capture `poses-060805.json` covers every frame through frame 120 (238 Jolt character frames), with both explicit `EquipSword(true)` calls succeeding. All 22 body/display pairs align within 2.048e-12 cm and 2.415e-6 degrees. The accompanying PNG was inspected and shows the connected strong fighter holding its sword. The earlier 12-frame FightStartup ownership/count checks missed skeletal integrity and are superseded. The final capture's optional native-summary command had a path-parsing error; its full pose metrics and PNG remain valid, without claiming that optional summary passed.

The frozen-offset explosion is fixed, but armed motion quality remains open: the final run still records an unresolved transient left-hand parent-link error of 8.228 cm at frame 36 (4.9 cm in another armed run). Do not hide this residual strain or retune PHAT/drives to mask it. Full motion/contact acceptance, cutting and the other deferred mechanics remain open. No new package or performance result is claimed.

Accepted packaged performance evidence is `Saved/JoltMigration/PackagedR5ComparisonDraft/BaselineR5AllBuilds.json/.md`: two Development and two Shipping runs pass every recorded gate, retaining all 1,440 samples, 22 bodies/21 hard joints/88 bones per agent, UE queries and real 30 Hz NN. R5 fixes callback-time NN animation restoration and the diagnostic response-container overread. Both configurations pass exact restored-class, same-frame fresh-pose, direct-component cleanup and 32-response checks; the old SetAnimInstanceClass warning is absent. Five matching-source query tests also pass. Shipping NN frames still average 10.023 / 10.069 ms, while intermediate frames remain below 10 ms. R6 runtime-settings capture and Development trials pass: one versus two intra-op threads gives approximately 9.347 versus 9.356 ms mean, with no total benefit. Matched standalone trials likewise establish no gain from a shared two-thread pool or prepared parallel feedback. Keep the existing defaults. Following the user's cost/time instruction, iterate in standalone with explicit matching ORT settings and reserve packaging for the final candidate. The models, sequential lower→upper dependency, cadence, Original feedback and all output/query requirements remain fixed. Main-project Editor rebuild passes (83.81 seconds); its full actual-NN standalone control passes at 10.600 ms mean. An automatic Editor ORT pool trial passes correctness but measures 17.817 ms mean, so explicit packaged settings remain the comparison control. Older paragraphs below are historical implementation evidence, not current acceptance.

Earlier Editor fight fixture acceptance: `Saved/JoltMigration/FightValidation-20260910-015228/validation.json` passes the saved Static floor check and all three actual-sword fixtures (equip/drop/query, floor contact, distinct-fighter contact), with zero new handled ensures in that Editor invocation. The held-sword trial uses identical 45-body initial states and unchanged PHAT/drive settings; only the sword-to-victim response differs. Over 12 steps per case, maximum head position/velocity differences are 0.0907659 cm and 4.8438934 cm/s, with both cleanups verified. This is controlled contact/depenetration evidence, not swing, cutting or full combat acceptance. The same run exposed and verified the repair for UE's legitimate tiny-transform early-out: component publication now uses engine update tolerances while native queries receive exact completed transforms. Both focused body-component tests pass, including deliberate 1 cm/1 degree callback mutation rejection (`BodyPublicationAutomation.log`). Live Coding succeeds. The new opt-in `-JoltFightValidationDir=<new absolute directory>` entry allows the same checks in Shipping without relying on disabled ExecCmds. The duplicate-pin repair is saved. The corresponding earlier Development and Shipping candidates passed packaged functional verification, as recorded below.

Earlier packaged candidate: `/Game/_mygame/NewFunctionLibrary` is repaired and saved. All 34 duplicate reserved user-defined `__WorldContext` entries were wholly unlinked and had the same UObject type. The narrow editor helper checked the exact entry-name/GUID manifest before any mutation, removed those definitions, reconstructed the proper generated hidden pins, and verified unchanged remaining pin/default/wire fingerprints across every graph. Compilation produced zero warnings/errors. The helper did not save; the subsequent publication saved only this library, with no other asset changes from the repair and no dirty packages remaining. [Repair report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/AngSpringRepair-20260910/repair.json>) and [publication/backup](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/AngSpringRepair-20260910/publication.json>). Fresh registry `Saved/JoltMigration/FightCookRegistry-20260910-023258.json` confirms only the library's asset bytes changed. Development packaged functional verification passes ([Development result](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FinalFight/Packages/Development-Resume-20260910-020305-616/result.json>)): saved Static floor, all 28 sword lifecycle checks, all 10 floor-contact checks, both matched 12-step fighter-contact trials and cleanup; direct staged game exit 0, ensure delta 0. The recovery reused the successful existing Development stage without rebuilding, cooking or staging. Its [recovery record](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FinalFight/Packages/Development-Resume-20260910-020305-616/recovery.json>) preserves the exact provenance limits: pre-cook Editor hashes were not persisted, and full cook/stage hashes were first recorded during recovery. [Shipping package](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FinalFight/Packages/Shipping-StageResume-20260910-021815-430/package.json>) confirms completed Shipping build/stage and required-asset checks. [Shipping result](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FinalFight/Packages/Shipping-RuntimeResume-20260910-022749-979/result.json>) and its [runtime report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FinalFight/Packages/Shipping-RuntimeResume-20260910-022749-979/runtime.json>) confirm all three fixtures plus saved Static floor, exit 0 and ensure delta 0. This runtime-only recovery preserved the original Shipping stage, binary and shared cook exactly; no further build/cook/stage ran. A fresh external `Engine.ini` selects the already cooked `/Engine/Maps/Entry` through `-EngineINI`, because Shipping ignores the positional map argument. Its SHA256 remained `0B966F1564143EBBA9A1D5293BBD831969B3349052E120855672DED0B86C6C71`. No game source, project config or asset changed. The current Shipping stage and shared cook are retained; Development evidence remains available after retirement of its duplicate stage. These are functional fixture packages, not playable-map, NN, rendering or cutting acceptance. These packages predate the saved startup actor and initial-velocity admission fix; they are not package verification of current source.

The [retained full native foundation report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-235719-740/index.json>) records **67 passed, zero failures, zero test warnings/errors and zero not-run tests**. It covers the existing rig, hard PHAT joints, profiles, ownership, pose and query tests plus captured standalone bodies, real static/simple and reflected triangle collision, separate instances, Blueprint world initialization, scene instance-to-mesh promotion/reindexing and cleanup. The shared coordinator prepares scene changes immediately before its single native step. Original UE receivers remain intact. The saved 127-vertex Training hull passes strict preparation. At the user's direction, sword admission disables MACD while retaining CCD. Actual A_Sword passes the 28-check manual-step equip/grip/drop/query/cleanup fixture. Controlled floor and distinct-fighter depenetration also pass; moving swing/contact sequences and final user motion/feel acceptance remain open. Earlier physical-feedback and sparse-target oracle reports remain valid within their recorded scope.

The actual sword additionally passes all ten checks in a 180-step native-floor contact/removal control (`Saved/JoltMigration/SwordContact-20260910-005817.json`): it settles on the imported floor, then falls after only Jolt floor collision is removed; original Unreal query receivers remain. The Blueprint initializer now sizes fixed scratch from contact capacity (256 MiB at default body/contact limits), fixing insufficient first-step scratch. The two focused scene tests pass cleanly, including 120 active-body steps through the actual default initializer (`Foundation-20260910-010517-582/index.json`, peak scratch 128,064,928 bytes). The 67-test report above precedes this fix; it is not a new full-suite run.

The user approved making `testNN` Floor fixed. It is saved as Static with its transform preserved (`Saved/JoltMigration/TestNNFloor-20260910/publication.json`). Both existing fighters and swords have run together under the automatic shared Jolt step: 47 bodies, 44 joints, two grips, 44 owner exclusions and one imported floor, with no reported world/character/sword errors (`VisualStatus-20260910-011702.json`). That earlier fresh PIE handoff completed 117 shared steps before pausing on the floor (`VisualStatus-20260910-012013.json`). `HighresScreenshot00019.png` shows the default-strength fighter walking with its held sword. The other fighter's 0.02 upper-body strengths are intentional per the user and remain unchanged. That preview used temporary PIE startup, superseded by the saved setup actor above. User motion/feel acceptance and moving swing/contact sequences remain open. The earlier Development and Shipping candidates passed packaged functional verification. Controlled held-sword contact against a distinct fighter passes as recorded above. No new performance result is claimed.

The generalized shared step-client coordinator has built and passed the [100-character fixture](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_full_scopes_100_20260909_1126.json>): 2,200 Jolt bodies, 2,100 joints, all 8,800 feedback/socket bones per frame, zero Chaos dynamic bodies, removal during bone finalization, survivor continuation, stale-handle rejection and pending-enable cancellation. This is 60 Hz synthetic motion with a 30 Hz authored stream on a separated grid with static-only fixture contacts. It does not establish actual NN inference, crowd interaction or rendered FPS. Native filters support broader authored blocking profiles; the fixture's own static-only profile is intentional.

The initial [100-character timing](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_shared_100_20260909_1054.json>) measured the synchronous Jolt step **2.348 ms** and world tick **32.010 ms**. User-authorized omission of unused cameras/fist closing, sparse target construction, batched retained query updates and duplicate Chaos finalization removal reduced measured work. Matched synthetic runs with explicit, restored game-thread placement on performance cores measured **10.420 ms** with PrePhysics/serial composition, **9.309 ms** with DuringPhysics/serial, and **9.099 / 8.986 ms** with DuringPhysics/batched composition. Those timings exclude actual NN and their p95 values remain above 10 ms.

Complete actual-NN 100-character runs now pass. Same-binary query-tree padding trials measured **12.827 / 12.373 / 12.231 ms** at 5/20/40 cm; the subsequent unused-camera-subtree detachment measured **12.155 ms mean, 12.050 median, 15.067 p95**. Each completed 180 real NN steps and 18,000 physical-feedback samples while retaining every body, joint, skeletal bone and query receiver. Capsule-aware validation now resolves the actual APawn root; all head rays in the complete runs remained unfiltered. Three current query regressions passed in `Foundation-20260909-141336-210/index.json`, including the independent 150 cm post-EndPhysics test. Actual-NN lifecycle checks pass; the separate synthetic test remains the evidence for authored-packet mutation during callbacks. DuringPhysics, controlled core placement and query padding remain explicit options. The goal is still unmet; no rendered FPS, crowd-intercontact performance or matched Chaos speedup is claimed. Details and limitations are in the performance document.

All three blood receiver fixture classes passed the [real-RHI test](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_blood_all_receivers_20260909_1052.json>) with an empty report error: static mesh **524 localized painted pixels**, Jolt character head **114**, and two independently promoted ISM receivers **273 / 687**. Existing Blueprint callback dispatch, full promoted mesh transforms, zero-scale retirement/query removal, separate masks, untouched neighbor, paint persistence and material restoration were verified. The character uses the existing bone-local UV fallback; Jolt subshape IDs are not UE triangle indices. This proves texture-mask pixels and receiver identity, not rasterized appearance, moving attachment, Niagara GPU emission/export, HISM/PCG coverage or production-material preservation. That pre-repair test reproduced the handled `NewFunctionLibrary.Ang Spring` world-context-pin ensure found during the initial inventory; no asset repair/save was made in that test. The 34 duplicate reserved inputs have since been repaired and saved, as recorded above.

The latest same-binary actual-NN comparison measured **11.907 ms** with ordinary idle-read locks and three Jolt workers, **11.761 ms** with exclusive-owner idle reads and three workers, and **11.304 ms mean / 11.429 median / 14.014 p95** with those reads and seven workers. All full-pose, query and lifecycle checks pass; the goal remains unmet. These explicit diagnostic settings preserve the cadence and body/joint counts. See the performance document for native phase attribution and workload limits.

The accepted SSE2 code measured **11.275 ms mean / 11.264 median / 13.917 p95** for actual NN with seven Jolt workers. Private accepted-packet commit, stricter finite quaternion rejection, retained query preflight and per-sample training rotation caching pass their regressions. Sampling/encoding timers measured only **0.110 / 0.080 ms**, within 0.546 ms resampling. Deferred query-scene maintenance passes correctness but only changes the mean from **11.232 to 11.119 ms** in one pair. Separate game-module SSE2/AVX2 trials, with matching private PCHs and unchanged native Jolt/Engine/ORT libraries, average **11.393 / 11.377 ms** across two runs each: no meaningful gain. Both profiles pass 59 Jolt, two feedback and three physical-target tests. Default shared-PCH/SSE2 compilation has been restored; normal Chaos maintenance remains preferred. Exact endpoint reuse and parallel feedback work are being validated next. The goal remains unmet, and no packaged actual-NN result exists yet. Full evidence is in the performance document.

Exact endpoint caching passed correctness but made target reads slower (**0.578 to 0.652 ms**); its active implementation and controls have been removed, with measured source/tests archived. The original path is restored. Optional prepared parallel feedback passes full-state and live gates and reduces resampling **0.545 to 0.258 ms**, but its first complete 100-agent world tick is **11.472 ms**, versus **11.521 ms** for the same binary's original path. This is not a demonstrated overall gain or the under-10-ms goal. Original remains the default. A current-source packaged comparison is being prepared using isolated copied assets/models/config, with no packaged result yet.

The [latest D3D12 blood rerun](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_current_blood_20260909_1529.json>) passes on the updated query code with the same **524 / 114 / 273 / 687** painted-pixel counts and all original receiver, neighbor, repeat and restoration checks. Its scope limitations above still apply; it predates the optional feedback batching change.

The historical native-proxy correction removes an unintended second animation update per publication. Five strengthened query tests (`Foundation-20260909-201658-412`) and the complete actual-NN run pass, with PreUpdates reduced from 72,000 to 36,000 and all evaluation/query/lifecycle work retained. That run measures 11.878 ms mean world time; no overall gain is established. The isolated r3 Development package builds/cooks/stages successfully, but its first two-agent runtime crashes at the final measured frame before producing its complete report. The later accepted [R5 Development/Shipping runs](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/PackagedR5ComparisonDraft/BaselineR5AllBuilds.json>) supersede this resolved crash and its pending validation gate. This is historical evidence, with no ongoing crash investigation; details remain in the performance document.

The following older checkpoints are retained as implementation evidence, not as current completion claims.

## Confirmed scope

- Local simulation only. The user explicitly excluded multiplayer and physics replication.
- Fix routine compiler, dependency and configuration issues autonomously. Stop for real blockers or behavior/design decisions.
- Use stock Jolt hard angular limits with the authored PHAT angles and motion selections, without retuning. User approved this compliance change; custom soft constraints are deferred unless needed.
- Later user clarification: require working, fast physical animation, solid bodies/joints and blood staining meshes, instanced meshes and characters. Exact Chaos/Jolt equivalence is not required. Chaos trajectory repeatability is diagnostic, not a release gate; native Jolt sleep, caps and solver behavior may be used while retaining functional control and the authored PHAT limits.
- Preserve existing gameplay, authored assets and unrelated working-tree changes. The existing journal rule still prohibits closing/restarting a user editor without authorization.

## Verified foundation

| Item | Evidence and limit |
|---|---|
| Pinned dependency | Jolt 5.6.0 at `e77f175595e64cb44218cc9d9d56fc365ad0e36a`, compiled with VS2022 compiler 19.44.35221 and Windows SDK 10.0.22621.0. Explicit preparation script: `Tools/Jolt/BuildJolt.ps1`. |
| Unreal integration build | New runtime/editor modules compiled and linked on UE5.7.4. [Successful build](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/UEBuild-20260909-050555.log>). [Niagara declaration verified](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/UEBuild-20260909-050923.log>). |
| Module startup and ABI | The isolated test process loaded the staged unique Jolt DLL, verified feature ABI, installed UE allocation callbacks and registered Jolt types. No production body registration or stepping is enabled. |
| Six initial automation tests | All six passed with zero test warnings/errors: signed basis/rotations, dimensional conversion, point velocity/impulse, fall/ray, impulse/constraint and repeated independent solver fixtures. [JSON results](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-050950/index.json>). These are primitive/solver fixtures, not character performance or UWorld ownership validation. |
| Working-tree baseline | [Snapshot](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Baseline-20260909-045414/baseline.json>): 333 tracked-file hashes, a binary-capable working-tree patch and copies of 10 pre-existing untracked source/tool files. HEAD alone does not identify the initial state. |

The initial tests exposed that UE5.7 enables exceptions for Editor compilation. The new dependency and runtime configuration have been aligned to C++ exceptions enabled, RTTI disabled, /MD, precise floating point, double world positions and SSE2. Assertion and allocation failures remain fatal; C++ exception support does not provide overflow recovery. No other project's module settings were changed.

The expanded suite passed **all 14 tests with zero test warnings/errors** in the main project: [results](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-075246-107/index.json>). Added coverage includes real Game-world lifetime/isolation and explicit stepping, generation-safe handles and weak associations, capacity refusal, capsule dimensions/axis correction, separate bone/body/collider/COM frames, point velocities, local-COM constraint frames and complete compound archives with corruption rejection. The fixture's sphere rests at Z=47.986 cm on a plane with a 50-cm radius: this reflects Jolt's default 2-cm penetration slop. Its acceptance is 3 cm plus less than 1 cm/s vertical velocity; no solver setting was altered to pass the test.

The minimal host editor built, and the guarded commandlet created `/Game/Fixture/DA_CompoundFixture` only in `Tools/Jolt/SmokeHost/Content`. Its Jolt payload is 328 bytes, SHA1 `3ED1105B52724A4F0CB8C14578B5FE9780A73A7A`. [Creation log](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/FixtureCook-20260909-074410/Unreal.log>). That first creation exited successfully with eight warnings about disabled engine-default ACL settings; the host now explicitly enables ACLPlugin. This was editor asset creation/restoration, **not yet a successful packaged run**.

Two verified build-path issues have been addressed before packaging. UE's external-plugin editor manifest can select the original plugin DLL instead of the host's newly built copy, so the main editor build is canonical and `Module List` records the actual loaded path. Windows dependency preloading can also select a same-named Shipping DLL during editor cooking. Dependency manifest schema 2 now requires distinct `ProphecyJolt_5_6_Development.dll` and `ProphecyJolt_5_6_Shipping.dll` names, stable within each configuration. Both libraries rebuilt and the combined current main-editor state passed all 14 tests again: [current results](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-080131-216/index.json>). The verbose Windows log records the Development dependency path.

**Both Development and Shipping build/cook/package and actual packaged runtime restoration passed.** Each direct monolithic executable loaded the editor-produced compound from its cooked content and verified its complete shape/material/COM/ray data. Both runners required exit 0, a true JSON result and a SUCCESS stdout marker: [Development runtime](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/PackagedFixture-20260909-061119-060/runner.json>), [Shipping runtime](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/PackagedFixture-20260909-061638-606/runner.json>). Package manifests record executable and configuration-specific dependency hashes: [Development package](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/SmokePackage-Development-20260909-060337-795/package.json>), [Shipping package](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/SmokePackage-Shipping-20260909-061128-590/package.json>). This validates the isolated compound foundation, not a cooked production PHAT importer or complete game package.

The historical [foundation preservation check](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Preservation-20260909-0808.json>) compared all 333 initial tracked hashes and ten original untracked source/tool copies before the character work. Later character integration intentionally also enables the plugin in the project, adds its private game-module dependency and adds an opt-in capture/replay hook in `ProphecyAgent.cpp`. That earlier report does not describe the later source changes. Original asset/configuration preservation is checked again at each recorded checkpoint.

## Character implementation checkpoint

The earlier 09:03 checkpoint added the live rig descriptor/preparation, hard PHAT joint mapper, explicit whole-rig world ownership, and the manual velocity step listener. **All 25 automation tests passed with zero warnings/errors** in [that historical report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-090307-607/index.json>). Coverage included authored hard angular values/axes, rotated and COM-offset joint frames, actual multi-step servo scheduling and its distinct denominator, strengths/quaternion signs, gravity/force ordering, scaled capsule geometry, whole-rig preflight, stale/cross-world handles, teardown and reinitialization. The later verified checkpoint below supersedes its outstanding real-rig execution gate.

`ManualCapture` is an explicit opt-in benchmark case using the actual `FManualFollowerSubstepCallback`. It creates a native separate `PhysicalMesh`, preserves the nine-entry body settings map, publishes synthetic poses at 30 Hz, and records exact body endpoints plus raw pre/post velocity state. `ManualReplay` follows capture, uses a fresh native actor and sealed packets, and records solver-cache restoration limits. `-ManualChaosOnly` permits this controller diagnostic independently of shape import. Input identity and valid captured state remain required; trajectory equivalence is diagnostic only.

The [first sealed actual-controller record](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_manual_chaos_20260909_0902-manual-air.json>) contains 60 packets/steps with 22 bodies each. Its [Chaos replay](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_manual_chaos_20260909_0902-replay-manual-air.json>) exceeded the initial diagnostic thresholds. All 60 inputs and all 22 first-step pre/post states match exactly. Later maximum differences are 0.003265 cm, **0.018111 degrees after normalization**, 0.195914 cm/s post-servo linear velocity and 0.038087 rad/s angular velocity. The original raw-quaternion angle metric included a false self-distance; the comparison now normalizes both orientations. Real solver-side divergence remains and the unchanged controller's small-angle axis fallback amplifies it. [Detailed analysis](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltManualReplayFindings.md>). Those unchanged trajectory thresholds remain exceeded in later diagnostics; this is historical repeatability evidence, not a current implementation or release gate and not a performance comparison.

The real native fixture exposed authored body scaling: head `(1.15,1.15,1.15)`, thighs/calves `(1,1.12,1.12)`. The importer first rejected unsupported scale explicitly. It now extracts actual native sphere/box/capsule geometry, retaining shape identity and flags, rigid wrappers and leaf collision margins. UE source scales remain provenance and are never applied again. Unsupported cooked convex/wrapper geometry is refused explicitly. Native joint `GetLocalPose` includes live connector scaling that stored `GetRefFrame` omits. The earlier packaged proofs cover the compound foundation snapshot; later rig code has not yet been revalidated in Shipping.

### Verified build, actual rig and live character

The combined main Editor target succeeded in [UEBuild-20260909-095544.log](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/UEBuild-20260909-095544.log>). The expanded [09:55 foundation report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Foundation-20260909-095553-725/index.json>) records **33 succeeded, zero failed, zero warnings/errors, zero not-run tests**. Added coverage includes completed pose composition, native ray queries, conditional waking/native velocity caps, and whole-rig ownership across generations, replacement and teardown readiness. This is an Editor build/test checkpoint, not a new Shipping proof.

The [actual-native-filter manual run](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_manual_native_filters_20260909_0957.json>) has an empty report error and completes air and floor capture/replay cases. Each independent Jolt execution consumes 60 sealed packets with 22 bodies and 21 hard PHAT joints and records `completed_and_torn_down=true`: [air](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_manual_native_filters_20260909_0957-jolt-air.json>), [floor](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_manual_native_filters_20260909_0957-jolt-floor.json>). Both preserve exact final input packets and exact first Chaos pre/post-servo state. The importer now derives effective simulation object/channel responses from live native shape filters, retaining raw BodyInstance and query-filter provenance; it no longer mistakes raw body defaults for the effective skeletal collision policy. The recorded Chaos repeatability comparison is still false and explicitly diagnostic only.

The [live character report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_live_character_20260909_0958.json>) has an empty report error and `live_jolt_validation.success=true` for both `air_no_gravity` and `floor_gravity`. Each completes 60 automatic fixed 1/60-second steps with one collision step, 22 Jolt dynamic bodies and 21 joints. Every frame validates all 88 skeletal bones through the actual `Agent.SampleActualComponentPose` entry point against normal `PhysicalMesh` socket evaluation. There are zero Chaos dynamic bodies in the inspected registered-component world scope. Completed revisions advance from the primed revision 1 through measured revisions 2–61.

| Live case | Maximum feedback/socket position difference | Maximum normalized angle difference | Mean Jolt step | Mean world tick |
|---|---:|---:|---:|---:|
| Air, no gravity | 2.726e-13 cm | 2.415e-6 degrees | 0.0809 ms | 0.7704 ms |
| Floor, gravity | 3.252e-13 cm | 2.415e-6 degrees | 0.0813 ms | 0.7547 ms |

Scale differences are zero. Every measured frame also validates a real Jolt ray mapped to the retained agent, `PhysicalMesh` and bone, with `FaceIndex=INDEX_NONE`; a deliberately stale body generation is rejected. Public Physical→Kinematic teardown removes the 22 rig bodies and 21 joints, rejects all 22 former handles, and retains only the explicit floor where present. Query identity validation does **not** establish UV painting or visible blood pixels. These are one-character NullRHI measurements using native synthetic authored poses; they exclude NN model inference/recurrent manager execution and do not demonstrate rendered FPS, a Chaos speedup, or crowd scaling. World-tick cost includes target publication, Jolt stepping and synchronous skeletal evaluation; detailed validation runs outside that timed interval.

The implemented [live adapter](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltLiveCharacterAdapter.md>) remains opt-in, with one rig per UWorld and static contacts only. It retains query-only Chaos proxies and the original mesh/material identity while Jolt owns dynamics. It does not automatically register the UE level or change production defaults.

## Asset inventory evidence

The [raw manifest](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Inventory-20260909-051135/AssetManifest.json>) contains 4,080 package records, 109 Blueprints, 27 Physics Assets and 7 Niagara systems from the declared roots/dependency closure. The exporter reported zero extraction errors within its declared scope and saved no assets.

The pre-repair inventory commandlet run was **not clean**: Unreal emitted a handled ensure while loading `/Game/_mygame/NewFunctionLibrary`, reporting a duplicate world-context entry pin on `Ang Spring`. The log's 38 error lines include that ensure's stack, not 38 separately identified asset failures. It also warned that `A_Sword`'s `get depth of location` execution pin is disconnected and its result is pruned/defaulted. [Full inventory log](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/Inventory-20260909-051135/Unreal.log>). The duplicate world-context definitions have since been repaired and saved; this log remains pre-repair evidence. The disconnected cutting graph remains unchanged under the user's deferral.

The general manifest explicitly does not prove map/WorldPartition instance overrides, level Blueprints, SCS component defaults, runtime/string/config reachability, evaluated body mass/inertia, or complete Niagara collision/export module configuration. Phase 0 therefore remains partial. The follow-up [asset findings](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltAssetInventoryFindings.md>) and [effective rig audit](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Docs/JoltEffectiveRigAudit.md>) supersede historical graph assumptions where evidence differs. The boat includes velocity-overwrite damping/clamping. No connected Blueprint NormalImpulse consumer was found within the exported scope.

A targeted CDO/SCS commandlet now exports the manual agent's component templates without running construction scripts or saving assets: [data](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/JoltMigration/ComponentAudit-20260909-075439/Components.json>). Its overall exit was 1; the effective-rig note records the extraction result and engine diagnostics separately. Placed instances, runtime initialization and mode history remain unobserved.

## Fresh Chaos comparison evidence

An opt-in `-PhysicsBenchRigAudit` captures the first live mesh's actual joint profiles and body mass/COM/inertia in the existing synthetic harness. It adds no simulation writes. Capture is outside the timed world tick; surrounding frame intervals can include audit cost, as they already do for the original audit.

The [one-agent audit](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_scope_rig_20260909_0745.json>) confirms 22 bodies/21 joints: ordinary native Physical has 21 soft-limited swing/twist joints at 179 degrees, whereas the inspected NativeWorld and Passive HalfSim cases have all angular axes Free. The [100-agent runs](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_chaos_baseline_100_20260909_0747.json>) completed six passes, each with 2,200 dynamic bodies, 2,100 anatomical constraints and no nonfinite transforms. NativeWorld also configures 2,200 target constraints; FullSim configures none. Native force/torque FullSim world-tick means were 34.745–36.210 ms; NativeWorld means were 24.940–26.516 ms. These are uncapped NullRHI world-tick measurements, not solver-only time, rendered FPS or a Jolt speedup.

The existing `FullSim` benchmark deliberately sets manual=false and uses AddForce/AddTorque OneStep control. It does **not** measure the manual follower's PreIntegrate SetV/SetW callback; that separate controller and recorded-input path are now implemented and exercised in the 09:57 reports above. The initial optional channel dump also included one deprecated sentinel beyond the 32 valid response slots. The loop is corrected, and the [one-agent diagnostic](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_scope_rig_32channels_20260909_0819.json>) completed FullSim/NativeWorld/Passive with exactly 32 response slots, no report error and no nonfinite transforms. That report supersedes the earlier channel-response field; it does not alter or replace the measured 100-agent timings. Neither those 100-agent drive configurations nor the one-character live fixture form a matched speedup comparison.

## Current work and remaining gates

- Controlled sword-floor and distinct-fighter contact/depenetration pass, alongside the saved 127-vertex hull and actual A_Sword equip/grip/attached/drop/query/cleanup fixture. Validate moving swing/contact sequences and final user motion/feel in the intended map; no further hull edit or rig retuning is needed.
- Use the saved testNN setup actor and press Play; other fight maps can place one Jolt Fight Setup actor as described in [JoltFightSetup.md](JoltFightSetup.md). Visual movement and moving contact acceptance are deferred. Fixture success does not certify every production level or collider.
- The static scene importer covers supported primitive/convex and cooked triangle collision plus individual ISM/HISM bodies and their lifetimes. Landscape and moving-world geometry still require additional support; unsupported live geometry produces an explicit error.
- Cutting, complete cutting/Niagara/blood integration, rope, noose and boat are deferred by explicit user direction. Leave the saved cutting loop disconnected. Earlier blood fixtures preserve their recorded static/character/instance staining evidence.
- Character backend selection is available; seamless whole-fight live transfer and Jolt HalfSim are unfinished. Dropped weapons retain their backend.
- The earlier Development and Shipping fixture candidates passed. Retain their Shipping stage, shared cook and evidence as historical records. Current source passes the normal Editor build/reopen, initial-velocity regressions and short automatic startup checks above; no new package or performance run is claimed.

The [migration plan](JoltMigrationPlan.md) and [capability ledger](JoltCapabilityLedger.md) retain the broader work. The user's current deferrals above control its order.

## Standalone integration update - 9 September 2026

**Earlier foundation checkpoint, 10 September 00:01:** the scene owner and Blueprint world initialization compile; all 67 foundation tests pass in `Foundation-20260909-235719-740`. Actual floor/ISM sources, zero-scale retirement, promoted StaticMeshActor collision, unchanged neighbor generation, reindex invalidation, UE/native rays and complete cleanup pass. The owner ignores ordinary components with no live native collision while observing later physics creation; actual live unsupported colliders still fail strict capture. Level-owned BSP components enter the source queue, but a dedicated populated-BSP runtime test has not been claimed.

`GetMassWeightedPoseError` now reads captured masses and current Jolt body-origin transforms for Jolt characters, retaining the original target/sign/units and Chaos path. The [one-character correctness report](<C:/Users/singerie/Documents/Unreal Projects/Prophecy/Saved/Benchmarks/jolt_fight_pose_error_20260910_000105.json>) passes handoff and 60 steps each in air and floor gravity: 22 bodies, 78.254547 kg captured mass, zero linear aggregate difference and at most 9.70e-18 kg-radians angular difference against the independent reference. Existing pose/query/lifecycle checks also pass. This is correctness evidence, not a new performance result. Combined game build: `UEBuild-FightStartup-20260909-235543.log`; final fixture-oracle build: `UEBuild-PoseOracle-20260909-235949.log`.

Performance tuning is closed at the accepted checkpoint. The new captured-body world API and shared-step primitive component are implemented; live testing has exercised mass/COM, nonuniform visual scale, movement and current UE/Jolt query hits. Six focused body tests pass in Foundation-20260909-231259-260, including removal during completed-transform publication and proper EndPlay cleanup. The sword routing builds in UEBuild-SwordRouting-20260909-231120.log; the actual A_Sword functional fixture now passes with the user-authorized MACD omission. A newly added contact-exhaustion test was removed because the pinned Development Jolt build deliberately asserts fatally before returning its resource error. Production assertions remain enabled; fault cleanup is source-reviewed, not claimed as runtime-tested.

Training hull completed on 10 September at the user's request: only Sword_GL01_Training simple collision changed, from 309 cooked vertices to 127. The source bounds are exact; maximum inward difference from the original source convex is 1.911 mm in mesh-local space, volume -0.922%. Source/render/material/complex-query fingerprints match. Strict preparation passes before saving and on two fresh loads, retaining zero hull tolerance and the 256 cap. Native equip mass remains 1 kg. Original Sword_GL01 and A_Sword assets are byte-unchanged. Evidence and original Training backup: `Saved/JoltMigration/SwordHull-20260910/`.

Actual sword functional acceptance, 10 September: `Saved/JoltMigration/SwordHull-20260910/VerifyNoMACD/sword_fixture.json` passes all 28 checks, including actual Blueprint/Training receiver identity, 22 owner exclusions, fixed grip after a shared step, attached/physical roundtrip, drop preserving the same native body and COM/angular velocity, direct drop from attached mode, UE query identity and complete owner/item cleanup. Both admission routes explicitly disable MACD, as the user requested; existing CCD remains true. Cold saved-asset verification also passes. Build `BuildNoMACDGetter.log` passes. The pre-repair `VerifyNoMACD.log` recorded the NewFunctionLibrary world-context ensure, disconnected cutting-node warning and a transient-world teardown warning; that commandlet exited 1 despite passing functional JSON checks. The duplicate reserved inputs have since been repaired and saved; the cutting graph remains unchanged. No rendering, NN, production-world ticks, representative combat contacts or cutting/blood acceptance is claimed by this fixture.
