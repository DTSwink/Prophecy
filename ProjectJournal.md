# Prophecy Project Journal

Current implementation and handoff — updated 2026-09-24.

Keep this journal about the finished current product: durable rules, accepted settings, authoritative paths, bounded validation and real remaining risks. Update the relevant section after each change; do not append turn-by-turn execution logs. Keep detailed evidence in topic documents. The [pre-tidy archive](Docs/Journal/ProjectJournal-history-through-2026-09-18.md) preserves all prior entries, including superseded behavior; it is not a current task queue.

## Working rules

- **Left foot hover260–270 diagnosed2026-09-24:** current Walk has no active tempering. NN requests downward foot motion, floor corrects its support geometry, then locomotion calf clamp lifts the ankle1.27–1.86cm at policy ticks259–265 because the NN calf exceeds rest length. Offline clamp replay exactly matches displayed target; physical mesh follows it closely. Same-pose knee-pop smoothing disable at266 changes foot zero, identical prefix confirmed. Near269 NN itself begins lifting. No code/Blueprint/settings changes; owned diagnostic PIE ended and trace off. [Evidence](Docs/FootHover260.md).

- **Backward-bound target heading2026-09-24:** `Set Walk Pinning Backward Bound` now appends Lerp Target, default0. It blends the selected root-window heading toward the mover's final locomotion target facing (1), keeping the origin at current root0. Shortest-angle interpolation handles opposite/wrapped headings without collapsing the direction. Gameplay, transfer caps and debug arrows share it; circle bounds unchanged. Per-agent, zero skips target lookup/math, disabled bypasses; no timer/inference. Live build104.27s loaded18:45:52UTC; all7 WalkPinning tests passed18:47:47UTC. Existing BP node refreshed default0 with values/wiring preserved, status3/zero stale types; no rollout, explicit save or restart. Include in next authorized normal build. [Contract](Docs/WalkPinningBackwardBound.md).

- **Left pin216 diagnosed2026-09-24:** current Walk-only replay reports raw hard pin1 but effective0.941601. Gate switches from right to left at215; connected Pin In3/Out0 smoothing has not reached1, and opposite-foot transfer1.15 raises it to the0.941601 minimum. Tick216 retains215's30Hz policy sample. This is not transfer imposing an upper cap. No gameplay changes; diagnostic PIE ended/trace off. [Evidence](Docs/WalkPin216.md).

- **Recovery scope audit2026-09-24:** shared lower/hand/FK tempering, regional checkpoint recovery, exit upper-body inertia and calf-length continuity have no additional accidental kick-only gate found. Kick profiles/limb-role mapping, physical ankle allowance/clamp bypass and kick self-balancing remain intentional. Optional Walk foot-rotation override still explicitly requires attack recovery and skips parry/dodge; reported as an exception to all-special coverage, not changed. Attack-entry pelvis inertia remains full-attack-only by original scope. Leg reconstruction itself is shared. Source audit only, no gameplay edits. [Scope and remaining exception](Docs/SpecialRecovery.md).

- **Shared calf-length recovery2026-09-24:** fixed jabR snap1494–1495 by extending existing signed calf-length/render continuity to full non-kick attacks and active parry/dodge exits. Pose-only return uses configured kick return duration when present, otherwise60 ticks; it never loosens non-kick physical joints. Active-kick joint/pose path unchanged; half attacks keep loco legs. New special/reset/completion/teardown removes return state and timer, no ongoing recovery work at normal. Paired replay enables fix only at1493: earlier bone positions identical; first-return tip-gap change left2.67545→0.00230cm/right2.19743→0.00181cm, normal by1554. Full1565-tick replay16 exits including both kicks, maximum first-return gap change0.004121cm; defense natural/stopped parry+dodge lifecycle passed470 finite samples. Six focused tests passed17:42:19UTC. Authorized BP save/restart clears an inert callback from an aborted diagnostic harness; normal Editor build10 actions succeeded45.23s. No Blueprint wiring/settings changes. [Implementation and bounded evidence](Docs/CalfSnap1495.md).

- **Backward pin bound heading-only index2026-09-24:** corrected `Set Walk Pinning Backward Bound`: Root Index selects heading from continuous window0..8; origin always current root0. Previously both position and heading came from the selected sample. Gameplay/transfer caps and debug arrows share the corrected reference. Circle bound retains its selected center. Root Index0 and disabled behavior unchanged; no new query/timer/inference. Live build118.00s loaded17:19:55UTC, all7 WalkPinning tests passed17:20:41 including translated-future invariance and heading rotation around root0. Pose BP status3/zero stale types,27 archived defaults repaired with values/wiring preserved. No gameplay rollout, asset save, graph wiring edit or restart. Include in next authorized normal build. [Current contract and evidence](Docs/WalkPinningBackwardBound.md).

- **Attack checkpoint184064 + headbutt cleanup2026-09-24:** `Set Attack Checkpoint` now adds `Predictive Pin x5 Refresh 2 (184064)`, per agent, keeping Current174664 default and160664 enum values/files unchanged. Viewer refresh2 manifest/receipt and source SHAe0759ba5039d… verified; separate `NN/Attack184064` export, same geometry/encoding/finish rules. Export parity batch1/3/100 lower max1.669e-6, upper2.981e-7, cone exact. Each alternative loads only on first selection, next-attack selection independent from active checkpoint; three inference batches share existing workflows. Removed default-on headbutt GT arm preparation (entry capture, loading, step override, cache/staging dependency); full/half headbutt arms now checkpoint-authored. Old properties deprecated/inert for saved references; reserved unused storage preserves Live Coding layouts. User authorized BP save/restart; saved backup `Saved/Diagnostics/BP-Before184064Restart.uasset`. Editor stalled during saved shutdown and was terminated; normal build completed, testNN reopened. Temporary Live Coding stale checkpoint enum cleared on normal load; BP status3/zero stale types, selections/wiring preserved. Owned short PIE passed16:15:22UTC: three concurrent models, queued switches, retrigger phase/frame preservation, half attacks,184064 batch1→3/full+half headbutts, restore current. Native184064 startup error5.245e-6. Own PIE ended; no saved checkpoint switch. [Selector/provenance/checks](Docs/AttackCheckpointComparison.md), [headbutt removal](Docs/AttackPinningAndHeadbutt.md).

- **Attack-arm neutral return extension2026-09-24:** existing node now displays `Set Attack Arm Return To Neutral`, retaining native function/pins and Blueprint connections/settings. Six slashes still return right arm; hookL/overL return left, hookR/overR right, full/half. Other melee and defense remain excluded. Same hold/blend/initial-distance speed, torso-front route and connected elbow solve; left uses its own idle clavicle/wrist/hinge and never the right sword grip. Fixed anatomical frame remains right-minus-left. Event-only arm selection, cleared on cancel/reset/retirement; no added inactive pose work/timer/inference. Live build213.42s loaded15:44:25UTC; eight SlashReturn/HandRecovery/CoreTempering tests passed15:44:39UTC, including all four melee names and12-tick retirement at30/60/120FPS. Pose BP status3, zero stale types,45 archived default objects repaired preserving values/wiring. No gameplay rollout, graph wiring edits, asset save or restart for this change. Fold into next authorized normal build. [Current contract and validation](Docs/SlashRightArmReturn.md).

- **Attack-start pelvis inertia2026-09-24:** foot-inertia experiment fully removed at user request. Restored original pelvis-only Set Attack Start Pelvis Inertia signature/settings/history, ReadPelvisWorld sampler, original MoveHip leg handling and original tests. Removed ankle-target override, foot/toe rotation handling and generic foot sampler; pre-existing locomotion reconstruction/clamps/knee smoothing/calf recovery remain. Walk raw pin-limit removal retained separately. Original tick timing, latching/retrigger, disable/end/reset behavior unchanged. Used prior save/restart authorization; normal closed-editor build succeeded54.79s, testNN reopened. Three original entry/calf/presentation regressions passed19:39:20UTC, including accepted knee checks. Node refreshed to pelvis-only with retained values/wiring preserved, status3/zero stale types. Pre-restart BP saved/backup BP-BeforePelvisOnlyRollback.uasset; final migrated graph remains unsaved because user Play began before final save, and that session was preserved. No diagnostic gameplay rollout/retuning. Normal binary includes this rollback, raw-limit removal and preceding Lerp Target change. [Current contract](Docs/AttackStartPelvisInertia.md).

- **Normalized knee-bend recovery trial2026-09-24, user acceptance pending:** replaces the infeasible absolute lateral knee-plane requirement with a dimensionless source bend coordinate in the foot-heading frame; always lies on the connected knee circle. Near-straight/vertical/degenerate references continuously fall back to the transported hinge. Existing endpoints,15cm inner reach, floor/pin order, calf return, rotation following and clocks retained. No extra inference/history/timer or work outside active reconstruction. Original solver available immediately with editor `Prophecy.Tempering.KneePlane 1`;3 selects trial(default). Initial no-guidance candidate was rejected/removed for worse knee motion. Paired current scene has identical prefix through130, removes reported first-return stop/resume; six-return max pelvis second difference2.519→1.168cm/tick² and thigh step17.462→11.003degrees. Active knee-direction maxima51.6→29.7left/48.4→43.2right. Eight further kick/over/slash trials improve worst overall steps, but individual later directions/trajectories can worsen: not certified globally perfect. Frozen-input native fixture proves original157 thigh turn46.256124→4.866947degrees without changing rollout; endpoint sweep max.372004degrees, geometry/oracle assertions pass14:33:29UTC. Remaining22 focused tests passed14:31:34; all23 now pass. Harness console-priority issue corrected, no thresholds relaxed. Final build21.13s loaded14:32:52UTC; owned PIE ended/tracing off, source trial3 selected, no BP/map edits/save/restart/push. Fold into next authorized normal build. [Trial, causal proof, limitations and rollback](Docs/Pelvis159HitchDiagnosis.md).

- **Pelvis159–161 original diagnosis2026-09-24 (replacement trial above):** unchanged kinematic replay confirms forward advance3.730→2.103→3.517cm/tick with vertical reversal. Left knee stance-plane guidance is infeasible/off through the near-straight155 pose, then returns at.9854 strength at157, rotating the thigh45.934degrees. Geometry replay matches within.0009degrees. Exact checkpoint replay error2.027e-6; fixed159 input removing only the prior left plane turn changes raw forward4.181→5.679cm and vertical-.938→-.003cm per policy step. Establishes a local causal contribution, not proof of a globally better rollout. A soft20-degree offline turn limit removes the dip but leaves slowdown; not installed or validated as a fix. All recovery weights remainWalk, blend retirement at161 is not required for the159 hitch. Two captures match pelvis exactly, Blueprint ticks verified. No gameplay code/settings/assets changed; owned PIE ended, trace off/capture memory released. [Detailed evidence and limits](Docs/Pelvis159HitchDiagnosis.md).

- **Optional backward pin transfer2026-09-24:** `Set Walk Pinning Backward Transfer` (Agent, Enabled, Multiplier=1), off until called. During visible Walk correction, opposite minimum pin is clamp((1-source backward cap)*Multiplier,0,1):50% release requests50% at1,100% at2. Uses geometric release even for an unselected source; preserves stronger pins. Simultaneous geometry-based requests avoid feedback. User explicitly chose to retain receiving backward/circle caps and reach guard; raw-limit veto subsequently removed at user request. Applied after temporal smoothing without changing its history; Effective Pinning includes transfer and subsequent mixing/veto. Backward bound required, circle alone does not drive it; Run/special/auxiliary hand-source evaluations unchanged. Disable/zero removes settings and added math; no timer, extra inference or duplicate window query. Build105.19s loaded13:40:15UTC; all7 WalkPinning tests including transfer cases passed13:40:58. Node reflected and whole pose BP status3/zero stale types after archived-default repair; no gameplay rollout, node wiring, save or restart. Fold into next authorized normal build. [Contract and checks](Docs/WalkPinningBackwardBound.md).

- **Optional knee-pop smoothing2026-09-24:** `Set Knee Pop Smoothing` (Agent, Enabled, Soft Zone Cm=4), off until called after NN initialization. Near full extension, soft IK moves the foot directly toward the hip by the minimum radial correction; foot height may change as requested. Preserve pelvis, foot rotation and segment lengths. Stable thigh-local bend references prevent amplification of near-straight reversed poles. Shared interpolated render/physical targets in all modes; raw NN recurrence unchanged. Disable/zero/source reset removes settings/reference state; disabled skips extra bone traversal/math/publication work after guards, no timer or extra inference. Exact current-scene617–619 physical bend7.799/0.071/14.363 becomes22.204/21.683/23.528 degrees (87% smaller rebound), ankle correction1.47cm. Full692-frame raw poses identical and target lengths unchanged within9e-14cm; disabled final replay matches all mesh positions/rotations/scales for all3 agents exactly (max error0). Wider right-leg regression from the first prototype corrected before delivery. Build88.03s loaded12:59:07UTC; native clamp/recovery checks including new smoothing cases passed12:59:39. No Blueprint wiring/save/restart; feature off in assets. Fold patch into next authorized normal build. [Contract and bounded evidence](Docs/KneePopSmoothing.md).

- **Per-agent attack checkpoint comparison2026-09-24:** `Set Attack Checkpoint` selects unchanged default Current174664 or viewer Predictive Pin x5 160664 for that agent's next new full/half attack. Running attacks latch their original model; target/type updates preserve history/latches/frame. `Get Attack Checkpoint` exposes selected/effective/attacking. Alternate files installed separately underNN/Attack160664; current exports unchanged. Same geometry/metadata/pin contract, separate active inference groups; no alternate model load in current-only sessions. Export batch1/3/100 parity passed. User asked to receive node before tests: remaining chain validation stopped and gameplay/native switching tests deferred. Normal editor build succeeded93.79s after the user authorized saving the pose Blueprint and restarting; testNN reopened and both nodes/choices verified by reflection. No gameplay tests run. [Usage/provenance/validation](Docs/AttackCheckpointComparison.md).

- **Walk pin bound coordinate fix2026-09-24:** the initial line/circle bound conversion omitted the lower state's `SeedRootRot` before world yaw. Corrected both to transform Z-up lower pose into Y-up native root-heading coordinates first. Debug arrows were already correct; the cap had measured the wrong point. Unchanged20/60cm root0 Walk setup reproduced full pin at about47cm behind root. Before/after240-frame captures now show the effective weight fading inside the range. Four focused tests (including independent coordinate/height/yaw regression) passed11:13:54UTC; Live Coding loaded11:13:31UTC. No Blueprint edits, save, restart or extra inactive work; owned diagnostic Play stopped. Include patch in the next normal editor build. See [bound details/evidence](Docs/WalkPinningBackwardBound.md).

- **Walk pin spatial bounds2026-09-24:** separate `Set Walk Pinning Backward Bound` and `Set Walk Pinning Circle Bound` (Agent, Enabled, Distance Min Cm=20, Distance Max Cm=60, Root Index=0). Off until called. Continuous-window indexing0=current/1..8=future, using selected position and heading for line, position only for planar circle. Current held NN foot distance sets cap1 below Min,0 above Max, linear between; apply minimum after temporal smoothing before Walk foot-roll/policy mixing. Both enabled compose by minimum and share one window query. Line ignores lateral/height; circle ignores height. No transfer to other foot or smoother-history mutation; Run/special checkpoint pinning unchanged. Separate one-frame DevelopmentOnly draw nodes provide white root heading, green/yellow/red ground arrows or rings with radial arrows; traces/drawing occur only on explicit calls. Disabled skips geometry/window allocation/query and retains no bound configuration/timer. Final live build60.52s loaded10:57:25UTC; BackwardBound/CircleBound/Smoothing tests all passed10:58:08UTC. Four reflected functions callable; pose BP status3 after47 archived default-object repairs, wiring/other values preserved, zero stale agent types. No gameplay/visual screenshot validation, node wiring, save or restart; include in next normal build. [Contract and controls](Docs/WalkPinningBackwardBound.md).

- **Optional Walk pin smoothing2026-09-24:** `Set Walk Pinning Smoothing` (Agent, Enabled, Pin In Frames=3, Pin Out Frames=3), off until called. Independent constant-rate pin weights per foot, exact unpaused game-tick timing; reversals retain current weight, zero direction immediate, both zero/disabled remove state and callback. Stable pins do not tick; no extra NN inference. Applied to the visible Walk correction before foot rolling/mixing/clamps; auxiliary hand-source evaluations cannot mutate it. Raw-value and full-extension vetoes remain immediate and clear residual smoothing; no transfer to opposite foot. Special entry/reset/pure Run clear histories but preserve configuration. Live build149.42s loaded10:31:25UTC, focused Smoothing test passed10:31:55UTC (rates at30/60/120FPS, reversals, repeated reads/setters, exact60 ticks, pause, zero, veto/reset/disable). Canonical reflected node callable; pose BP compiled status3,47 archived library defaults repaired with other values/wiring preserved and zero stale agent types. No node wired, asset saved, Play/restart or gameplay validation. Include in next normal build. [Node and contract](Docs/WalkPinningSmoothing.md).

- **Editor first-attack warmup/cache 2026-09-24:** enabled by default in PIE via `Prophecy.Editor.AttackWarmup 1`; three attack models initialize before agents and one exclusive idle model is retained across Play sessions. Content hashes invalidate changed contracts/weights, batch/parity resets precede reuse, gameplay histories are not reused; engine exit/clear command releases storage. Entire feature excluded from non-editor builds. Same-binary302-frame tests: first-attack frame648ms lazy versus31ms cold prewarm and29ms cached; model initialization644ms cold versus16ms cached, both before gameplay. Attack-state sequences identical and startup parity max1.818e-6. Normal build60.22s, all28 focused tests passed09:54:51UTC. Initial empty hash scratch buffer blocked editor; corrected to64KiB, user authorized restart/autosave recovery. Saved/autosave backups retained in `Saved/Diagnostics/AttackCacheRecovery-20260924-1152`;11:36 pose BP/map autosaves restored, BP compiled status3/zero stale pins and saved. No claim of recovering later unsaved edits. Owned PIE ended, captures released, Unreal open, warmup1; no graph edits or push. [Contract, timing and recovery evidence](Docs/EditorAttackWarmup.md).

- **Unpinned knee experiment rejected 2026-09-24:** user reports worse flexion jiggle. Removed free-calf projection and its candidate-only test, retaining the Walk reach guard and earlier accepted reconstruction. Improvement of frame262 minimum bend was insufficient: later bend steps worsened and short supporting-knee reversals at144/146/148 (16.17/17.87/15.01degrees) were unchanged. Judge repeated bend reversals, duration and magnitude across full recoveries, not one minimum angle. Rollback build succeeded55.36s; loaded successfully22:02:52UTC. Fresh602-frame replay exactly matches pre-experiment physical transforms (maximum component error0). Owned PIE ended, trace off and capture memory released. No replacement fix yet. [Rejected experiment and evidence](Docs/UnpinnedKneeRecovery.md).

- **Walk pinning reach guard 2026-09-23:** `Set Walk Pinning Reach Guard` (Agent, Enabled, Frames=6), off until configured. After actual regional pelvis/foot mixing, a selected Walk/mixed pin reaching the connected leg's full-extension boundary is rejected; restore its unpinned prediction with floor clearance and lock out only that foot. Uses current signed returning calf length; no opposite-foot selection or knee-guidance changes. Frames counts unpaused game ticks including rejection; zero adds no cooldown, repeated unsafe requests do not rearm, reset clears cooldowns, disable removes state. Disabled skips geometry/cooldown ticking; callback exists only during an active lockout. Current kickR left knee pops246/250 reproduced; tolerance experiments moved problems elsewhere and were discarded. Guard six-frame replay removes those extension events, while release itself remains fast (10.42-degree thigh step): no claim of universally smooth motion. Disabled replay NN and physical transforms exactly match baseline. Live build155.87s; RigVM reload ensures temporarily blocked remote execution but recovered21:39:18UTC (not a crash/restart). Node reflected, pose BP status3 after archived-default repair with wiring/values preserved; all27 focused tests passed21:41:03UTC. Owned PIE ended, traces off/capture memory released; no node wired or asset saved for this feature. Prior scripts+pose Blueprint checkpoint `c6be3ab` pushed to origin/codex/standalone-sim before this work. Include live changes in next authorized normal build. [Contract, diagnosis and evidence](Docs/WalkPinningReachGuard.md).

- **Walk kick-recovery correction 2026-09-23 — pelvis partly unresolved:** final stance-plane guidance now follows FeetRotation, preventing a full-strength knee turn after slow hinge following. Independent geometry isolated this turn and exact checkpoint replay established its feedback into later pelvis predictions. Five unchanged Walk returns reduce maximum supporting thigh step13.81→4.04degrees, swivel13.90→4.73degrees; no backward supporting knee in364 samples. Pelvis vertical displacement change falls1.864→1.222cm/frame (34%); a smaller abrupt slowdown remains. Good foot descent, calf scale and final connection pass; no new timer, inference, family gate or work outside active reconstruction. Eight bilateral kick/overhead episodes show no full knee spins, but earlier kicks used Run: not equivalent Run A/B. A transient Run override was reasserted to Walk by the BP and is not Run evidence. Gameplay live patch loaded20:37:06UTC; test-only build114.74s loaded20:42:36UTC; all23 focused native tests passed20:49:08UTC. Owned PIE ended, tracing restored and capture memory released; no BP/map edits, saves or restart. Include live changes in the next authorized normal build. [Evidence and remaining limits](Docs/WalkRecoveryPelvisKneeDiagnosis.md).

- **Supporting-foot recovery correction 2026-09-23:** the preceding knee fix left a measured 0.951 cm early vertical reversal in the kinematic supporting foot. Grounded hinge guidance admitted NN thigh motion faster than the held foot, disturbing subsequent recurrent predictions. `SourceFollow` now includes FeetRotation once for both hinge/twist and stance admission; removed the stance blend's second multiplication so the planted knee's effective lateral guidance remains unchanged. Five current-scene returns remove that early dip; supporting knee remains forward in all 366 locomotion samples, thigh step19.76→9.85degrees, calf scale/gap continuity retained. A smaller late rise up to0.196cm remains recorded: do not claim perfectly monotonic NN motion. Independent double-precision evaluation preserves the planted fixture's exact knee plane/lengths and predicts the updated3.494478degree thigh step. Gameplay patch loaded19:55:50UTC; final test-only build244.60s loaded20:09:14UTC; all23 focused native tests passed20:09:38UTC. No new timer/filter/inference, normal-path work, BP changes, save or restart. Live changes still need the next authorized normal build. [Evidence and limits](Docs/SupportingFootRecovery.md).

- **Supporting-knee recovery2026-09-23:** current kickL exposes grounded NN hinge guidance using an untempered foot heading against the held displayed foot. Calf recovery exacerbated some jumps, but the large sweep also reproduced with it disabled. Shared tempering now aligns a copied source leg to displayed foot heading, solves both intact hinges at the final ankle and blends on the knee circle. Ambiguous heading/source bends fade to prior guidance. Existing guidance speed, knee plane, floor/pinning,15cm inner reach, outer-clamp setting and calf return stay intact; no attack-family gate, timer, node or normal-locomotion work. Five complete current-scene returns: supporting backward-knee samples159→0/366, max swivel step65.88→7.83degrees; calf scale continuity and final ankle gap pass. Eight bilateral kick/overhead comparison episodes preserve prior kickR/overL behavior. Kicking foot follow0.7 still commands a rapid initial descent also seen without new calf recovery; do not claim all rapid knee motion eliminated. Final live build30.28s,23 focused tests passed19:43:22UTC including heading-equivariance/degeneracy and existing planted-hitch checks. Diagnostic Play ended; no asset save, graph changes or restart. Include in next authorized normal build. [Diagnosis and evidence](Docs/SupportingKneeRecovery.md).

- **Kick-exit calf-length recovery2026-09-23:** full kick exits capture each actual outgoing calf length and fade its signed difference from rest on the existing `Set Kick Foot Joint Leeway` return clock (1=60ticks, no hold). Reconstruction, pelvis inertia, presentation and authored-target reads share the returning length, preserving the ankle and bend side. The remaining visible snap was a separate render-scale switch: locomotion instantly stretched the calf mesh to the ankle despite smooth joint-centre distance. Kinematic rendering now retains authored calf scale during recovery. Five complete post-restart recovery windows keep scale unchanged; maximum first-tick left tip-gap change falls4.907983→0.004045cm, and all gaps finish below0.02cm. Previous joint-distance checks alone did not prove render continuity. Zero/finished/new special/reset cancels pose work; no extra clock/inference or reflected nodes. Physical compression constraints unchanged; bounded kinematic verification only. Authorized pose Blueprint save/restart completed; normal Editor build succeeded24 actions/247.24s and includes earlier live changes. Diagnostic Play ended, capture memory released, Unreal open on testNN; no graph edits. [Contract](Docs/KickFootJointLeeway.md), [evidence](Docs/KickExitCalfLengthRegression.md).

- **Optional Walk foot rotation after attacks2026-09-23:** new `Set Attack Recovery Foot Rotation From Walk` (Agent, Enabled) is off until called. Each foot uses its existing leg recovery hold/blend, including kick-role mapping, to select Walk→normal orientation while translation and other policy regions retain their existing source. Current Run→Walk translation therefore uses Walk foot orientation throughout. Selection precedes tempering/contact correction and feeds recurrence; final contact-corrected ankle positions can change with orientation. Full/half attacks only, including kicks; no active-special override. Toggle does not restart timing. Zero/Normal regions bypass; completed recovery retains no extra inference, rotation processing or clock. Live Coding build passed375.34s; node enable/disable verified, pose BP status3/zero stale types, four focused tests passed17:25:05UTC. The later authorized calf-fix save/restart incorporated this feature into the normal Editor build. Its node remains unwired in the current scene; no Walk-override scene claim. [Usage and validation](Docs/AttackRecoveryBlend.md).

- **Elbow recovery bend2026-09-23 — user accepted:** kinematic slashR's late elbow swivel came from blending source shoulder rotations and endpoints before solving, not the torso clearance guard. Shared hand reconstruction now transports both complete source hinges to the final wrist axis and blends their bend directions on the exact solution circle; slash idle/NN guidance uses the same solve. Nearly straight/ambiguous guidance retains prior bend. Both hands' tempering/recovery benefit; special output, legs, sword path/winding, initial-distance speed and60-tick timing remain unchanged. No new state/timer/inference or normal-locomotion work. Gameplay patch22 built129.55s; test-only patch23 built74.13s; eight focused tests passed16:55:08UTC. Same three slashR exits reduce late idle-bend error~34→1.54degrees without increasing maximum shoulder step7.31degrees.900-frame six-direction/sword check preserves existing clearance behavior and initial slashLU caveat. Longer1173-frame capture stopped; not claimed complete. User called current result perfect and authorized push; pose Blueprint compiled/saved for that backup. No further gameplay modifications. [Evidence and scope](Docs/ElbowRecoveryBend.md).

- **Slash return initial-distance speed2026-09-23:** user chose one-time distance scaling, not continuous slowing. Existing Return Speed pin now means reference cm/authored-second at100cm: each return copies `ReturnSpeed * InitialDistanceCm /100` once from outgoing wrist to idle mounted on the outgoing clavicle. Constant for the episode even as torso/target moves; next return captures fresh, saved/reset settings stay unscaled. No new node/pin/state/timer; route, winding, blade guard and60-tick ownership durations unchanged. Patch20 built234.15s, loaded16:22:16UTC; seven tests passed16:23:06UTC, pose BP status3/zero stale types;47 archived defaults repaired preserving other values/wiring, no explicit save/restart. Current360-frame trace confirms configured300 becomes fixed57.216839 for initial19.0723cm; old mid-return dip disappears in this capture.900-frame slash variant check retains clearance after initial escape; slashLU begins slightly inside padded envelope at exit (0.978) and clears after4 ticks (≥1.266); do not claim universal snap-free or collision-proof motion. Owned PIE ended, audit off. [Usage](Docs/SlashRightArmReturn.md), [evidence](Docs/SlashReturnSpeedDip.md).

- **SlashR recovery speed dip diagnosed2026-09-23:** current kinematic right-hand target drops about227→165cm/authored-second at exit+9/10 ticks, then accelerates again. Stage trace proves the slash return's speed-capped route reaches the moving local idle target at elapsed7 ticks (Advance becomes1); its catch-up velocity stops abruptly while torso/clavicle motion continues. At the dip BlendToNN alpha0, blade projection0, reach projection0. Disabling upper-body inertia or ordinary hand/forearm clamps retains the dip. No remedy or Blueprint-setting change yet: smooth arrival on the existing safe route is the appropriate next change. Editor-only trace defaults off and is active-path-only; renamed native ApplyPose entry so current manager calls the instrumented live function. Patch19 passed146.47s/loaded16:09:30UTC; current360-frame trace reproduces baseline, pose BP status3/zero stale types, no save/restart, owned PIE ended. [Evidence and diagnosis](Docs/SlashReturnSpeedDip.md).

- **Shared special recovery2026-09-23:** attack (full/half), active parry and active dodge now use common entry cancellation and exit initialization for regional Walk/Run recovery, lower-body and spine-local hand tempering, hand sources, FK core and opt-in upper-body inertia. Defense selects the regular profile; kick roles/leeway/balancing and slash-only sword return remain restricted. Fixed regular lower-profile restoration when no kick override exists. Defense captures its actual two outgoing endpoints before root recentering/callbacks; possessed camera follow/fade now covers defense too. New optional Blueprint interface event **Special Ended** outputs Special, Attack, Half Attack, Returning To Locomotion; existing Attack Ended remains attack-only. Migrated the pose BP's recovery execution chain to Special Ended behind Returning To Locomotion, retaining downstream nodes/values/links; old event disconnected. No explicit save/restart. Live build passed; all33 focused tests passed13:43:43UTC, pose BP status3/zero stale types. Owned PIE passed natural/stopped parry+dodge (71 return ticks each) and600-frame scene replay/six attack exits, finite poses/no runtime errors; owned sessions ended. Durations60 unpaused ticks; completed paths retain no return clock/extra inference/IK. Upper-body inertia remains opt-in/unwired. Include in next authorized normal build. [Contract and evidence](Docs/SpecialRecovery.md), [idle audit](Docs/BlendIdleAudit.md).

- **Upper-body attack-exit inertia2026-09-23:** confirmed the current slashR/FK-follow0.1 handoff abruptly cuts head velocity. New opt-in `Set Attack Upper Body Inertia` provides Enabled, Response Time0.25, Hold0, Blend To Normal0.5, Momentum1. Core spine_01→head plus clavicles carries outgoing angular motion with a damped return; connected arms follow, corrected rotations feed accepted upper state. First interpolation preserves the actual outgoing upper endpoint instead of re-decoding its different spine offsets. Root/pelvis/legs and special checkpoint output unchanged. Hold/blend use60 unpaused ticks; response is a spring coefficient, not recovery lifetime. Zero/disabled/finished skips pose work and retains no clock; specials/reset cancel, reset restores configuration. Audit fixed missing hand/slash-return cancellation on dodge admission; updated two stale knee-test assertions without changing production leg math. Patch15 passed150.76s; all32 focused tests passed13:24:53UTC; pose BP status3/zero stale types/wiring preserved.600-frame enabled current-scene captures reduce initial head velocity change181–203→59–64cm/s (response0.4:51–54); offset spike37.85→<0.003. Fade endpoint remains15–19cm/s, not zero acceleration. No graph edits, explicit save or restart; owned PIE ended. **Node available but not wired; configure before attack end or inside Attack Ended.** [Contract/evidence](Docs/UpperBodyAttackInertia.md), [full idle audit](Docs/BlendIdleAudit.md). Include live changes in next authorized normal build.

- **Attack calf roll2026-09-23:** confirmed current slashR left-calf target jumps139.175° with only2.522° axis swing; the attack export's projected-pole/hemisphere reconstruction matches the observed error. Full attacks now use locomotion's signed knee-hinge calf roll at the UE output boundary, after clamps and before visible/physics/defender-collider caching. Both legs, all full attack families; half attacks retain their locomotion lower body. Positions, thigh/foot rotations, weights/raw decoder and reduced history preserved; calf collider orientation intentionally changes. No new node/state/timer/inference or recurring locomotion work. Patch12 loaded12:43:27UTC; ten PhysicalTargets tests passed12:44:10UTC; pose BP status3/zero stale types.600-frame before/after repeated-attack capture reduces left target max139.175→8.983° and physical frame step69.597→4.491°.142 paired samples retain non-calf poses within0.015cm/0.015°. No graph/scene edits, saves or restart; owned PIE ended. Preserve the user's separately saved pose BP. [Diagnosis and bounded evidence](Docs/CalfRollDuringAttacks.md).

- **Slash right-arm return2026-09-23:** `Set Slash Right Arm Return To Neutral` (Enabled, Hold0.3, Blend To NN0.5, Return Speed100cm per authored second) is opt-in recovery for six slash directions/full and half; **pike and melee excluded**. Configure before attacks or inside Attack Ended. Authored idle arm is carried onto the clavicle; wrist wraps around the torso front with connected arm reconstruction and bounded elbow avoidance. Hand rotation uses the actual sword axis/grip, aims forward and selects a continuous unwrapped winding from blade clearance; the long outward turn is allowed, and an already-forward blade avoids a needless revolution. Position/rotation share route progress; clearance includes blade length, width and grip offset. Earlier wrist-only evidence was insufficient and is superseded. Stored in accepted upper state for recurrence/publication/physical targets; right arm only, no extra inference. Durations60 unpaused ticks; new specials/reset cancel; captured config restored on reset; zero/disabled/finished retains no timer/pose work. Patch11 loaded12:23:37UTC; seven tests passed12:25:20UTC, node callable, pose BP status3/zero stale types.900-frame isolated test passes blade-envelope checks for six full directions and half slashL, including displayed sword and sampled between-frame sweep.360-frame current enabled-scene check passes three slashL returns. Existing initial arm penetration can still require gradual escape; not universally snap-free or arbitrary-pose collision proof. No graph/scene edits, save or restart; owned PIE ended. **Node available, not wired into pose BP** (fresh audit12:26:30UTC). Include live patches in next authorized normal build. [Contract and bounded evidence](Docs/SlashRightArmReturn.md).

- **FK core tempering2026-09-23:** `Set Locomotion FK Core Tempering` (Enabled, Rotation=1) and `Blend Locomotion FK Core Tempering to Normal` (Hold=0, Blend=1) control all ten FK joints from spine_01 through head, including both clavicles. Parent-local shortest-arc slerp;0 holds local bends,1 bypasses. FK retains offsets and carries the arms with adjusted clavicles before spine-local hand tempering. Accepted state drives both recurrence and physical/publication targets. Specials bypass; attack end restores configured follow before the end event; reset restores captured values and cancels returns. Durations60 unpaused ticks, no extra inference/tick; normal/finished skips pose work. Live Coding patch7 loaded10:43:54UTC (393.11s), six focused tests passed10:45:10UTC; both nodes callable and pose BP status3 with zero stale types. Owned enabled/disabled360-frame captures confirmed held rotations/attachments; the existing0.52033cm intermediate-spine translation handoff also occurs disabled and is not introduced by this rotation control. No asset/graph edits, saves or restart; owned PIE ended. [Contract and bounded evidence](Docs/CoreTempering.md).

- **Shared forearm roll for all specials2026-09-23:** attack (full/half), parry and dodge now use locomotion's wrist-derived forearm roll through the same helper. Native trained decoders/weights stay unchanged; correction occurs at the UE output boundary, changing only forearm orientation. Attack visible/physical/defender-collider cache and defense PHAT checks/publication share the corrected orientation. This intentionally changes forearm collision orientation, not joint positions, wrist/shoulder rotations or scale. No new node, state, tick, timer or inference; work only on new special poses, no additional locomotion path. Fixes the diagnosed attack-vs-locomotion roll mismatch (left104.51deg/right172.23deg), rather than merely blending it away. Live Coding patch5 loaded10:05:46UTC (92.25s), no object changes. Nine PhysicalTargets tests passed10:06:18UTC; pose BP status3, zero stale types, wiring preserved. Owned360-frame PIE exercised full/half attacks, parry and dodge; both arms matched wrist-derived target within0.000064deg over122/32/20 respective samples. Owned PIE stopped; no scene/graph edits, asset saves or restart. Include patches in next authorized normal build. [Contract/evidence](Docs/HandRecovery.md#shared-forearm-roll-for-all-specials--implemented).

- **Hand recovery2026-09-23:** three nodes provide independent left/right spine_05-local XY/Z/rotation tempering, hold/blend to normal, and Normal/Walk/Run attack recovery. Tempered hand targets, source arm chain and retained forearm twist move with the decoded upper torso; source-only recovery and separate hand inertia retain their existing reference. Spine-reference Live Coding patch loaded10:21:53UTC; four HandRecovery tests passed10:22:40UTC, including independent axes and blend-to-normal. Owned360-frame PIE confirmed zero-tempered wrist orientation follows spine within0.000022degrees across three agents; pose BP compiled with zero stale types. No new timers/storage, graph edits, saves or restart. One profile for all attacks, no kick variants; durations60 unpaused ticks. Walk/Run conditions the same upper checkpoint on corrected/rebased lower predictions, sharing accepted history; only arm outputs are mixed. Extra inference/reconstruction ends when recovery expires/all controls are normal. Hand/leg timelines are independent; specials bypass/cancel hand recovery; reset restores configured/current values and cancels timelines. Arm refinement now transports the rebased accepted elbow hinge instead of the newly predicted shoulder alone; wrist roll cannot orbit the elbow. Source forearm length returns continuously instead of forcing reference length then releasing. Publication retains actual forearm twist, follows its new aim and blends twist; no new timer, actor state or attack-family gate. Keep endpoint positions from rebased NN state: cached component positions may predate a root-only snap. Final Live Coding patch4 loaded09:40:33UTC, no reflected object changes. Three focused tests pass09:41:39UTC; pose BP compiles status3, zero stale types, wiring preserved. Three-return360-frame comparison reduced max forearm steps104/74→7.5/13.5degrees and shoulder-relative elbow steps1.78/3.89→0.91/2.44cm. Right upper-arm maximum later in recovery remains about9degrees; do not claim universal visual perfection. No scene/graph edits, explicit asset save or restart; owned diagnostic PIE ended. Include live patches in next authorized normal editor build before reopening. See [HandRecovery](Docs/HandRecovery.md) for contract, evidence and limitations.

- **Attack ending trim2026-09-23:** `Set Trim Attack` provides all16 named attack integer pins, default0, per agent. User changed units to60Hz: 1 removes1/60 second, 2 removes one30Hz NN frame. Whole-frame tail remains cached; odd values use one intervening unpaused game tick at the final interval, then the one-shot callback unregisters. No persistent ticking, timer or extra inference; normal locomotion has no trim polling. Full/half attacks share family settings; negative inputs reject, trim saturates at the post-Hit lower bound, zero clears settings. Stop, family/settings changes and world teardown cancel pending handoffs; Armed/Hit/history and normal Attack Ended/recovery remain. Configuration persists across attack/reset cycles. Live Coding rebuild passed240.78s and loaded00:23:12UTC; all16 reflected calls, zero/negative validation and Prophecy.Attack.Trim passed00:23:32UTC (consecutive60Hz endpoints, family isolation, odd/even scheduling, cancellation and callback retirement). Pose BP compiled status3 with zero stale native properties/pins and preserved wiring. No restart, asset save or gameplay rollout. Include live changes in next normal editor build before reopening. Evidence Saved/Diagnostics/AttackTrimValidation.json. See [usage](Docs/SlashBlueprintUsage.md#trim-the-ending).

- **Attack checkpoint174664 — installed September24:** active source is the exact checkpoint from the user-linked `best_total_172k175k_20260924` viewer, SHA256 `3c68cbeec601b814d569c53c577812402b17cb4f94a931d657bcf3f8f9b5a61f`. Replaces123793; same no-frozen architecture, clamps/geometry and phase rules, with its serialized0.99-to-full pin transform applied once before foot projection. Export records the threshold; native defaults legacy exports to1. Python replay exactly matches448 stored viewer frames/30 segments; Unreal446-transition autoregressive parity max0.015556mm, one-step0.001352mm, all Armed/Hit latches equal. Export batches1/3/100 max2.146e-6. Live build248.58s loaded10:09:32UTC; installed hashes and startup parity verified. Idle editor cache cleared, next Play prepares new model. Previous exports retained in `Saved/CheckpointBackups/20260924-101457-before-174664`; stage/evidence `Saved/Slash174664`. No Blueprint/map edits, restart, gameplay rollout or push. **Include this live runtime change in the normal editor DLL before next fresh launch.** [Current contract/evidence](Docs/AttackCheckpoint174664.md); [previous integration and removed redundant NN leeway rule](Docs/AttackCheckpoint123793.md).

- **Kick foot floor diagnosis2026-09-22:** current kickL loop confirmed support-foot penetration in kinematic mode. Existing5cm kick joint leeway projects the NN foot onto the authored calf axis with extension clamped to[0,5]; it cannot retain a shorter NN calf and can push the foot downward after the NN floor correction. Raw-to-presented replay reproduces all captured attack feet within0.000029cm; one support foot is pushed down3.98cm from a requested4.52cm compression. This function predates limb-role changes; no causal claim that the latest role mapping introduced it. No gameplay changes yet. Two owned600-frame runs ended, user setup/assets preserved. See [diagnosis and evidence](Docs/KickFootFloorDiagnosis.md).

- **Kick limb roles2026-09-22:** kick recovery controls now mean Pelvis / Kicking Leg / Non Kicking Leg, automatically mapped for kickL/kickR. Kick tempering has independent Kicking Foot and Non Kicking Foot XY/Z/rotation plus pelvis controls. Existing Blend To Normal uses each foot's own initial values and the shared feet timing; pelvis retains separate timing. Both foot roles propagate through toe motion, reconstruction source guidance and calf twist continuity. An unchanged foot/pelvis bypasses tempering reconstruction unless regional policy mixing requires it. Reset captures both feet and cancels all return timelines. No new clock/inference or idle modifier work. Native old pin names remain for compatibility; explicit node refresh copies old shared foot inputs to both new groups. Normal editor build passed115.26s; final test-only rebuild passed60.75s. Unreal reopened on testNN, both role nodes refreshed with existing values/links preserved, new non-kicking inputs copied from shared feet, and pose Blueprint compiled status3 and saved. All11 focused tests passed20:21:05UTC (role mirroring, per-axis pose/toes, return/hold retirement, reset, regional policy, calf continuity and60-tick clock). Initial role-test rotation assertion differed by one float ULP; corrected its tolerance to1e-6, with no gameplay change. No gameplay rollout. Evidence `Saved/Diagnostics/KickRolesValidation.txt` and `KickRolePinValidation.json`; asset backup `KickRoles-BeforeRefresh.uasset`. See [recovery](Docs/AttackRecoveryBlend.md) and [tempering](Docs/LowerBodyTempering.md).

- **Kick recovery profiles and sword-owner Hit restoration2026-09-22:** `Set Kick Locomotion Lower Body Tempering` and `Set Kick To Locomotion Blend` configure a separate kick recovery profile, automatically selected at return to locomotion before On Attack Ended. Until configured, kicks retain regular behavior. Matching event setters affect the current handoff; regular setters cannot overwrite an explicitly selected kick profile. Existing tempering Blend To Normal consumes the selected XY/Z/rotation values with independent feet/pelvis timing. Regular attacks select the regular profile. Force Run is removed; regional Source pins exclusively choose Normal/Walk/Run. No knee/pin math changes, new per-frame profile selection, clock or inference; finished/all-one/zero bypasses remain. Reset clears the active tempering selection/blends before restoring its baseline. The held sword restores its owner-body contacts on first NN Hit for every family, retaining the gripping-hand exclusion and attack sweeps/solver/physical context through the remaining attack. Explicit body-body collision overlays are unchanged. Normal editor build passed108.36s after user-authorized restart; testNN reopened. Startup rebuilt missing cached engine shaders. Both reflected nodes verified; three recovery nodes refreshed with other values/links preserved, pose BP compiled status3 and saved. All six focused tests passed19:14:49UTC (KickProfiles, AttackRecovery, ReturnTimeline, SeparateReturns, SixtyTickClock, actual Jolt AttackCollisionPhases). Collision checks cover simulated/attached swords, native owner-pair restoration at Hit, retained active-attack context, repeated refresh and unchanged body/joint counts. No gameplay rollout or scene changes. Evidence `Saved/Diagnostics/KickProfilesValidation.txt`; before-refresh BP backup `Saved/Diagnostics/KickProfiles-BeforeRefresh.uasset`; see [recovery](Docs/AttackRecoveryBlend.md), [tempering](Docs/LowerBodyTempering.md), [sword collision](Docs/SwordAttackCollision.md).

- **Stop locomotion requests Walk2026-09-22:** `Stop Locomotion Input` now clears `LocomotionInput.bRun` as well as movement/facing vectors. Previously it retained Run indefinitely when the low-speed override was disabled. Existing checkpoint selection and Run→Walk blend own the transition; no direct policy reset, extra tick or velocity reset. Scales, normal braking and explicit auto-run/attack-recovery overrides retain their behavior. Live Coding build passed132.08s, patch loaded18:44:43UTC with no object reinstancing. Source path verified through mover intent, checkpoint selection and existing directional blend; no scene test, Blueprint edit, save or restart. Included in the108.36s normal kick-profile build on September22. [Input contract](Docs/LocomotionBlueprintInput.md).

- **Active attack Trigger contract2026-09-22:** `Trigger NN Attack` edits an ongoing attack, not restarts it. Active calls update target/victim, family labels/tail and requested half/full ownership while preserving Armed/Hit, first-Hit/policy frames, recurrent history and current pose. No Stop/Ended, root handoff, static reinitialization, recovery/camera restart or solver/sweep end/start. Family-only side effects retarget fist levels without resetting their clock, update sword weapon/melee gating from the existing phase, and transition kick foot leeway only when crossing kick/non-kick. `Stop NN Attack` followed by Trigger explicitly starts fresh; natural completion remains Hit plus the selected family's tail. Invalid names/half kicks are rejected before mutation. Normal build passed285.45s after Live Coding reload crash; reopened testNN. Sword collision phase test passed11:34:04UTC. Owned PIE passed11:34:38UTC: same-type repeats, Armed/frame10 and Armed+Hit/frame13 family changes, unchanged immediate root/published pose, target/victim updates, rejected requests, natural completion despite repeated triggers, fresh reset and half/full switching. Evidence `Saved/Diagnostics/AttackRetargetPIE.json`. Pose BP compiles status3; no user graph changes or explicit asset saves. This normal build also includes the target-margin and Time Dilatation label changes. [Usage](Docs/SlashBlueprintUsage.md).

- **Time node labels2026-09-22:** user-facing names are **Set Agent Time Dilatation** / **Get Agent Time Dilatation**. Native `*TimeDilation` identifiers remain unchanged to preserve existing nodes. Label-only Live Coding build passed155.81s; reflected labels verified and pose Blueprint compiled status3 after repairing15 archived library defaults, all other values/connections preserved. No runtime behavior change, restart or explicit asset save.

- **Attack target reach margin2026-09-22:** `Get Valid Attack Target` additionally returns **Distance To Limit**, in cm: `max(0, GT radius + extra reach - wanted target planar distance)`. Measured against the same live self-balancing center as its projection; height is ignored. Boundary/outside/invalid inputs return0; the original three vector outputs remain unchanged. Reuses the existing planar distance; no tick or timer. Live Coding loaded10:38:54UTC; geometry test passed10:41:34UTC. Explicit editor refresh added the output to both existing nodes, preserving their values/links; whole pose Blueprint compiled status3. No restart, gameplay test or explicit asset save. Include patches in the next normal build before reopening. See [AttackTargetReach](Docs/AttackTargetReach.md).

- **Constrained skeletal props2026-09-22:** fixed the mixed Chaos/Jolt ownership in placed `A_Pot`. Ordinary skeletal constraint endpoints now import their passive PHAT rig into shared Jolt; the rope has29 bodies/28 joints and a kinematic Root, with the pot attached to actual `joint27`. Agent bindings unchanged; no new actor tick, no source asset edits. Positive component scales are supported: the user's `(0.492965,0.492965,1)` rope originally tripped a unit-scale capture guard, now removed with matching scaled pose publication. A completed12-second scaled scene capture confirms native external ownership, fixed top and numerical bone/socket agreement. Normal editor scale build passed54.24s and testNN reopened under user authorization. Do not claim zero joint stretch: the external attachment still showed5.65–9.19cm separation at defaults; temporary PHAT iteration/collision experiments did not establish a full solution and no solver/collision defaults were changed. Existing hard-angular PHAT mapping/deferred Chaos features still apply. See [implementation, validation and limits](Docs/ConstrainedSkeletalPhysics.md).

- **Current knee/calf continuity2026-09-21:** user confirmed the global knee correction keeps kicks good and makes overL much better. Carry the source knee's lateral stance offset instead of forcing it to zero; choose the nearest coherent circle branch and fade ambiguous source headings/branch ties. No attack-family gates. The remaining overL calf snap was independently measured at31–54degrees/frame: the lower state omits calf twist, and locomotion re-decoded the outgoing attack with a different orientation convention. During feet tempering, preserve the preceding published calf rotation, transport its aim, and follow the new twist using Feet Rotation. Repeated same-time publication does not advance it again. No new timer, state layout or NN inference; inactive/normal bypasses calf blend maths. Six-overL comparison reduced initial calf steps to2.4–3.8degrees with bit-identical lower/upper NN inputs, outputs and published lower poses. Knee/pelvis paths are identical; interpolated foot differences stay below0.05cm. Six-kick A/B likewise keeps NN inputs/outputs and pelvis/knee paths exact; interpolated feet differ by at most0.225cm. New calf regression passed;11/12 focused checks pass, with the same two legacy assertions failing in RootLocalPinAndChain. Final Live Coding patch13 loaded16:19:12UTC; normal editor DLL still requires rebuilding before any approved restart. See [current diagnosis and evidence](Docs/OverLRecoveryKnee.md). The older zero-offset guidance below is historical and superseded; retain the15cm inward safeguard and accepted support-source handover.
- **Tempering knee reconstruction — accepted2026-09-20:** minimum inward clearance is15cm, including projection into the foot-forward vertical plane. Sphere-only clearance still allowed sideways singular passes. Transport the previous coherent hinge, then solve the knee circle in the foot-forward plane using one oriented bend branch; no hip-height branch flip or synthetic upright guidance. Existing Calf Clamp controls distant reach; no added outer clamp. Inactive/all-one bypasses the entire tempering solve. Live Coding loaded15:17:05UTC; user explicitly called this version perfect. Keep it unchanged unless asked. See [LowerBodyTempering](Docs/LowerBodyTempering.md).
  - **Support-leg refinement2026-09-20, visual acceptance pending:** user authorized experiments to reduce pelvis hitch while preserving kick descent. Retained forward-knee guidance and reach rules; near-floor legs now gradually admit their untouched NN source hinge with a20-degree source handover bound per30Hz policy step. Raised/frozen feet retain the previous source. Each leg uses its own Run/Walk source/pelvis. No alternate hidden recurrent pose, new clock/history/layout, or extra inference; all-one/inactive bypasses new work. Live Coding loaded18:30:17UTC; final600-frame capture exactly matches the selected experiment's lower inputs, outputs, published poses and pelvis targets. Eleven-recovery comparison reduced pelvis hitch and maximum thigh-rotation steps each cycle, with no observed new bend flip or accumulating drift. Early kick trajectory changes slightly and later descent reaches20cm height one NN step later; do not claim identical motion or universal perfection. Editor A/B: `Prophecy.Tempering.SupportSource 0` restores the old source;1(default) enables refinement. No Blueprint/scene edits, asset saves or restart. [Full experiment evidence and limits](Docs/PelvisRecoveryHitchDiagnosis-20260920.md).
    - Six focused tests passed, including new SupportSourceContracts and existing timing/axis/chain/regional checks. RootLocalPinAndChain retains two legacy assertion failures (oblique radial-pole expectation and sideways sweep step bound). Their null-source path is text-equivalent to the previous solver; independent replay proves the expectations conflict with the accepted plane/fixture behavior. Neither assertions nor solver were changed to hide those failures. Evidence: `Saved/Diagnostics/RecoveryCandidateReviewLegacyTests.json`.
  - Accepted replay `ThighOutward-20260920-171739`:20 attacks,384 recovery policy samples per leg, no reversed hinge among stable samples; left target-side residual below0.000016cm. Presented abduction p950.382 degrees, largest thigh step21.13 degrees. No claim of universal correctness. Old1.2 multiplier node remains, but effective minimum is max(15cm, multiplier*length difference). Earlier solver diagnoses/rollbacks below are historical.
  - Follow-up inward-foot diagnosis, runtime unchanged: recovery peaks after kicks1–4 move7.19/15.36/16.00/18.93cm toward the other leg relative to the kicking hip; kick4 reaches11.18cm beyond the pelvis midline. Blended raw NN already requests18.81cm at that peak, while new clearance adds about0.05cm. Run/Walk individually request19.81/17.58cm. This is post-attack descending locomotion recovery, not an active-attack tempering leak. Recurrent poses and later attack seeds differ, but no causal ablation isolates which setting produces the whole cross-kick change. No anti-crossing constraint added. Evidence: accepted capture `foot-drift.json` and `Saved/Diagnostics/FootDriftAudit.py`.
  - **Root-cause audit after rollback:** fresh600-frame replay `ThighOutward-20260920-170452` reproduced the baseline. Independent solver replay matches180 recovery knee positions within0.000023cm. At kick3 the guidance increases sideways displacement16.76→23.38cm; full guidance would yield29.39cm. Endpoint distance11.69cm is outside the4.27cm minimum but nearly fully folded; exact zero-sideways solutions both put the knee behind the hip. The discarded solver's hip-height reflection caused a108-degree policy-step jump from only9degrees of raw-thigh change. Current restored runtime unchanged. See [measured diagnosis and constraints for a replacement](Docs/KneeReconstructionDiagnosis-20260920.md); no claim that the issue is solved.
  - Backward knee confirmed in the earlier600-frame capture: left94/177 recovery samples had negative bend sign; right0/177 (`ThighOutward-20260920-155954/signed-knee.json`). Direct signed-normal guidance reduced that but reintroduced a47-degree sideways-crossing step; discarded. Current reference-transport tests cover backward/oblique sources and a continuous sideways crossing; both focused leg tests passed14:21UTC.
  - **Current unresolved outward motion:** baseline patch loaded14:16UTC;600-frame/10-kick capture `ThighOutward-20260920-162025`. Recovery knee sideways peaks after kicks1/2/3/4:15.92/28.01/36.83/38.68 degrees; after kick3 lateral offset23.38cm. Future and presented targets agree, with simulation mode Kinematic. Max recovery thigh step14.81 degrees; no negative anatomical bend sign with ankle below hip, but2 above-hip near-singular samples remain. The outward motion is real and is NOT declared fixed. Evidence `outward-by-kick.json`, `signed-knee.json`, `recovery-summary.json`.
  - **Rollback completed15:01:52UTC:** subsequent knee-circle/foot-plane experiments reduced outward drift in some samples but introduced roughly67–75-degree recovery steps in repeated kicks (`ThighOutward-20260920-164826`, `ThighOutward-20260920-165427`). User rejected the snapping regression. Restored the14:16UTC solver and its original test assertions, preserving the1.2 minimum-reach rule; Live Coding loaded successfully and both focused leg tests passed15:02UTC. A candidate must address outward drift, snapping, jitter and backward knees together; isolated test passes or improvement in one metric do not establish success. No Blueprint/map edits or restart.
  - **Inner reach:** user requested `1.2 * abs(thigh length - calf length)`, derived per leg, replacing the proposed arbitrary20cm radius. `Set Locomotion Minimum Leg Reach` exposes Multiplier (default1.2;1 gives the mathematical minimum). Applies only in active locomotion tempering reconstruction, leaves foot rotation unchanged. Existing locomotion Calf Clamp controls outer reach; disabling it preserves distant endpoints, with a straightened thigh for unreachable targets. This is not a general anatomical workspace or joint-angle limit.
  - Current formula compiled/loaded through Live Coding at13:58UTC. Reflected node callable, pose-agent BP compiled without asset save; `RootLocalPinAndChain` and `PelvisInertia.LegChain` passed. Checks cover inner projection, preserved distant endpoints with outer clamp off, unchanged foot rotation and connected reachable legs. Current skeleton minimums: left4.2685cm/right4.4379cm. Final600-frame/10-attack replay `ThighOutward-20260920-155954` completed; recovery abduction p9517.98 degrees/max42.79, but per-frame thigh change still reaches29.31 degrees. Inner boundary implemented; remaining recovery snapping/backward knee poses are not declared solved. Diagnostic callback/CVars restored and only owned PIE ended; no BP wiring/map changes or restart.
  - **Kinematic diagnosis:** the user's kinematic-after-first-attack setup reproduces the outward knee without physics. Baseline `ThighOutward-20260920-144637` measured recovery abduction p9570.8 degrees/max86.2 degrees, with p95 lateral knee offset36.8cm. Earlier attribution to physical wobble was insufficient. Subsequent experiments are saved under `Saved/Diagnostics/ThighOutward-*`; final validation must be reported separately from those superseded candidates.
  - Earlier mixed-mode detection (`Saved/Diagnostics/ThighOutward-20260920-143727`) already measured90.36-degree sideways knee heading physically and90.94 degrees in the presented target. Kinematic confirmation above supersedes any inference that physics alone caused the instability.
  - Diagnostic node **Set Leg Chain Reconstruction** (Agent, Enabled=true) disables the extra chain-repair/reach/floor passes for tempering, pelvis inertia and Dodge lower modifications, while retaining their pelvis/foot changes. Does not disable native NN decoding, clamps, pinning or Jolt joint constraints. Per-agent override has no tick/timer/inference work; normal modifier-free locomotion does not query it. True removes the debug entry. No automatic Blueprint wiring.
  - Normal Editor build passed2026-09-20 11:56UTC (196.95s,23 actions) after user-authorized save/restart. The preceding Live Coding build stalled under memory/disk pressure: editor15.85GB private commit, LiveCodingConsole3.37GB, crash reporter1.99GB on16GB RAM; pagefile10.8GB used and C:2.1GB free. Retained editor allocations were not individually attributed. The2.42GB PCH already existed sinceSeptember16; not created by this node. Stopping/retrying one stalled compiler without UBA did not solve the pressure. Temporary build configuration was restored exactly. The closed-editor build completed normally. Reopened `/Game/testNN`; startup rebuilt missing engine shaders, then blocked on a Low Drive Space modal. Computer-use initialization failed; user dismissed it. Node reflection and existing pose-agent Blueprint compilation verified afterward with no wiring changes or saves. No DDC/cache deletion or scene testing. Fresh editor private commit was about6.93GB after loading, compared with15.85GB before restart; different session/cache state, not proof of a specific leak.
  - Live Coding succeeded; `LowerTempering.RootLocalPinAndChain` and `PelvisInertia.LegChain` passed, including an independent deep-fold/foot-rotation oracle. Current unchanged kickL setup replayed for4,731 captured frames/78 attacks. Largest visible left-knee bend-direction change was26.77 degrees (startup), compared with138.28 before; the large sideways flips did not recur in this bounded run. This does not certify all poses or eliminate the kick-end snap: visible calf rotation still jumps roughly46–60 degrees on many attack exits. A120-frame target readback confirms that snap also exists in the NN target (roughly60–63 degrees at two exits), with interpolated target and physical calf closely matching. It is not resolved by this patch. No Blueprint/map edits, temporary callbacks removed and only the diagnostic PIE stopped. Evidence: `Saved/Diagnostics/KickKnee-20260920-125101` (baseline), `KickKnee-20260920-130855` (patched, including `target-check.json`). Desktop computer-use backend failed, so observation used Unreal screenshots/socket readback.

- **Attack-ended Blueprint event:** `AProphecyAgent::OnNNAttackEnded` appears as **Event On Attack Ended**, inherited by the pose-agent Blueprint. Invoked once at the end of the shared active-attack stop path, after root/sword/fist/recovery cleanup; covers natural completion, cancellation and interruption. Active Trigger updates do not stop/restart and do not emit this event. Outputs the ended attack name and **Half Attack** boolean (true for half, false for full), both captured before cleanup; inactive repeated stops emit nothing. Suitable for snapshot/tempering blends, with no per-frame polling. Reset baseline restoration still wins over returns started during reset.
  - Half Attack pin loaded through Live Coding2026-09-20 15:45:43UTC. Existing EventGraph event has `bHalfAttack` and retains its original execution link. Repaired91 stale native agent property references from reinstancing, compiled pose-agent BP successfully (status3), wiring/default fingerprint unchanged by repair. No asset saves or editor restart; no gameplay test. User authorized stopping Play to complete the memory-constrained build.
  - Live Coding succeeded 2026-09-20 10:10 UTC. Read-only reflection check confirmed the loaded base event and pose-agent Blueprint inheritance. No gameplay test or Blueprint wiring changes; helper: `Saved/Diagnostics/CheckAttackEndedEvent.py`.
  - That reflection-only check missed a Live Coding failure: existing native function parameters and graph pins retained `/Engine/Transient.LIVECODING_ProphecyAgent_0`, causing incompatible Self/Agent errors when the user wired the event. Repaired 2026-09-20 10:25 UTC: 89 native references and 66 pin types retargeted to the canonical base class; pose-agent Blueprint compiled with BS_UpToDate, wiring/default fingerprint unchanged. Explicit editor-only command `Prophecy.Editor.LiveAgentTypes Inspect|Repair`; no startup/polling hook, asset saves or node replacement. Require a full affected Blueprint compile after native base-class reflection changes, not just event existence checks. Reports: `Saved/Diagnostics/LiveAgentTypes-Repair.txt` and `LiveAgentTypes-Inspect.txt`.

- **Clamp profile snapshots:** the existing Save Physical Profile Snapshot includes shared left/right Locomotion/Attack/Parry/Dodge clamps (15 mode/type settings). Blend Clamp To Snapshot selects Calf, Forearm, Hand or Foot independently; Blend All Clamps To Snapshot restores all existing types. Both default to all modes. These are existing clamp settings, not per-bone profiles: no Below node or bone selection. Smoothstep uses60 unpaused ticks per authored second; off transitions use1000 allowance before restoring exact flags, setters cancel matching returns, completion removes the callback. Print appends locomotion clamp leeways/off only on hand/foot rows, without units. Re-save old snapshots. See [PhysicalProfileSnapshots](Docs/PhysicalProfileSnapshots.md).
  - Clamp-type selector revision compiled and loaded via Live Coding on 2026-09-20; `Prophecy.PhysicalProfiles.ClampSnapshots` passed, including independent Foot/Calf and Forearm/Hand selection. No gameplay scene test or Blueprint/map edit performed.

- **Editor debug-camera input:** `;` / `ToggleDebugCamera` preserves the original Prophecy Agent's existing Blueprint key events and legacy action bindings through a non-consuming temporary input component. Created on activation, destroyed on exit/EndPlay; no Tick/polling. Entire installation/forwarding path is `WITH_EDITOR` only, with no runtime hook/work in packaged games. Axis/look input stays with the free camera; custom camera classes are preserved. See [DebugCameraInput](Docs/DebugCameraInput.md).
  - Live PIE check passed: existing Right Shift drops sword in debug camera, re-entry preserves the Blueprint FlipFlop and equips once, normal input still works after exit. Loaded with Live Coding, no restart or Blueprint/map edits.

- **Reset hit-branch audit:** explicit reset at frame100 followed by automatic overL and a hookL retained both hit events and true defender-side PhysicalMesh/Attacking/not-self/Armed checks, including hand-to-head contacts. False recorded cases were self-collisions or a non-attacking Other actor. No reset bug reproduced in that AND; branch unchanged. See [AgentReset](Docs/AgentReset.md).
  - **Follow-up reproduced the actual one-shot loop:** the user's hit chain sets `dead debug=true` before Delay0.8/reset. Native reset restores physics but not arbitrary BP variables; the retained flag blocks the outer Event Hit gate and Tick. Defender `tick debug` stays83 through frame600. Clear that BP flag as part of the BP reset flow; the AND itself is not the failing gate. Evidence: `Saved/Diagnostics/HitResetLoop.json`.
  - **Sword reset:** Initialize Agent Reset now also captures held/empty equipment, sword class and simulated/attached mode. Reset recreates the captured sword at the restored hand and removes its tracked dropped copy; repeated resets track the replacement. No per-frame work. Requires a fresh Play session to capture equipment after the patch; external references to the old sword must be reacquired. User requested compilation during PIE and will perform scene tests. See [AgentReset](Docs/AgentReset.md).

- Follow the user's scope. Resolve routine implementation/build issues autonomously; ask about material behavior/design ambiguity. Do not begin unrelated optimization, benchmarks or extensive tests.
- Default to a successful compile and minimal meaningful checks. The user normally does extensive scene testing. Isolated tests do not establish scene quality or current packaged gameplay.
- **Blend/hold duration convention: 1 means60 unpaused game ticks, regardless of actual FPS or time dilation.** Do not substitute wall/game elapsed seconds or count30Hz NN evaluations as60Hz ticks. Inactive/finished features must not introduce recurring timer, interpolation, IK or extra-inference work. Scope: locomotion/recovery, tempering, physical-profile blends; [details](Docs/BlendTickTiming.md).
- **All new authored blend/fade durations use that same60-unpaused-game-tick convention, including camera fades.** This is deliberate tick counting, not wall-clock timing:1 takes2 real seconds at30FPS and0.5 at120FPS. Duration0 is immediate; completed/inactive fades retain no tick work.
- **Specials = attack (full or half), active parry, active dodge.** A defense queued before attacker Armed still permits ordinary locomotion. Per-agent time dilation must reset permanently to1 when a special actually starts, remaining1 after it ends until explicitly set again. User clarified that per-agent time dilation must not accelerate our gameplay blend/hold clocks:1 authored second remains60 unpaused game ticks.
- **Time dilation / magic-cube units:** checkpoint selection uses agent-local trajectory speed; playback-rate changes alone must not select another gait. `handle magic cube` must drive `(Root[1]-Cube)/TimeOffsetsSeconds[1]`, not a hardcoded30Hz divisor. Convert world-time positional feedback to local magic velocity using `WorldDeltaSeconds * Get Agent Time Dilation` in the denominator. Apply tick settings before driving the cube. The user's September21 kinematic walking mismatch was false cube feedback, not knee reconstruction: old cube lag10.48cm created-125.75cm/s local braking. Arithmetic and tick-order migrations compile/leave BP unsaved, with original saved asset backed up. Full diagnosis and current validation: [AgentTimeDilation](Docs/AgentTimeDilation.md).
- Preserve user PIE, unsaved assets, scene transforms and Blueprint wiring. Do not save unrelated dirty assets or dismiss import/material dialogs on the user's behalf.
- **Use Live Coding for routine C++ changes. Never close/restart Unreal without user authorization.** Reflected changes alone do not justify a restart; new nodes can use separate Blueprint libraries.
- **Exception: do not Live-Code reflected changes to existing WorldSubsystem classes, especially ProphecyJoltWorldSubsystem.** This causes Ret->IsA during world creation. Add nodes in separate libraries.
- **Agent tooltip reload crash2026-09-22:** even changing `AProphecyAgent` UFUNCTION documentation can make UHT reinstance the entire agent class. The Trigger-in-place build compiled successfully but crashed during agent Blueprint replacement in `FDynamicRenderAssetInstanceManager::Remove`, invalid component/CDO assertion, before gameplay testing (crash `UECC-Windows-5E0631A54B1B9A949DB9D2A7FDEE5B13_0000`). Prefer external docs for routine runtime-only changes; do not provoke base-class reinstancing merely to update tooltips. Recovery uses a normal build with the already-crashed editor closed. Latest13:14 local pose-agent autosave copied to `Saved/Diagnostics/AttackRetargetCrashRecovery-20260922`; original asset untouched.
- Reinstancing does not migrate retained non-UObject allocations. Preserve live layouts; adaptive-unity regrouping of nontrivial globals also needs care.
- Editor target now disables **adaptive** unity (`bUseAdaptiveUnityBuild=false`), keeping ordinary unity enabled. Git commits must not change translation-unit ownership underneath Live Coding's retained globals. After the2026-09-19 backup commit, the next patch regrouped `ProphecyAgent.cpp`; first PIE crashed in patch13 at the `CVarProphecyAuditManualPhysicalFollower` read in `Tick` (null address). Print-node reflection calls had passed, but did not cover actor ticking. Crash report: `Saved/Crashes/UECC-Windows-A55E718941349E73648C62BA2FA99233_0000`. Recover with a normal build of the closed editor, not another patch over that session.
- Recovery verified2026-09-19 15:30UTC: normal Editor build succeeded, reopened `/Game/testNN`, completed60 PIE frames with all3 agents ticking and the print node called each frame (22 physical-bone rows each, no unit suffixes). Ended only the diagnostic PIE; editor remains open. No Blueprint/map edits or saves. Evidence: `Saved/Diagnostics/recovered_profile_print.json`.
- Newly added cross-DLL symbols/classes may be unavailable through existing import libraries during Live Coding. Event-only reflected bridges are used where needed. Stale Python wrappers for new types are not grounds for a restart.
- **After Live Coding changes to an existing Blueprint function library, compile the user's existing pose Blueprint before reporting completion.** New-node reflection/PIE API checks alone miss archived default objects on older call nodes. If an unlinked self pin references `BPGC_ARCH_FOR_CDO_*`, use `Prophecy.Editor.RepairLibraryDefaults`: it replaces only archived GameAnimationSample3 library defaults, verifies all other values/connections, compiles, and leaves unsaved. September22 attack-target follow-up repaired28 such defaults, status3 and wiring preserved; no function signature removal or runtime behavior change. Validation script `CheckAttackTargetReach.py` now includes this check.
- Normal Editor builds: retain Live Coding support, use the normal target with -WaitMutex -NoHotReload, and omit -NoLiveCoding. Changing WITH_LIVE_CODING causes broad rebuilds.
- Include live patches/new libraries in the next planned normal build before relying on a fresh process. Do not restart merely to replace a working patch.
- **Fresh-launch regression2026-09-21:** reopening without incorporating the previous session's Live Coding changes loaded an old18:09Sept20 game DLL. Pose-agent nodes then reported missing Set Walk Pinning Limit / physical foot clamp leeway / trajectory functions. Before a requested fresh launch, check normal binary coverage of the latest changes; build while the editor is already closed when needed. Do not refresh/delete Blueprint pins to compensate for absent native functions. User authorized close/build/reopen; saved pose Blueprint backed up under `Saved/Diagnostics/ReopenMissingNodes-20260921-142937`.
  - Resolved with normal Editor build (16 actions,131.77s), then reopened `/Game/testNN`. All three native functions and Get Launch Trajectory display name verified in the fresh process. Pose-agent Blueprint compiled12:33:51UTC with status3, zero stale native properties/pin types and wiring preserved. Blueprint editor left open; no graph edits or explicit asset saves. Build log: `Saved/Diagnostics/BuildReopenMissingNodes-20260921.log`.
- When an authorized launch is needed, open **/Game/testNN** explicitly. Project-only startup opens heavy /Game/mybasic. Do not delete caches or change startup settings as a shortcut.
- Use apply_patch; preserve unrelated working-tree changes. Check local UE 5.7 source/supported APIs before inventing replacements for nontrivial engine behavior.
- If runtime evidence contradicts the model, investigate the exact symptom instead of stacking compensating patches.

- **Kick ankle extension:** Set Kick Foot Joint Leeway defaults Leeway Cm0 / Return Duration Seconds1. Both physical foot-to-calf joints instantly permit extension along the reference calf length during kickL/kickR; no added sideways/compression allowance or angular changes. Matching axial physical-foot drive allowance prevents the existing endpoint clamp from defeating it. Attack exit returns to0 with no hold, smoothstep over60 ticks per authored second; new kick cancels the return. Reset closes joints/cancels return but retains configuration. Temporary translation constraints and return callbacks retire at0; no existing UObject/native-state layouts changed. See [KickFootJointLeeway](Docs/KickFootJointLeeway.md).
  - **Scene snap corrected2026-09-21:** the presentation-only allowance missed locomotion reconstruction, which immediately solved the calf at reference length. Return now captures each foot's actual presented extension separately and fades it on the existing clock; locomotion tempering/regional recovery and pelvis-inertia reconstruction consume that effective calf length, and NN presentation/physical targets agree between NN updates. Existing knee guidance retained; new kicks/reset/retirement clear the return sidecars, with no new recurring work outside the return. Gameplay patch loaded14:37:16UTC. Identical24s/15-exit capture reduced maximum two-tick physical shortening at exit from5.29→0.18cm right and2.88→0.27cm left. Active target return curves matched smoothstep within0.002cm; maximum calf target rotation steps did not increase overall. Full evidence/limits in [KickFootJointLeeway](Docs/KickFootJointLeeway.md). Owned diagnostic PIE ended; user dirty pose Blueprint preserved, no asset saves.
    - Final Live Coding patch loaded14:40:20UTC; KickFootLeeway, AttackLegClamps and KickLeewayInheritance all passed14:40:53UTC. Final rebuild changed only the float-clock assertion tolerance; gameplay matches the measured patch. Include live changes in the next authorized normal build before a fresh editor launch.
  - NN inheritance: the same positive allowance now temporarily replaces mode-specific foot/calf presentation clamps, through kick and recovery. Shared reference-axis correction applies to published poses and after interpolation; the latter receives each return tick without extra NN inference. At0 ordinary clamp settings resume and the sidecar retires. Raw policy recurrence and rotations are untouched. Kinematic playback inherits it too. Live Coding loaded2026-09-21 14:01:17UTC; KickLeewayInheritance, AttackLegClamps and KickFootLeeway all passed14:02:11UTC. Checks cover unclamped-attack/hard-locomotion transition, shrinking allowance without a new NN sample, original reference length, custom baseline length, rotation/toe preservation and teardown. Pose-agent BP compiled successfully, zero stale native types/pins, wiring preserved. No gameplay rollout or explicit asset save.
  - Live Coding loaded2026-09-21 13:45:39UTC. Three focused tests passed13:46:20UTC: KickFootLeeway (axis target and60-tick lifecycle), Jolt.Joints.FootExtension (actual rotated-parent solver, lateral lock, unchanged angular bounds, return), Jolt.RigWorld.FootExtension (native bridge, in-place update, angular update compatibility, removal/recreation/teardown). Node defaults/reflection verified and pose-agent Blueprint compiled successfully, no stale agent types or wiring changes. No asset saves, restart or scene rollout. Changes remain in Live Coding until the next authorized normal build.

## Project and Git

- Workspace: C:/Users/singerie/Documents/Unreal Projects/Prophecy. Project: GameAnimationSample3.uproject. UE: C:/Program Files/Epic Games/UE_5.7.
- Focused map: /Game/testNN; production map: /Game/mybasic; flat parity/audit map: /Game/locomotion.
- Main Blueprint: /Game/_mygame/locomotion/BP_ProphecyManualPoseAgent; native parent AProphecyAgent.
- Local simulation only; multiplayer and physics replication were explicitly excluded.
- Remote: https://github.com/DTSwink/Prophecy.git. Read actual branch/commit from Git; old recorded hashes are not current state.
- User has limited internet data: push only requested code/scripts/config and explicitly selected assets, not generated models, binaries or caches.
- **Do not re-enable Git LFS or trigger LFS transfers.** Preserve local pass-through overrides. ManualPoseAgent was requested as an ordinary Git file, not permission to push all assets.
- Preserve authored PHAT, accepted checkpoints, collider geometry and intentional per-agent drive differences. Do not change training/viewer behavior to hide a UE presentation issue.

## Accepted checkpoints and pose contract

**Accepted does not mean “best.”** Accepted Walk is July 5 fine-tune **_latest**: Use July 5 Walk Fine Tune = true; Use July 5 Walk Best Checkpoint = false.

- Run checkpoint: `C:\Users\singerie\Documents\Cursor\stepper\training\runs\20260617_234645_ik_ik_full_RESUME_best47200_k32fixed_s05_rootaccelx01_i_e8b756b3\checkpoints\20260617_234645_ik_ik_full_RESUME_best47200_k32fixed_s05_rootaccelx01_i_e8b756b3_init.pt`, SHA-256 `CCC03FEE15E825EBCBCD24F9E71934D515B5133E760114ABE664445042D379C1`.
- Run ONNX: `Content/locomotion/NN/prophecy_lower_body_run_b100.onnx`, SHA-256 `75D9907B6FD62534CD4DC4FC7ED05C9E2DD32D1C173D4530044965887359C209`.
- Accepted Walk checkpoint: `C:\Users\singerie\Documents\Cursor\stepper\training\runs\20260705_142401_ik_walk_finetune_legcap30_idlepin03_from_final\checkpoints\20260705_142401_ik_walk_finetune_legcap30_idlepin03_from_final_latest.pt`, SHA-256 `8BAE21C5B3159D69726CD98867C85000B51D566012708EED36374B2B5A9D4C79`.
- Accepted Walk ONNX: `Content/locomotion/NN/prophecy_lower_body_walk_july5_b100.onnx`, SHA-256 `549C1AC57971BE33AD72BB63EA5E9CA9D60E892027C3B5A8093479FB3A71949F`.
- Optional July 5 `_best` checkpoint: `C:\Users\singerie\Documents\Cursor\stepper\training\runs\20260705_142401_ik_walk_finetune_legcap30_idlepin03_from_final\checkpoints\20260705_142401_ik_walk_finetune_legcap30_idlepin03_from_final_best.pt`, SHA-256 `860934962AD94894E5AF1262DD403D85DA46733E02A6BE4AFC09CF5E1197F64E`. Its ONNX is `Content/locomotion/NN/prophecy_lower_body_walk_july5_best_b100.onnx`, SHA-256 `F2122B104CA946F958C107BBF4967F0592AA94B7DC185AFA3807EEDF2981E30F`.
- Accepted Upper checkpoint (user-selected 2026-09-15): `C:\Users\singerie\Documents\Cursor\stepper\training\runs\20260915_102838_ik_upper_cached_ae1ae4_bs64_allk32_ble_h66961999d3\checkpoints\20260915_102838_ik_upper_cached_ae1ae4_bs64_allk32_blend_noise50_initgaze50each_latest_download2.pt`, step `76,750`, SHA-256 `0597C163F93D447A40E970E0C757DB02E32CCD46C0154A3C037D01E9552B1C94`. Supersedes the August-16 checkpoint; this is the accepted selection, not a claim of best quality.
- Upper ONNX: `Content/locomotion/NN/prophecy_upper_body_b100.onnx`, SHA-256 `6FE1F1ED3D3633D2C9FB4FB6F466F916031FCCC66FFEBF78D2479741FA7B8D4F`; runtime contract: `Content/locomotion/NN/prophecy_upper_body_runtime.json`; exporter: `Tools/NN/ExportProphecyUpperBodyPolicy.py` (default updated to the selected September-15 checkpoint).
- Accepted Dodge upper: C:/Users/singerie/Documents/Cursor/stepper/training/slashes2/saved_defense_checkpoints/dodge_step_205525.pt, SHA-256 256035eb190f0b2d683ea8089b3232936ee7cc93ec4d69aef6034b12a38aafb1. Installed prophecy_dodge_upper.onnx SHA-256 385dc27e5f847bc38c7bd695eae834b1544353d6f639f36e04873bb3606357ba. Frozen lower weights/contracts unchanged. Provenance: Content/locomotion/NN/defense/dodge_checkpoint.json; installer: Tools/NN/InstallDodgeCheckpoint.py. Old 198044 fixtures are original-port references, not current weights.
- Policy cadence is 30 Hz. Root, visible mesh and physical targets share completed-sample interpolation/accumulator phase, including slow frames. Explicit interpolation=false remains an exact-pose override.
- Default interpolation remains Current (Linear / Viewer Rotation); bounded Hermite positions / SLERP rotations is optional.
- Upper is the cached-lower 281-input/90-output policy. Lower owns pelvis/legs; upper predicts its delta; arms resolve through two-bone IK. Sword input comes from the actual held sword.
- Upper startup uses authored M_Neutral_Stand_Idle_Loop frame 0 transported through the lower pelvis.
- Parity requires identical primers, checkpoints, root windows, timing and geometry. The corrected Armed-aligned reference supersedes the early Hook L viewer diagnosis.
- Fixed whole-game 60 Hz configuration is separate from 30 Hz inference; lower rendered FPS can slow gameplay under that configuration. Do not silently change project timing.

References: [interpolation](Docs/NNInterpolationModes.md), [idle initialization](Docs/UpperIdleInitialization.md), [Dodge mismatch evidence](Docs/DodgeHookLViewerMismatch.md).

## Attacks and defense

- **Attack recovery Force Run2026-09-22:** `Set Attack To Locomotion Blend` now has an unchecked-by-default Force Run checkbox. Checked resolves enabled per-region Walk/Run sources to Run, keeping all existing hold/blend durations; Normal and zero-duration regions remain bypassed. Unchecked retains the fine-grained source choices. Resolution happens only in the setter; no new active/idle state layout, clock or per-frame work. Existing pins/names remain, and node refresh verifies their values/connections. Whole-pose-Blueprint compilation and archived library-default repair are included in `Saved/Diagnostics/CheckRecoveryForceRun.py`. [Recovery behavior](Docs/AttackRecoveryBlend.md).

- **GT attack target cylinder2026-09-21:** `Get Valid Attack Target(Agent, Attack, Target)` returns Effective Target, Difference (effective minus wanted), Wanted Target, and Distance To Limit. Per-family GT radii use the first-frame normal self-balancing feet midpoint to the saved GT hit target, ignoring vertical distance, as explicitly clarified by the user. All16 original attack families, including pike, are baked from `slashes2/final_gt_attack_dataset_npz`; no training/model changes. At call time the cylinder is centered on the agent's current normal flat-feet balancing target (works with balancing disabled); height is preserved. `Set Attack Target Extra Reach` has16 named per-attack float pins for each agent, each default50cm on new nodes; finite/nonnegative, invalid input rejects the whole profile. Each target query/margin uses its selected family value. All50 removes overrides. Read-only query, no automatic attack wiring/state changes, no tick/timer/inference or runtime file load. [API, measurements and source provenance](Docs/AttackTargetReach.md). September24 per-attack update compiled162.37s, loaded13:28:21UTC; existing common0 copied to all16 pins with original wiring intact. Pose BP status3/zero stale types after library-default repair, asset left unsaved. Native geometry test and owned PIE passed (16 distinct radii/margins, agent isolation, invalid-input atomicity and default restoration). No new tick/inference; owned PIE ended, no restart. Include in next authorized normal build.

- Attack-to-locomotion recovery now has independent Pelvis/Left Leg/Right Leg Source (Normal/Walk/Run), Duration and Hold controls. Each starts from its chosen checkpoint and blends to current normal locomotion selection, rather than forcing a walk endpoint. Defaults Run/1/0; each duration0 or Normal bypasses that region, all bypassed adds no recovery clock/inference/IK. Separate leg weights propagate through pinning, geometry, published history, recurrence and upper conditioning. Mixed pelvis/leg sources resolve connected legs with existing reconstruction/clamp controls; tempering reuses its accepted solve. Full/half attack exit starts recovery; new specials/reset cancel it. Root motion is unchanged. Normal Editor build passed2026-09-20 (112.83s), nine focused checks passed across headless and final editor runs. Five pose-agent nodes upgraded with old timings/translation wires preserved; status3, no stale native types, original graph links retained. Verified pose-agent saved; Unreal reopened on testNN, no scene rollout. Physical walk/run profiles now follow individual leg weights. See [AttackRecoveryBlend](Docs/AttackRecoveryBlend.md).
- BP_ProphecyManualPoseAgent class defaults contain three populated Name arrays (saved 2026-09-18): attack list = all 16 supported families; melee list = jabL/R, hookL/R, overL/R, headbutt, kickL/R (9); slash list = slashL/R, slashLD/RD, slashLU/RU, plus pike (7). Verified against the runtime family catalog and after Blueprint compilation; no graph wiring or map save. Backup/result: Saved/Diagnostics/AttackLists/20260918-111537.
- Before attacker Armed, queued parry/dodge leaves the defender free to locomote or attack. Current history capture permits the first defense output **on Armed**; the old one-step-later implementation is superseded.
- Both defenses retain locomotion input/facing, mover, magic velocities and root controls. Dodge composes its learned root correction once with the planned window.
- Parry ends on first latched attacker Hit. Dodge ends X policy frames after Hit, default X = 1, adjustable in BP. Explicit stop/replacement/duration/failure cleanup remain.
- Automatic contact-based defense stopping is for **Kinematic defenders only**. Sim/HalfSim stop-on-hit is user Blueprint gameplay.
- Termination queries use actual PHAT/sword shapes, never enlarged training boxes. NN conditioning geometry retains the training contract. Parry Blocker/selected-arm input and its downstream classification were removed.
- Set NN Attack Target changes aim without resetting frame/history/latches. Trigger NN Attack has optional Victim; unplugged means no targeted agent. Defense-state queries include accepted pre-Armed waiting responses.
- **Set Attack Return To Root Balancing(Agent, Use Root Balancing Target=true)** chooses attack-to-locomotion root placement per agent. Default/no call uses the flat feet midpoint; false preserves placement directly below the pelvis. Simulated body positions take priority over visible kinematic bones; root height stays unchanged. Selection and target sampling run only at handoff, without Tick/timer overhead and independently of balancing enable/thresholds. Parry/dodge keep their feet midpoint. Both choices preserve world pose, shift the whole root window and reset the registered magic cube to the root. Direct replacements and unactivated cancellations retain their continuity rules. See [AttackControls](Docs/AttackControls.md).
- Sword collision is suppressed at attack start. Slash/pike restore it on first Armed; melee (punch/kick/headbutt) restores it on first learned Hit (>0.5), including recovery animation. Restoration is latched for the episode. End/cancel/interruption also restores saved responses. Owner exclusions remain separate; this is NN Hit, not a physics Event Hit. Installed via Live Coding 2026-09-19; AttackCollisionPhases passed (UE/native filters, simulated/attached/kinematic swords, lifecycle and stable bodies/joints). No restart or Blueprint/map edits.
- Get NN Attack Colliders returns attack name and striking PHAT bones: punch hand/lowerarm; kick calf/foot plus ball only if separate; headbutt head; slash/pike sword component only. Available before Armed.
- Attack Initialization Mode: Dynamic is default. Static duplicates current lower/upper frames into previous once at attack start, including chains. Subsequent inference is normal; half attacks retain GT ghost placement.
- Independent parry/dodge calf/foot/forearm/hand clamps coexist with attack/locomotion controls.
- KinematicAttackDefenseDemo retains role reversal, interval and parry/dodge selection. Preserve current scene wiring and input debugging; do not reinstall it without a request.

References: [live defense](Docs/LiveDefenseControls.md), [NN defense](Docs/NNDefense.md), [attack controls](Docs/AttackControls.md), [sword phases](Docs/SwordAttackCollision.md), [demo](Docs/KinematicCombatDemo.md), [half attacks](Docs/HalfAttackGTInitialization.md).

## Jolt sweeps, collision and hits

- **Physical foot position targets:** Jolt magnetization targets for `foot_l`/`foot_r` use the current interpolated authored calf transform applied to the reference skeleton's calf-to-foot offset. **Set Physical Foot Target Clamp Leeway** sets a nonnegative radius in cm around that endpoint, shared left/right; Mode selects All (default), Locomotion, Attack (full/half), Parry or Dodge. Default0 preserves exact calf attachment; positive allowance retains the original NN foot position when inside the radius and otherwise projects toward it onto the radius. Already applied during attacks/defenses, independently of NN/presentation clamps. Foot angular targets, calves, thighs and NN/visible kinematic pose are unchanged. Corrected positions also populate target history/lower-feedback target capture. Runs only in existing active physical drive publication; no new tick, timer or pose solve. Zero overrides are removed; world cleanup releases custom settings. Live Coding loaded2026-09-20 17:02UTC; reflected node verified, pose Blueprint status3/no stale types, FootTargetLeeway geometry/mode test passed. No Blueprint edits, saves or restart; scene testing left to the user.
  - Compiled and loaded through Live Coding2026-09-20 10:58 local time; no restart or Blueprint/map edits. Build144.26seconds. No scene test run, per the user's testing preference.

- **Set Physical Material Override runtime Jolt forwarding is restored at the user's request.** After comparison, the user identified the idle sliding as their own setup, not a confirmed regression here. The standard PhysicalMesh node updates live Jolt PHAT friction/restitution and effective combine modes, with contact-cache refresh only when values change. None restores each body's normal UE fallback, including removal of an override present at admission. No polling/rig rebuild; editor material assets, Blueprint wiring and Below damping remain unchanged. The exact previously tested implementation and regression checks were restored. [Details](Docs/JoltRuntimePhysicalMaterials.md).

- **Automatic PHAT sweeps equal Enabled = true, Strength = 1, Max Iterations = 64**, unless overridden per agent.
- Active during attacks (including preparation) and actual parry/dodge checkpoint control. Queued defense waiting for Armed and locomotion alone do not activate them.
- Independent attack/defense flags OR together; ending one cannot disable the other. Defense release/stop and world cleanup remove activation.
- Set Jolt PHAT Sweeps configures permission/tuning for both. Enabled=false or Strength=0 opts out. Enabled=true while idle stores configuration without running sweeps.
- The setter needs its white execution input wired. Self alone does not call it. No setter is needed for automatic defaults.
- Get Jolt PHAT Sweeps reports effective activation and retained tuning. Strength 0–1; iterations 1–128. The setter's Enabled pin default false is distinct from automatic defaults when no override exists.
- Registered PhysicalMesh PHAT bodies, including welded sword, predict translation/rotation after servo writes. Filters remain; response uses normal impulses weighted by mass/inertia. Pairs selected by both agents are processed once.
- This does not enable CCD or alter global substeps. Existing overlaps and subsequent joint solving can still permit penetration; no perfect-contact guarantee.
- Dormant path has empty-registry guards, no shape queries/body scans/history capture. Enabled cost has not been benchmarked.
- Focused activation/contact tests passed 2026-09-18 08:02:52 UTC: defense defaults, disable/zero, overlapping ownership, cleanup, fast crossing, rotation-only contact and momentum. Latest defense change was not a new current-scene contact capture.
- Set/Get Jolt Collision Substeps is a **world-wide minimum**, last writer wins; optional default off, Substeps pin 2, range 1–16. It preserves higher project-required counts.
- Set/Get Jolt Penetration Slop is shared-world; default 2 cm, nonnegative. Temporary 0.1 cm reduced measured blade penetration but was not made the production default.
- Generic Jolt PhysicalMesh hit notifications include self-collisions. **Inspected BP diagnosis:** OtherComp == Other.PhysicalMesh and Other.State == Attacking also pass for an attacker's self-contact. Add **Other != Self** to the AND. This was graph/code analysis, not a runtime pair trace; no BP wiring was changed.
- Standard PhysicalMesh Add Force / Add Torque in Radians with Bone Name route to individual Jolt bodies.
- Per-limb channel/response overrides remember original filters, restore them during attacks/parry/dodge, and reapply locomotion overrides afterward.
- Plain scene/runtime mesh integration has its documented admission rules; do not assume every unrelated Chaos component automatically participates.

References: [sweeps](Docs/JoltPHATSweeps.md), [substeps](Docs/JoltCollisionSubsteps.md), [slop](Docs/JoltPenetrationSlop.md), [hit events](Docs/JoltPhysicalHitEvents.md), [default meshes](Docs/JoltDefaultMeshes.md), [runtime meshes](Docs/JoltRuntimeStaticMeshes.md), [channels](Docs/JoltRuntimeCollisionChannels.md), [integration status](Docs/JoltIntegrationStatus.md).

## Magnetization, feedback, damping and snapshots

- `Print Physical Bone Profiles` is an on-demand Blueprint debug node. It prints each unique PHAT bone as `bone : L=mag A=mag / L=tolerance A=tolerance / D=damping` (no unit suffixes), in stable reference-pose height order (head at top, feet at bottom). Reads currently applied blend/context values; magnetization includes global scales and becomes0 when disabled. Unsupported feedback bones or inbound joints show `n/a`. One keyed block per agent refreshes on Tick; Duration0 is one-frame display, increase for one-shot calls. No automatic tick, profile mutation or work when disconnected; omitted in Shipping/Test builds.
- Print-node validation: Live Coding succeeded2026-09-19; reflected call on the current editor agent returned22 unique PHAT rows, head first/foot_r last; null-agent call returned empty. No PIE, restart, Blueprint rewiring or scene edits.

- Single/Below Set and Blend nodes support Locomotion = Both/Walk/Run and Equipment = Both/Drawn/Sheathed. Existing defaults are Both/Both.
- Four context cells preserve combinations. Actual checkpoint weights blend walk/run; held sword selects equipment. Last writer changes selected cells.
- Attacks temporarily use enabled magnetization 1/1 and controlled tolerance 1000 cm / 1000 degrees, restoring locomotion profiles afterward. Global drive settings, gravity and simulation membership are separate.
- Tolerance is a deadband: 0 feeds realized physics back; within tolerance the NN value stays unchanged; beyond it only residual error is fed back. Lower feedback uses the authored target paired with the completed physics step, transported to the current recurrent frame.
- Controlled channels: pelvis, spine/neck/head, clavicles, upper arms, hands, thighs, feet, balls. Calves/lowerarms are IK results, not independent recurrent feedback channels.
- Timed blends use smoothstep over60 game ticks per authored second; setters cancel affected timelines, replacements start at current values. Conditional cells and snapshot returns use the same convention. All timing callbacks retire when their active work ends. Legacy Tick velocity overwrites can bypass magnetization.
- `Set All Body Magnetization` now discards all active magnetization context/snapshot entries before cancelling ordinary blends and applying the immediate settings. In particular, Enabled=false during an attack is not republished as enabled by the old attack/profile override, later snapshot completion or attack exit. Tolerance/damping timelines and saved snapshot contents are retained. This cancels existing work; a later explicit magnetization command is still a new request.
- Disable/restore regression validated2026-09-19 17:24UTC: Live Coding compile/load succeeded; `SelectionAndAttacks` covers all-body disable during restore in locomotion and attack,70 subsequent ticks, attack exit, tolerance continuation and explicit re-enable. It and `PhysicalBlends.RuntimeAndBatch` passed. No restart, asset save or Blueprint edits.
- **Save Physical Profile Snapshot** after delayed BeginPlay captures body magnetization enabled/scales, supported tolerances and inbound anatomical joint angular damping across all four contexts. Re-save snapshots created before damping support. Sword grip is excluded.
- Snapshots are per-agent runtime data, not disk saves. Name None is a default slot; same name overwrites.
- **Blend Body Magnetization To Snapshot** and **Blend Physical Feedback Tolerance To Snapshot** have Below and All variants. Same name + duration; <=0 is immediate. Missing name/bone returns false/0 without changes.
- **Blend Jolt Joint Angular Damping** has Below and All variants with Both/Walk/Run and Both/Drawn/Sheathed selection. **Blend Joint Angular Damping To Snapshot** has Below/All variants restoring the four saved values. **Get Jolt Joint Angular Damping** reports the effective agent-node setting. Full/half attacks temporarily force zero added damping; underlying profiles resume afterward. Requires a live Jolt rig to write damping. Uniform completed profiles retire their blend/context work; cached applied values permit restoration after rig recreation without repeated joint writes. Default zero stores no applied-value entry.
- **Set Jolt Joint Locomotion Damping Below** adds Parent Bone/Include Parent to the existing Walk Sheathed, Run Sheathed, Walk Drawn and Run Drawn pins. Selects descendant inbound PHAT joints, skips bones without joints, returns the number changed. Include Parent also selects its inbound joint (lowerarm_l: elbow+wrist versus wrist only). Uses the same conditional profile/snapshot/attack suppression path; no recurring subtree scan.
- Below damping compiled/loaded via Live Coding2026-09-19; `Prophecy.Jolt.Character.LocomotionDampingBelow` passed18:28UTC on a live Jolt fixture: Include Parent selection, opposite limb unchanged, invalid-value rejection, attack suppression/restoration and all-zero clearing. No restart or asset edits/saves.
- **Blend To Authored Angular Limits(Agent, Duration Seconds=1)** narrows current PHAT angular freedom to authored ranges, preserving linear settings/frames; free starts at180 degrees, locked axes reach Locked at completion. Jolt intermediate limited angles respect0.5..179.5 degrees. Duration1=60 unpaused game ticks;0 immediate. Retarget starts at current ranges. Immediate joint/all limit setters cancel the pending restore. Active-only world callback is removed on finish/cancel/destruction/world cleanup. The existing automatic Jolt target pipeline still requires agent tick enabled; disabling it stops the rig. This node does not change that requirement or automatically implement death logic.
- Saves capture current blend values, not unfinished destinations. Attack overrides remain; save/restore addresses underlying locomotion profiles.
- Disabled magnetization fades to zero then restores disabled flag/remembered scales. Simulation membership, gravity, global settings and physical state are excluded.
- Saving adds no tick or per-frame snapshot lookup. Restores use existing updates; identical completed profiles release context state, conditional profiles retain normal selection.
- **Initialize Agent Reset / Reset Initial Agents** now include captured magnetization, tolerance and damping profiles (all four locomotion/equipment contexts), global magnetization gate/scales, body membership/gravity, lower-body tempering, component material override and angular limits. Initialize after all agents' delayed setup. Reset cancels outstanding profile/snapshot-return, angular-limit, tempering and gait/recovery blends before restoring; stages damping through Jolt rig recreation. Retains other NN tuning/current simulation mode, and does not reset arbitrary BP variables/timers. Private baselines add no recurring reset work; new Play session required for captures made before this patch. See [AgentReset](Docs/AgentReset.md).
- Reset preserves the original sparse body-settings map: missing entries use native Jolt/HalfSim drive defaults; they must not become explicit simulation/drive exclusions. The first reset test used an already-reset baseline and missed this; the user's hit/reset setup demonstrated upper-body magnetization1/1 becoming0/0 on all3 agents. Regression coverage now compares against pre-reset values and distinguishes missing entries from explicit exclusions. Evidence/scope in the reset document.
- Sparse-map correction installed via Live Coding2026-09-19 19:14UTC. Unchanged hit/reset scene: all3 agents' full printed profiles atframe200 matched the pre-hit frame6 values; upper-body magnetization stays1/1 after reset. Corrected blend-cancellation check also matched its pre-reset baseline at10/90 ticks. No restart or Blueprint/map edits.
- Focused snapshot/context and existing blend tests passed 2026-09-18 07:36:13 UTC. Scene wiring/testing remains with user.
- Damping/limit extensions compiled via Live Coding2026-09-19; `AngularLimitBlend`, `DampingProfileBlend` and `SelectionAndAttacks` passed. Bounded PIE completed17:16UTC: damping0->60 gave30 halfway and60 at completion; Below/snapshot return, print D column, limit timeline, immediate restore and cancellation calls passed. No restart or asset edits/saves. Evidence: `Saved/Diagnostics/damping_limits_blend_result.json`. Limit endpoint/range math is automation-tested; the PIE check covers lifecycle/backend acceptance, not ragdoll visual quality. Attempting to disable actor tick during the diagnostic exposed the existing automatic-target prerequisite; that invalid trial was discarded and normal ticking passed.

References: [snapshots](Docs/PhysicalProfileSnapshots.md), [conditional settings](Docs/ConditionalPhysicalSettings.md), [blends](Docs/PhysicalPropertyBlends.md), [feedback alignment](Docs/LowerPhysicalFeedbackAlignment.md), [joint damping](Docs/JointAngularDamping.md), [damping blends and gradual limits](Docs/JointProfileBlends.md).

## Root, locomotion and inertia

- **Per-agent time dilation2026-09-21:** `Set Agent Time Dilation`/`Get Agent Time Dilation` (default1; finite positive multiplier) change the cadence of complete trained30Hz steps, including mover, both magic sets, lower/upper recurrence and presentation. Continuous-root window timestamps use world seconds; velocity readbacks include the multiplier. Specials reset permanently to1; non-1 sets during active specials fail. Waiting before Armed remains locomotion. Gameplay blends/holds retain60-game-tick timing. Normal clocks retire after a short phase-preserving handoff; all-normal uses the previous scheduler with no extra inference/timer/history work. Fixed100-lane models mean accelerated agents can require extra full-batch calls. Shared Jolt physics remains at world rate and follows faster targets; completed feedback is applied at most once per agent per game tick. Four timing/blend/presentation checks passed; owned PIE verified1/2/0.5 linear/angular rates, independent NN cadence and full/half/parry/dodge reset behavior. Jolt90-frame integration remained finite/bound; physical tracking accuracy is not claimed (post-combat pelvis error max42.10cm). New nodes compiled and pose-agent BP status3, no graph/map edits or saves. [Contract, evidence and limitations](Docs/AgentTimeDilation.md).

- **Mover velocity setters2026-09-21:** `Set Root Velocity` (world XY cm/s) and `Set Root Ang Velocity` (world Z degrees/s), each with Add to Current=false. One-shot current momentum assignment, preserves pose/root and both magic sets; normal input/damping/smoothing/limits resume next policy step. Angular set recomputes stopping target; zero set cancels mover rotation, zero addition is no-op. Existing yaw safety cap retained. Planar mover ignores linearZ/angularXY; existing magic supports independent XYZ. Separate Blueprint library avoids reloading existing root-library defaults. No added tick/state layout. `Get Root Velocity` remains combined applied velocity with angular radians/s. [Details](Docs/RootOffsets.md).
  - Live Coding patch15 loaded17:45UTC; pose-agent Blueprint compiled with status3/no stale agent types, no asset saves or wiring changes. Owned PIE check passed17:45:28UTC: overwrite/add, unchanged opposite channel, both magic sources retained, zero set/add semantics, high yaw rate cap, unchanged immediate pose/root, six subsequent finite frames. User Play was preserved during compilation; test started only after they ended it. Evidence `Saved/Diagnostics/RootVelocitySettersPIE.json`. Normal editor binary still needs these patches folded in before the next authorized fresh launch.
- **Root-only offsets2026-09-21:** `Add Root Offset` (world vector, cm) and `Add Root Angle Offset` (yaw degrees) preserve the current world skeleton. Translation shifts the entire window; yaw shifts window orientations/facing target while preserving world positions and momentum. Rebase recurrent/publication history and cached calf orientation rather than rotating the NN pose. One-shot only, no added tick/state layout/inference. Available in locomotion/half attacks/defense, rejected during full attacks, disabled inference or external bridge. Existing absolute-location node retains pose-moving behavior. Normal bounds/balancing/input still apply afterward. See [RootOffsets](Docs/RootOffsets.md).
  - Live Coding loaded17:19:49UTC. Owned PIE check passed17:24UTC:18 offset operations in kinematic/physical modes, raw/continuous window error below0.000008cm, bone-position and velocity error0; yaw error below0.000013degrees. Includes magic linear/angular velocity, repeated yaw/translation, zero/invalid-angle handling and six follow-up frames. Defense/half-attack paths not scene-tested. Original attempted Play was blocked by archived library CDOs after reloading: explicit editor-only `Prophecy.Editor.RepairLibraryDefaults` repaired86 stale unlinked self defaults, preserving all other values/links (status3), no asset save. Final Blueprint compile verified17:24UTC; only owned PIE ended. Fold patches into the next approved normal build before a fresh launch.
- **Kick balancing exception2026-09-21:** `Set Kick Self Balancing Exception Durations` (Agent, Hold default0, Fade default1), opt-in before the kick;0/0 disables/cancels. A kickL/kickR return to locomotion places the root below the current pelvis, holds the balancing target on the live flat pelvis, then smoothstep blends to the live feet midpoint. Shared60-game-tick duration clock, gate-independent expiry, same target for actual/future roots. Existing spring enable/input/speed/magic gates and manual root-return mode remain. New valid attack, active parry/dodge and reset cancel the active override; configuration persists. Disabled/completed states add no fade ticking or pelvis sampling. Existing camera compensation and magic-cube reset cover the changed kick handoff. [Details](Docs/RootSelfBalancing.md).
  - Live Coding loaded13:11:42UTC (6 actions,287.94s). Reflected node/defaults verified. `Prophecy.Root.KickSelfBalancingException` and `Prophecy.Blends.SixtyTickClock` passed13:12:17UTC: kick-family/return gating, hold/fade/zero boundaries, cancellation/retirement, config persistence, and60 ticks at30/60/120FPS with dilation. No Blueprint wiring/save, gameplay rollout or restart; fold into next normal build before fresh launch.
- **Walk raw pin limit removed2026-09-24:** user no longer wants Set/Get Walk Pinning Limit. Removed stored settings and both raw-value veto passes, including implicit threshold2. Other pinning controls remain. Blueprint migration removed one setter and reconnected execution; pose BP compiled status3 and saved. All six remaining Walk pinning tests pass. The smoothing residual-veto branch was also removed. [Pin controls](Docs/WalkPinningTolerance.md).
- User accepted the retained pelvis recovery hitch change as “perfect” before requesting the Walk pin limit; this supersedes the pending visual-acceptance note in the diagnosis entry below.
- Walk pin verification completed19:14:55UTC: `Prophecy.NN.WalkPinning.Limit` and existing `Tolerance` both passed, covering strict boundary, both winners, no transfer, signed thresholds and soft-fallback coexistence.
- **Pelvis recovery hitch refined2026-09-20:** measured backward-walk slowdowns originate in the NN pelvis target. Exact checkpoint replay isolated reconstructed thigh rotations and their differences, with negligible contribution from small foot-position corrections. During kickR recovery, the LEFT support leg's previous-frame hinge stalls/twists; the right leg is the descending kick. Ground-guidance removal and slow source blends were rejected for sideways knees. The retained bounded support-source handover preserves the accepted forward-knee solve and reduces the hitch across11 recoveries: pelvis position second-difference peaks0.753–1.084cm versus1.408–2.103cm, and both legs' maximum thigh rotation steps decrease. Input packing/history scales still match training, final corrected pose still drives recurrence/upper inference, and normal inactive locomotion adds no source work. No claim of identical foot trajectory or every metric improving: initial right-knee positional acceleration rises9–18% in some later cycles; user visual acceptance remains pending. See working-rule entry above and [evidence/limitations](Docs/PelvisRecoveryHitchDiagnosis-20260920.md).
- **Tempering node pin order:** Feet Translation XY → Translation Z → Rotation, then Pelvis Translation XY → Translation Z → Rotation. Stable internal pin names preserve existing values and connections. Live Coding loaded2026-09-20 16:26UTC; both existing pose-agent calls refreshed with all values/links preserved, Blueprint status3 and no stale native types. SeparateReturns, PhysicalBaseline and SixtyTickClock passed. Blueprint remains unsaved to preserve the user's pending edits. Refresh command: `Prophecy.Editor.RefreshTemperingPinOrder`; no gameplay behavior change.
- **Recovery node pin order:** Pelvis, Left Leg, Right Leg, each ordered Source → Hold Duration Seconds → Blend Duration Seconds. Stable internal pin names preserve values and connections. Live Coding loaded2026-09-20; three existing pose-agent calls reconstructed, all values/links preserved, Blueprint status3 and no stale native types. Existing unsaved Blueprint edits remain unsaved. Explicit refresh command: `Prophecy.Editor.RefreshRecoveryPinOrder`; no gameplay timing/behavior changes.
- `Set Locomotion Lower Body Tempering` has Enabled and six per-agent values: Feet Translation XY/Z, Feet Rotation, Pelvis Translation XY/Z, Pelvis Rotation (all default 1). One preserves normal prediction; zero holds the previous published pose in root-local coordinates, so it travels/turns with the root. Intermediate values follow the new prediction per policy step. Feet rotation includes toe/knee-frame motion. Tempering happens before existing world-space foot pinning; fixed-length leg resolution follows pinning and checkpoint blending, before recurrence/upper conditioning. Uses the pelvis-inertia reach/floor/hinge solver. **Locomotion only: all active specials, including half attacks, full attacks, parries and dodges, bypass tempering and its additional history copies/leg solve.** Pending defense requests remain free to locomote. Settings and return schedules are retained for locomotion; clocks continue rather than restarting on special entry. Disabled/all-one entries are removed, skipping tempering/history copies/extra solves and inference. No retained pose cache. See [LowerBodyTempering](Docs/LowerBodyTempering.md).
- `Blend Locomotion Lower Body Tempering To Normal` captures current controls and independently holds/restores feet and pelvis to1. Each group has Duration(default1) and Hold(default0); XY/Z and rotation return together: duration1=60 game ticks; hold0.5=30 game ticks. Active-only shared clock; sampled on the existing eligible policy path. The ticking clock retires at its deadline even during full attacks/defense; the final result is consumed on return. Completion removes settings/timeline and bypasses tempering work. New Set cancels; new Blend retargets from current values. Duration0 snaps after hold;0/0 immediate. Already-normal allocates nothing.
- Hold/return controls loaded via Live Coding2026-09-19; reflected node signatures verified, LowerTempering.ReturnTimeline and PolicyBlend.AttackRecovery passed (12:33UTC): holds, four-value interpolation, timeline retirement, setter cancellation, run-only hold, blend-boundary residual time and legacy zero-duration recovery bypass. No restart or user asset edits; scene testing remains with user.
- Blend clocks subsequently changed to the requested60-game-tick convention (2026-09-19, loaded via Live Coding). Six focused tests passed, including30/60/120FPS clock inputs, exact hold/return boundaries, callback retirement, physical/profile/snapshot behavior and attack transfer; final profile test passed12:59UTC. [Scope and evidence](Docs/BlendTickTiming.md). Physics/NN scheduling and Blueprint wiring unchanged.
- Tempering exposes exactly **two menu nodes: Set and Blend Locomotion Lower Body Tempering To Normal**. Blend has Feet Duration/Hold and Pelvis Duration/Hold pins, independently restoring each XY/Z translation and rotation group. Completed parts retire independently; both normal bypass all tempering. New Set/Blend replace both schedules. Feet/Pelvis-only compatibility functions are hidden from the menu. Existing Duration/Hold connections now control feet; new pelvis pins default1/0. Same60-tick convention and zero-duration behavior. [Details](Docs/LowerBodyTempering.md).
- **Repeated full-attack diagnosis:** zero tempering holds the first attack's finishing stance between attacks. In the current two-`overL` scene, disabling tempering immediately after the second Trigger produced exactly identical18-step attack inputs/outputs; the weak lower motion is already in the attack prediction. Allowing locomotion recovery in the gap restored a ready stance and much larger second-attack movement. Full attack bypass is working; it does not reset the starting pose. No forced recovery/reset added. Three bounded360-frame captures, no Blueprint/map changes or restart; evidence and scope in [LowerBodyTempering](Docs/LowerBodyTempering.md).
- Two-node interface loaded via Live Coding without restart; SeparateReturns, ReturnTimeline and SixtyTickClock passed2026-09-19 at13:19UTC, including exactly two menu-exposed functions and separate timing through one Blend node. No Blueprint/scene edits.
- Blueprint owns locomotion inputs; do not restore native keyboard polling.
- Continuous root readback follows estimated present root. Future sample spacing is still the policy spacing, not 1/60 s; use returned Time Offsets Seconds.
- Explicit root placement shifts root/window together. Automatic pelvis bounds instead rebase root/window/cube while preserving NN world targets. Do not revive the root/cube runaway.
- Root impulses and forces/torques act through the mover. World linear momentum must not rotate with yaw. Units/scope are documented.
- Two independent magic linear/angular velocity sets have separate setters, additive/overwrite selection and getters; they compose separately from mover motion.
- Root limits, optional pelvis bounds/cube registration and balancing remain per-agent. Balancing includes speed, move-input, tolerance and magic-velocity conditions. Preserve user tuning.
- Future-root smoothing independently controls distance, direction change and orientation. Zero preserves previous root-to-root local relationships; present root is not smoothed.
- Pelvis inertia runs after lower inference with leg-chain correction, before upper conditioning. Hand inertia resolves arms via IK with root-local axes and per-checkpoint values. Defaults preserve existing behavior; disabled paths avoid correction work.
- Lower tempering and zero attack-recovery bypass compiled in the normal Editor target on 2026-09-19 (Unreal was already closed). Four focused headless tests passed: root-local tempering/pin/chain, existing pelvis leg-chain regression and both policy-blend tests. Temporary process exited successfully; no scene/BP edits or extensive rollout testing.
- Walk pin tolerance/fallback, raw/effective diagnostics and near-ground threshold are separate controls. Debug capture is opt-in.
- Automatic run selection above root speed defaults to 100000, distinct from the low-speed walk-checkpoint override.

References: [continuous window](Docs/ContinuousRootWindow.md), [smoothing](Docs/LocomotionRootWindowSmoothing.md), [magic](Docs/RootMagicVelocities.md), [bounds](Docs/RootPelvisBounds.md), [limits](Docs/RootVelocityLimits.md), [balancing](Docs/RootSelfBalancing.md), [impulses](Docs/RootAngularImpulse.md), [pelvis](Docs/PelvisInertia.md), [hand](Docs/HandInertia.md), [pin diagnostics](Docs/FootPinningDebug.md), [walk tolerance](Docs/WalkPinningTolerance.md).

## Other retained systems and remaining risks

- **Attack camera fade2026-09-21:** `Set Attack Camera Offset Fade Duration` (Agent, Duration Seconds=1) fades the possessed full-attacker's added horizontal pelvis offset to zero after attack exit. Smoothstep;1=60 unpaused game ticks,0 immediate. New attack cancels/rebases from residual offset; active-duration changes retime from current value. Loss of possession/teardown clears immediately. No NPC follower/fade; completion removes fade state and disables existing PostPhysics component tick. Component layout unchanged, weak sidecar settings/state, no new timer. [Camera behavior](Docs/SlashFootContactsAndCamera.md).
  - Root-snap correction: attack stop now samples the possessed spring origin across BOTH carrier catch-up and balancing/pelvis return placement, subtracts their actual combined displacement from the applied offset, then fades that combined value using the same node/duration. This preserves world camera pivot through the snap without moving root/NN or counting either move twice. Duration0 remains immediate. New attack preserves residual compensation; no NPC effect or permanent tick added. Camera rotation/arm collision and unrelated Blueprint teleports are outside this translation compensation.
    - Follow-up Live Coding loaded12:55:49UTC (3 actions,226.71s); expanded `Prophecy.Camera.AttackOffsetFade` passed12:56:13UTC. Transient-world test verifies world-pivot continuity across two actual actor moves plus yaw with off-center boom, inverse displacement applied once, combined fade initialization and interrupted-fade continuity. No Blueprint rewiring or explicit save, no gameplay rollout/restart. Retain this patch in the next normal build before fresh launch.
  - Live Coding loaded12:45:59UTC, reflected node/default1 verified, `Prophecy.Camera.AttackOffsetFade` passed12:46:47UTC (tick boundary, duplicate/pause suppression, retiming, zero duration, real transient-world possession/retirement and preservation of independent offset). Pose-agent Blueprint subsequently compiled status3 with zero stale types and wiring intact; no graph edits/save/restart. Reload logged two handled RigVM delegate access-detector ensures during CDO reference replacement, but completed and editor remained responsive. No full gameplay-camera rollout; incorporate this patch in the next normal build before fresh launch.
- **Get Launch Trajectory:** callable node in `PhysicsHitVelocityLibrary`, inputs Start Location, Current Target Location, Current Target Speed (world velocity vector), Angle. Renamed from Get Hit Trajectory through display metadata; internal `GetHitTrajectory` identity retained for existing Blueprint links. User clarified standard elevation0=flat/90=up and closest-pass fallback must retain the angle. Returns world launch velocity plus Exact Hit, Flight Time and Miss Distance. Advanced bounds default10000cm/s and10seconds; Gravity Scale defaults1 against world gravity. Constant target velocity/vertical gravity, no drag/obstacle/radius model. Bounded polynomial-extrema search includes tangent solutions and speed-limit regimes; no tick or retained state. [Usage and mathematical contract](Docs/HitTrajectory.md).
- Hit trajectory Live Coding loaded2026-09-20 at19:35UTC (2 actions,148.56s build). Reflected Blueprint invocation returned the analytic stationary45-degree launch(700,0,700), time1.428571s and negligible miss. `Prophecy.Physics.HitTrajectory` passed19:35:56UTC, including80 deterministic cases against4000 sampled times each. No Blueprint/scene rewiring, asset save or restart; no physical-ball scene test.
- Preserve Kinematic/HalfSim/Sim continuity and distinct backend/controller contracts. Experimental classes/controllers are not production defaults. HalfSim and Sim are not interchangeable.
- Sword grip uses accepted Training sword geometry. Cutting behavior and owner exclusions are separate from attack-phase filtering; old global sword-ignore descriptions are historical.
- Simulated Jolt swords inherit the gripping hand's accepted magnetisation packet (default hand_r): effective linear/angular strengths, gravity compensation, enable state and target trajectory. The grip/socket/body-origin offset is applied after hand-target interpolation on each native substep, so the independent sword drive follows the authored target, not the deflected physical hand. `Break Sword Grip Constraint` removes only the fixed joint, retaining held ownership, drive and collision exclusions for testing; switch simulation off/on or drop/re-equip to restore it. Drop/mode change/body or hand destruction removes the drive. Attached/kinematic swords register no extra drive. Live Coding loaded 2026-09-19; IndependentHandMagnetization and AttackCollisionPhases passed through the shared step path (11:34 UTC). No restart or user asset edits; contact jitter remains for user scene testing. See [SwordMagnetization](Docs/SwordMagnetization.md).
- Repeated overL left-arm separation measured on2026-09-19: actual native elbow anchors reached6.71cm apart at default10 velocity/2 position iterations. 40/2 barely helped;10/8 reached2.77cm;two substeps reached2.75cm;10/32 reached0.55cm (91.75% reduction). Attacker-only10/32 reproduced this result, with other agents retaining default overrides. Existing `Set Jolt Solver Iterations` exposes the tested settings;0/0 restores defaults. No gameplay defaults/assets changed, no restart, no performance claim. Position convergence is implicated, not a demonstrated missing constraint/upstream bug. See [OverL arm investigation](Docs/OverLArmConstraintInvestigation.md).
- **Accepted specials solver policy:** attacks and committed parry/dodge automatically use10 velocity/32 position iterations; exit/cancel/interruption restores that agent's configured locomotion counts (normally0/0 overrides =10/2). Pending pre-Armed defenses do not activate it. Attack/defense flags compose; re-created rigs use effective counts. Solver setter during a special changes the later locomotion values while getter reports effective10/32. Event-only state, no new Tick/poll; teardown removes it. Live Coding loaded2026-09-19, SpecialSolver.Lifecycle passed;180-frame current overL capture confirmed73 attacking samples/native joints at10/32 and107 locomotion samples at0/0, with no mismatched effective samples. Blueprint/map unchanged; no restart.
- Numerical containment preserves requested gains, rejects invalid native state and safely stops the affected shared simulation. Do not suppress assertions or retune PHAT to hide failures.
- Blood, reach, Boss/MetaHuman, rope/noose and boat keeper decisions remain in topic docs and the [archived domain sections](Docs/Journal/ProjectJournal-history-through-2026-09-18.md#other-accepted-project-systems). Do not resume deferred work from historical next-task lists.
- Consult Boss material/asset provenance before reimporting; historical FBX exports are not automatically equivalent to the accepted fitted keeper.
- Performance figures are workload-specific. Headless fixtures and historical packages do not prove current rendered performance or current packaged gameplay.
- User owns extensive visual/contact tuning. Exact frame-90 sword pop was not reproduced in the recorded comparison; startup overlap caused a separate large spike.
- Sword cold-load guard is installed, finishing compilation of the selected mesh before editor physics admission. Specific first-Play failure still needs fresh-process confirmation at the next planned launch; do not restart solely for this.
- Include live changes/new libraries in the next planned normal build before fresh launch. Latest results are live-editor tests, not new packaging acceptance.
- Reinspect current BP before assuming the user has added Other != Self or removed legacy velocity overwrites. The investigation did not edit either.
- No historical pending task becomes active automatically; follow the user's next request.

References: [fight setup](Docs/JoltFightSetup.md), [capability ledger](Docs/JoltCapabilityLedger.md), [accepted performance](Docs/JoltAcceptedPerformance.md), [Boss](Docs/BossUEFNImport.md), [blood bridge](Docs/JoltBloodBridge.md), [blood visual evidence](Docs/JoltBloodVisualValidation.md).

Journal tidy on 2026-09-18 changed documentation only. All previous entries/evidence were preserved in the archive; current sweep defaults and attack/defense gating above supersede old default-off and attack-only records.
